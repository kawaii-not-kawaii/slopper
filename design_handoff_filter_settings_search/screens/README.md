# Screenshots

Captured at 2× from the design canvas (412×892dp frame). Read every px as **dp**.
The HTML canvas is the source of truth — these are for quick reference and PR descriptions.

## Build these
| File | id | Screen |
|---|---|---|
| `2a-filter-sheet-default.png` | 2a | Filter sheet — default state |
| `3a-filter-sheet-custom-duration.png` | 3a | Filter sheet — custom duration + untouched controls |
| `3b-settings.png` | 3b | Settings |
| `3c-search.png` | 3c | Search overlay |

## Before — current build, for comparison
| File | id | Screen |
|---|---|---|
| `1a-before-filter-sheet.png` | 1a | `FilterSheet.kt` as built |
| `1b-before-settings.png` | 1b | `SettingsScreen.kt` as built |
| `1c-before-search.png` | 1c | `SearchOverlay.kt` as built |

Note: 2a and 3a are two states of the same sheet, not two designs — 2a shows an active
filter (duration, resolution, rating, flags set), 3a shows custom duration latched with
every other control untouched.
