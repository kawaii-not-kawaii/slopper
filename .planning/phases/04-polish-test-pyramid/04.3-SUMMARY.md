---
phase: 04-polish-test-pyramid
plan: 3
subsystem: infra
tags: [ci, forgejo, gradle, detekt, ktlint, lint, gitignore, android]

# Dependency graph
requires:
  - phase: 04-polish-test-pyramid/04.1
    provides: PlayerScreen split (PlayerControls.kt + PlayerTimeline.kt) and Throwable catch narrowing — required for detekt re-baseline in Task 1

provides:
  - Forgejo Actions CI workflow (.forgejo/workflows/ci.yml) with correct Gradle cache keys
  - local.properties removed from VCS tracking
  - DEVICE_TESTING.md updated with Phase 2 compliance + Phase 3 regression checks
  - ARCHITECTURE.md updated with Phase 4 structural changes
  - POLISH-02/03 detekt + lint re-baseline DEFERRED pending Plan 4.1 completion

affects: [future-ci, 04.1-completion]

# Tech tracking
tech-stack:
  added: [forgejo-actions, actions/checkout@v4, actions/setup-java@v4, actions/cache@v4]
  patterns: [gradle-cache-keyed-on-libs-versions-toml, bootstrap-sh-ci-pattern]

key-files:
  created:
    - .forgejo/workflows/ci.yml
  modified:
    - DEVICE_TESTING.md
    - .planning/codebase/ARCHITECTURE.md
    - .gitignore (no change — local.properties was already present)
  removed-from-vcs:
    - local.properties

key-decisions:
  - "Task 1 (lint + detekt re-baseline) deferred: Plan 4.1 files exist on disk but are untracked/uncommitted; regenerating baselines before 4.1 commits would capture wrong signatures (List<Marker> still in baseline, Throwable catches not narrowed yet)"
  - "CI workflow uses no lintDebug step per research finding A6 — lint not included in CI build command"
  - "local.properties VCS removal used git rm --cached only — file stays on disk for local SDK builds"
  - "ARCHITECTURE.md Phase 4 Changes section appended (not restructured) to minimize diff"

patterns-established:
  - "Forgejo CI: gradle cache key pattern = wrapper.properties-hash + jdk17 + agp8 + libs.versions.toml-hash"
  - "bootstrap.sh must precede ./gradlew in CI — wrapper jar is not committed"

requirements-completed: [POLISH-08, POLISH-09, POLISH-10]

# Metrics
duration: 15min
completed: 2026-05-19
---

# Phase 4 Plan 3: POLISH-08/09/10 — CI Workflow + Docs + VCS Hygiene Summary

**Forgejo Actions CI workflow created with libs.versions.toml cache key; local.properties removed from VCS; DEVICE_TESTING.md extended with COMPLY/PERF regression checks; lint+detekt re-baseline deferred pending Plan 4.1 commit**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-19T03:41:00Z
- **Completed:** 2026-05-19T03:56:00Z
- **Tasks:** 2 of 3 completed (Task 1 deferred — Plan 4.1 dependency not yet committed)
- **Files modified:** 4

## Accomplishments
- Created `.forgejo/workflows/ci.yml` with correct Gradle cache key including `hashFiles('gradle/libs.versions.toml')` — cache invalidates on dependency version bumps
- Removed `local.properties` from git index (`git rm --cached`) — file remains on disk for local builds, .gitignore already covered it
- Extended `DEVICE_TESTING.md` with Phase 2 platform compliance checks (COMPLY-01/02/04/06) and Phase 3 performance regression checks (PERF-04 shuffle, PERF-09 frame rate)
- Updated `.planning/codebase/ARCHITECTURE.md` with Phase 4 structural changes (PlayerScreen split, PlayerSettings/UiSettings interfaces, ErrorResult + catch narrowing)

## Task Commits

Each task was committed atomically:

1. **Task 2: POLISH-08 — Forgejo Actions CI workflow** - `47ee4a9` (ci)
2. **Task 3a: POLISH-10 — Remove local.properties from VCS** - `91b88b8` (chore)
3. **Task 3b: POLISH-09 — DEVICE_TESTING.md + ARCHITECTURE.md docs refresh** - `4268f5d` (docs)

**Task 1 (POLISH-02/03) deferred** — see Deviations section.

## Files Created/Modified
- `.forgejo/workflows/ci.yml` — Forgejo Actions CI workflow (35 lines, YAML valid)
- `DEVICE_TESTING.md` — +76 lines: Phase 2 compliance checks + Phase 3 regression checks
- `.planning/codebase/ARCHITECTURE.md` — +48 lines: Phase 4 Changes section, refreshed date to 2026-05-19
- `local.properties` — removed from git index (file kept on disk)

## Decisions Made
- Task 1 (lint baseline shrink + detekt re-baseline) deferred until Plan 4.1 commits are visible. `PlayerControls.kt` and `PlayerTimeline.kt` exist on disk as untracked files, and `catch (e: Throwable)` blocks have not been narrowed yet. Regenerating baselines in this state would capture the wrong signatures and the wrong exception types — defeating the purpose of POLISH-02/03.
- CI workflow excludes `lintDebug` per research finding A6 — only `assembleDebug detekt ktlintCheck` in the build step.
- No `.gitignore` edits needed — `local.properties` was already at line 12.

## Deviations from Plan

### Deferred Task

**Task 1 (POLISH-02 + POLISH-03) — Lint + Detekt re-baseline — NOT COMPLETED**

- **Reason:** Plan 4.1 is a declared prerequisite (plan frontmatter: `task1_requires`). PlayerControls.kt and PlayerTimeline.kt exist on disk as untracked files but have not been committed. `catch (e: Throwable)` blocks in `core/data` are still present (not narrowed). Running `detektGenerateBaseline` now would capture `List<Marker>` signatures (not `ImmutableList<Marker>`) and would not remove the `TooGenericExceptionCaught` entries.
- **Status:** Requires Plan 4.1 to commit first. Task 1 should be run as a follow-up once `git log` shows Plan 4.1 commits for PlayerControls.kt, PlayerTimeline.kt, and the Throwable narrowing.
- **Files pending:** `app/lint-baseline.xml`, `feature/player/detekt-baseline.xml`, `core/data/detekt-baseline.xml`
- **Commands ready:**
  ```bash
  # After Plan 4.1 commits:
  ./gradlew :app:updateLintBaseline --no-daemon
  ./gradlew :feature:player:detektGenerateBaseline --no-daemon
  ./gradlew :core:data:detektGenerateBaseline --no-daemon
  ./gradlew detekt ktlintCheck --no-daemon 2>&1 | tail -5
  ```

---

**Total deviations:** 0 auto-fixed. 1 task deferred (planned prerequisite not met).
**Impact on plan:** Tasks 2 and 3 executed fully. Task 1 is a clean deferral — no regressions introduced, baselines are unchanged from current committed state.

## Issues Encountered

None for Tasks 2 and 3.

Task 1 dependency: Plan 4.1 was expected to run in parallel but its commits were not yet visible when Plan 4.3 executed. The plan frontmatter explicitly documents this ordering constraint (`task1_requires` field).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

**Completed (POLISH-08, POLISH-09, POLISH-10):**
- CI workflow is ready and will trigger on push/PR to master
- DEVICE_TESTING.md is up to date for Phase 2/3 verification
- local.properties no longer pollutes git history

**Remaining (POLISH-02, POLISH-03):**
- Wait for Plan 4.1 completion, then run the three `updateLintBaseline` / `detektGenerateBaseline` commands above
- Verify `wc -l app/lint-baseline.xml` <= 700
- Verify `./gradlew detekt ktlintCheck` exits 0

---
*Phase: 04-polish-test-pyramid*
*Completed: 2026-05-19*
