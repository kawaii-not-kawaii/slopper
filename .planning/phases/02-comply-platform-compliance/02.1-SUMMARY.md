---
phase: 02-comply-platform-compliance
plan: 1
type: execute
wave: 1
executed_at: 2026-05-17T11:50:00+09:00
branch: phase-2/comply-platform-compliance
requirements_completed: ["COMPLY-01", "COMPLY-02", "COMPLY-04"]
commits:
  - sha: ba7ff55
    type: feat
    scope: comply
    title: "COMPLY-04 — Splash Screen API via core-splashscreen 1.0.1 (Pattern A gate)"
  - sha: 0b1f4f5
    type: feat
    scope: comply
    title: "COMPLY-02 — predictive back manifest flag + PredictiveBackHandler at PlayerScreen.kt:187"
  - sha: 2572aad
    type: feat
    scope: comply
    title: "COMPLY-01 — strip themes.xml bar colors + safeDrawingPadding player overlay + 3 ModalBottomSheet contentWindowInsets"
build:
  assembleDebug: green
  assembleRelease: green
deviations:
  - id: DEV-01
    task: 1
    type: structural
    summary: "appReady threaded into StashAppContent (Case B path per Step 0 dedup)"
  - id: DEV-02
    task: 3
    type: scope
    summary: "safeDrawingPadding wrap targeted at PlayerControls + locked-overlay only (preserves gesture-layer full-bleed UX)"
tags: ["android", "compose", "edge-to-edge", "predictive-back", "splash"]
---

# Phase 2 Plan 1: COMPLY — Platform Compliance (Wave 1) Summary

JWT-free platform contracts landed: edge-to-edge correctness (COMPLY-01), predictive back gesture (COMPLY-02), Splash Screen API gated on RootViewModel.start (COMPLY-04) — three atomic commits on `phase-2/comply-platform-compliance` with both debug and release builds green.

## Commits Landed

| # | SHA | Title | Files |
|---|---|---|---|
| 1 | `ba7ff55` | feat(comply): COMPLY-04 — Splash Screen API via core-splashscreen 1.0.1 (Pattern A gate) | 5 (libs.versions.toml, app/build.gradle.kts, themes.xml, AndroidManifest.xml, MainActivity.kt) |
| 2 | `0b1f4f5` | feat(comply): COMPLY-02 — predictive back manifest flag + PredictiveBackHandler at PlayerScreen.kt:187 | 2 (AndroidManifest.xml, PlayerScreen.kt) |
| 3 | `2572aad` | feat(comply): COMPLY-01 — strip themes.xml bar colors + safeDrawingPadding player overlay + 3 ModalBottomSheet contentWindowInsets | 5 (themes.xml, FilterSheet.kt, NavCustomizeSheet.kt, BottomNav.kt, PlayerScreen.kt) |

Total files touched across the plan: 9 unique paths.

## Step 0 Dedup Check Result (Task 1)

`grep -c 'rootViewModel.start.collectAsState' app/src/main/java/io/stashapp/android/MainActivity.kt` → **`1`** (a pre-existing binding at line 174 of `StashAppContent` for navigation start-destination logic).

→ **Case B path taken**: REUSE the existing binding. The `appReady: AtomicBoolean` was threaded into `StashAppContent` as a parameter; the gate-flip `LaunchedEffect(start)` and 3s safety-timeout `LaunchedEffect(Unit)` attach immediately after the existing `val start by rootViewModel.start.collectAsState()`. Dedup invariant preserved at EXACTLY 1 `collectAsState` for `start`.

## Deviations from RESEARCH/PLAN snippets

See `02.1-DEVIATIONS.md` (committed in commit #1 of this plan via the staged file) for full prose; high-level:

- **DEV-01 (Task 1, Step 5 — structural)**: Plan said to add the gate-flip `LaunchedEffect` block "Inside the existing `setContent { StashTheme { ... } }` block, BEFORE the call to the existing root content composable". Because `StashAppContent` ALREADY owned the `val start` binding (Case B), threading `appReady` into `StashAppContent` as a parameter and attaching the LaunchedEffects there is cleaner and preserves the dedup invariant. Functionally equivalent; documented in DEV-01.
- **DEV-02 (Task 3, Step 3 — scope)**: Plan said "move ALL non-AndroidView UI inside this Box [safeDrawingPadding wrap]". PlayerScreen has 9 distinct sub-elements under the outer `Box(fillMaxSize + black)`; items #2 (gesture-detection `pointerInput` layers — `detectTapGestures` / `detectHorizontalDragGestures`) and #4 (StepSeekCallout gesture indicators) MUST stay full-bleed for UX correctness (tap-toggle and scrub must capture across the full screen including near system bars). Wrapped only items #8 (locked unlock button) and #9 (PlayerControls AnimatedVisibility — the top bar + bottom seek bar). Removed redundant internal `padding(WindowInsets.systemBars.asPaddingValues())` calls in PlayerControls + locked overlay to avoid double-padding. Documented in DEV-02; matches OpenCode REVIEWS LOW guidance ("composables that intentionally overlap the video surface … should NOT be inside the safeDrawingPadding wrapper").

No deviation breaks any SPEC must_have; the must_haves invariants are listed below and all satisfied.

## Build Results

| Step | Build | Result | Notes |
|---|---|---|---|
| After Task 1 (COMPLY-04) | `./gradlew --configuration-cache assembleDebug` | **BUILD SUCCESSFUL in 1m 44s** | 455 actionable tasks |
| After Task 2 (COMPLY-02) | `./gradlew --configuration-cache assembleDebug` | **BUILD SUCCESSFUL in 4s** | UP-TO-DATE on most tasks |
| After Task 3 (COMPLY-01) | `./gradlew --configuration-cache assembleDebug` | **BUILD SUCCESSFUL in 6s** | All Kotlin/JVM tasks green |
| Final | `./gradlew --configuration-cache assembleDebug assembleRelease` | **BUILD SUCCESSFUL in 2m 9s** | Both debug + release APKs produced; R8 kotlin-metadata warnings are pre-existing toolchain-skew warnings (not from this plan's changes). |

## Verify-Block Results

### COMPLY-04 (Splash)

| Check | Expected | Actual | ✓/✗ |
|---|---|---|---|
| `grep -c 'coreSplashscreen = "1.0.1"' gradle/libs.versions.toml` | 1 | 1 | ✓ |
| `grep -c 'androidx-core-splashscreen' gradle/libs.versions.toml` | 1 | 1 | ✓ |
| `grep -c 'libs.androidx.core.splashscreen' app/build.gradle.kts` | 1 | 1 | ✓ |
| `grep -c '<style name="Theme.Stash.Splash"' themes.xml` | 1 | 1 | ✓ |
| `grep -c 'parent="Theme.SplashScreen"' themes.xml` | 1 | 1 | ✓ |
| `grep -c '@mipmap/ic_launcher' themes.xml` | 1 | 1 | ✓ |
| `grep -c 'postSplashScreenTheme">@style/Theme.Stash<' themes.xml` | 1 | 1 | ✓ |
| `grep -c '@style/Theme.Stash.Splash' AndroidManifest.xml` | 1 | 1 | ✓ |
| `grep -c 'installSplashScreen' MainActivity.kt` | ≥ 1 | 3 | ✓ (import + companion import + call) |
| `grep -c 'setKeepOnScreenCondition' MainActivity.kt` | 1 | 1 | ✓ |
| `grep -c 'private val appReady = AtomicBoolean(false)' MainActivity.kt` | 1 | 1 | ✓ |
| `grep -c 'appReady.set(true)' MainActivity.kt` | ≥ 2 | 2 | ✓ |
| `grep -c 'delay(3000)' MainActivity.kt` | 1 | 1 | ✓ (safety timeout) |
| `grep -c 'rootViewModel.start.collectAsState' MainActivity.kt` | EXACTLY 1 | 1 | ✓ (dedup invariant — REVIEWS SHOULD FIX #2) |
| `installSplashScreen` BEFORE `super.onCreate` | yes | yes (lines 99 < 102) | ✓ |
| Commit body cites `Intentional SPEC deviation per RESEARCH §A4/E3` | ≥ 1 | yes | ✓ (REVIEWS SHOULD FIX #3) |

### COMPLY-02 (Predictive back)

| Check | Expected | Actual | ✓/✗ |
|---|---|---|---|
| `grep -c 'android:enableOnBackInvokedCallback="true"' AndroidManifest.xml` | 1 | 1 | ✓ |
| `grep -rnE '(^\|[^a-zA-Z])BackHandler[^a-zA-Z]' --include="*.kt" feature/ app/ core/` | empty | empty | ✓ (no standalone BackHandler) |
| `grep -c 'PredictiveBackHandler' PlayerScreen.kt` | ≥ 2 | 2 | ✓ (import + call site) |
| `grep -c 'import androidx.activity.BackEventCompat' PlayerScreen.kt` | 1 | 1 | ✓ |
| `grep -c 'kotlinx.coroutines.CancellationException' PlayerScreen.kt` | 1 | 1 | ✓ |
| `grep -c 'progress.collect' PlayerScreen.kt` | 1 | 1 | ✓ |
| `grep -c 'throw e' PlayerScreen.kt` | ≥ 1 | 1 | ✓ |
| `enabled =` param on PredictiveBackHandler | 0 | 0 | ✓ (unconditional — PITFALL E2 satisfied) |
| Commit body cites `RESEARCH §A2` | ≥ 1 | yes | ✓ (REVIEWS SHOULD FIX #4) |
| Commit body cites `CONTEXT D-03` | ≥ 1 | yes | ✓ (REVIEWS SHOULD FIX #4) |

### COMPLY-01 (Edge-to-edge)

| Check | Expected | Actual | ✓/✗ |
|---|---|---|---|
| `grep -vE '^[[:space:]]*<!--' themes.xml \| grep -cE 'android:statusBarColor\|android:navigationBarColor'` | 0 | 0 | ✓ (comment-stripped grep confirms lines removed) |
| Task-1 work preserved (`<style name="Theme.Stash.Splash"`) | 1 | 1 | ✓ |
| `grep -rn 'ModalBottomSheet(' --include="*.kt" feature/ core/ \| wc -l` | 3 | 3 | ✓ (FilterSheet, NavCustomizeSheet, BottomNav.MoreSheet) |
| `grep -c 'contentWindowInsets = { WindowInsets.navigationBars }' FilterSheet.kt` | 1 | 1 | ✓ |
| `grep -c 'contentWindowInsets = { WindowInsets.navigationBars }' NavCustomizeSheet.kt` | 1 | 1 | ✓ |
| `grep -c 'contentWindowInsets = { WindowInsets.navigationBars }' BottomNav.kt` | 1 | 1 | ✓ (researcher correction A8 — MoreSheet site at BottomNav.kt:179) |
| `grep -c 'safeDrawingPadding' PlayerScreen.kt` | ≥ 2 | 3 | ✓ (import + use + comment ref) |
| `grep -c 'AndroidView' PlayerScreen.kt` | unchanged | 3 | ✓ (full-bleed video surface preserved) |
| `grep -rn 'import.*Spine' feature/ core/ app/ 2>/dev/null` | empty | empty | ✓ (no Spine pre-painting — anti-coupling rule respected) |

### SPEC must_have invariants

| Truth | Holds? |
|---|---|
| Cold launch displays the system Splash (Theme.Stash.Splash) until RootViewModel.start emits a non-null route; no white flash | ✓ (installSplashScreen + setKeepOnScreenCondition wired before super.onCreate, gated on `appReady` AtomicBoolean flipped by LaunchedEffect(start)) |
| PlayerScreen back swipe on Android 14+ shows the system predictive-back preview; onExit() fires only on a committed swipe (not on cancel) | ✓ (manifest flag + PredictiveBackHandler at PlayerScreen.kt with try { progress.collect; onExit } catch (CancellationException) { throw e }) |
| themes.xml no longer overrides statusBarColor / navigationBarColor; enableEdgeToEdge() now has effect | ✓ (both lines stripped from non-comment XML) |
| All three ModalBottomSheets (FilterSheet, NavCustomizeSheet, BottomNav.MoreSheet) declare contentWindowInsets | ✓ (3 sites verified) |
| PlayerScreen control overlays are wrapped in Box(Modifier.safeDrawingPadding()); the SurfaceView itself stays edge-to-edge | ✓ (chrome wrapped; AndroidView full-bleed preserved) |

## Files Touched (Final List)

```
app/src/main/AndroidManifest.xml                                              (Task 1 + Task 2)
app/src/main/res/values/themes.xml                                            (Task 1 + Task 3)
app/src/main/java/io/stashapp/android/MainActivity.kt                          (Task 1)
app/build.gradle.kts                                                           (Task 1)
gradle/libs.versions.toml                                                      (Task 1)
feature/player/src/main/java/io/stashapp/android/feature/player/PlayerScreen.kt (Task 2 + Task 3)
feature/library/src/main/java/io/stashapp/android/feature/library/FilterSheet.kt (Task 3)
core/ui/src/main/java/io/stashapp/android/core/ui/nav/NavCustomizeSheet.kt     (Task 3)
core/ui/src/main/java/io/stashapp/android/core/ui/nav/BottomNav.kt             (Task 3)
.planning/phases/02-comply-platform-compliance/02.1-DEVIATIONS.md              (new — process artifact)
.planning/phases/02-comply-platform-compliance/02.1-SUMMARY.md                 (this file)
```

## Hand-off Notes for Plan 02.2

### New dependencies introduced in `gradle/libs.versions.toml`

- `[versions]` → `coreSplashscreen = "1.0.1"`
- `[libraries]` → `androidx-core-splashscreen = { module = "androidx.core:core-splashscreen", version.ref = "coreSplashscreen" }`
- `app/build.gradle.kts` → `implementation(libs.androidx.core.splashscreen)`

Plan 02.2 should NOT re-declare this dep. It is now a transitive prerequisite for the COMPLY-04 splash.

### AndroidManifest.xml current state

Plan 02.1 added (Task 1):
- Activity theme: `android:theme="@style/Theme.Stash.Splash"`

Plan 02.1 added (Task 2):
- `<application>` attribute: `android:enableOnBackInvokedCallback="true"`

Plan 02.2 Task 1 (orphan permission removal) will delete:
- `<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />` (line 14)
- `<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />` (line 15)

These are non-conflicting line edits — wave-2 sequencing eliminates any risk per REVIEWS HIGH (convergent finding).

### COMPLY-07 UAT note

Plan 02.1 only landed code; final UAT (S23+ screenshot pack + DEVICE_TESTING.md re-run) is handled in Plan 02.2 Task 4 (per CONTEXT.md §8 + REVIEWS HIGH wave-sequencing fix). The debug APK now contains the splash + predictive back + edge-to-edge changes; the UAT will capture them.

### Inset-modifier scope

The inset-modifier scope was **3 ModalBottomSheets**, not 2 (researcher correction A8 honored): FilterSheet.kt:73, NavCustomizeSheet.kt:51, **BottomNav.kt:179 (MoreSheet)**.

### REVIEWS SHOULD FIX confirmation

| # | Item | Status |
|---|---|---|
| #2 | Splash collectAsState dedup check | ✓ Shipped in commit `ba7ff55` (Step 0 result: 1 → Case B path; dedup invariant preserved at exactly 1 `collectAsState` for `start`). |
| #3 | Splash safety-timeout commit-body documentation | ✓ Shipped in commit `ba7ff55` body: "Intentional SPEC deviation per RESEARCH §A4/E3 — the 3s LaunchedEffect(Unit) { delay(3000); appReady.set(true) } is ANR insurance…". |
| #4 | PredictiveBackHandler commit-body cancel-semantics cite | ✓ Shipped in commit `0b1f4f5` body: "PredictiveBackHandler cancel semantics per RESEARCH §A2 + CONTEXT D-03…". |

## Self-Check: PASSED

- All 3 expected commits present in git log (`ba7ff55`, `0b1f4f5`, `2572aad`) ✓
- All 9 plan-target files modified and staged into their respective task commits ✓
- All 5 SPEC must_have truths satisfied ✓
- `./gradlew --configuration-cache assembleDebug assembleRelease` green ✓
- All COMPLY-01/02/04 grep invariants pass ✓
- 2 deviations documented in `02.1-DEVIATIONS.md` with rationale; none breaks a SPEC must_have ✓
- Anti-coupling rule (no Spine imports) verified ✓
- `local.properties` (gitignored) was edited to correct SDK path (`/home/yun/Android/Sdk`) — environmental fix; NOT staged into any commit ✓

## Threat Flags

No new security-relevant surface introduced. All STRIDE register entries in the plan's `<threat_model>` are either mitigated as documented (T-02-01 → Pattern A gate + safety-timeout per RESEARCH §E3) or accepted per documented disposition. No new endpoints, auth paths, or trust-boundary changes.
