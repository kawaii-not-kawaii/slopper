---
phase: 02-comply-platform-compliance
plan: 2
type: execute
wave: 1
depends_on: ["phase-1"]
files_modified:
  - app/src/main/AndroidManifest.xml
  - app/build.gradle.kts
  - feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt
  - app/src/main/res/values/strings.xml
  - .planning/REQUIREMENTS.md
  - .planning/phases/02-comply-platform-compliance/02-UAT.md
  - .planning/phases/02-comply-platform-compliance/screenshots/home-light.png
  - .planning/phases/02-comply-platform-compliance/screenshots/home-dark.png
  - .planning/phases/02-comply-platform-compliance/screenshots/library-light.png
  - .planning/phases/02-comply-platform-compliance/screenshots/library-dark.png
  - .planning/phases/02-comply-platform-compliance/screenshots/player-controls-visible-light.png
  - .planning/phases/02-comply-platform-compliance/screenshots/player-controls-visible-dark.png
  - .planning/phases/02-comply-platform-compliance/screenshots/player-controls-hidden-light.png
  - .planning/phases/02-comply-platform-compliance/screenshots/player-controls-hidden-dark.png
  - .planning/phases/02-comply-platform-compliance/screenshots/settings-light.png
  - .planning/phases/02-comply-platform-compliance/screenshots/settings-dark.png
  - .planning/phases/02-comply-platform-compliance/screenshots/connection-light.png
  - .planning/phases/02-comply-platform-compliance/screenshots/connection-dark.png
  - .planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md
autonomous: false
requirements: ["COMPLY-03", "COMPLY-05", "COMPLY-06", "COMPLY-07"]
tags: ["android", "manifest-hygiene", "locales", "uat", "screenshots"]
user_setup:
  - service: "Galaxy S23+ Android 16 (gesture nav) device"
    why: "Manual UAT screenshot capture + DEVICE_TESTING.md re-run for COMPLY-07 (gesture-nav rows; 3-button-nav rows DEFERRED per COMPLY-07-3BTN backlog)"
    env_vars: []
    dashboard_config:
      - task: "Install debug APK via adb after Plan 2.1 + Plan 2.2 Tasks 1-3 land green; toggle system theme between Light and Dark; capture 12 PNG screenshots per CONTEXT.md Decision 8 layout; rename per `<screen>-<theme>.png` schema"
        location: "Physical S23+ device + adb (or device's built-in screenshot)"

must_haves:
  truths:
    - "AndroidManifest.xml declares only INTERNET, ACCESS_NETWORK_STATE, FOREGROUND_SERVICE (the two orphan permissions are gone)"
    - "Tapping the Language row in Settings on an API 33+ device opens the system per-app language dialog (Settings.ACTION_APP_LOCALE_SETTINGS)"
    - "Below API 33 the LanguageRow does not render — no crash, no visible row"
    - "AGP-generated locale_config.xml exists under app/build/intermediates after assembleDebug, and the merged debug manifest references android:localeConfig=\"@xml/locale_config\""
    - "02-UAT.md exists with full DEVICE_TESTING.md rows for S23+ gesture-nav (all PASS or DEFERRED-with-justification); 3-button-nav rows uniformly DEFERRED → COMPLY-07-3BTN"
    - ".planning/REQUIREMENTS.md Deferred section contains COMPLY-07-3BTN and COMPLY-02-NAV-EVENT entries with traceback to this phase"
    - "12 PNG screenshots exist under .planning/phases/02-comply-platform-compliance/screenshots/ with the locked naming schema, plus a SCREENSHOTS.md index documenting device + build SHA + reviewer line"
  artifacts:
    - path: "app/src/main/AndroidManifest.xml"
      provides: "Cleaned permission block (FOREGROUND_SERVICE preserved; FGS_MEDIA_PLAYBACK + POST_NOTIFICATIONS removed)"
    - path: "app/build.gradle.kts"
      provides: "androidResources { generateLocaleConfig = true } inside android { } block"
      contains: "generateLocaleConfig = true"
    - path: "app/src/main/res/values/strings.xml"
      provides: "settings_language + settings_language_description resource keys"
      contains: "settings_language"
    - path: "feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt"
      provides: "LanguageRow composable + call site, gated by Build.VERSION.SDK_INT >= 33"
      contains: "ACTION_APP_LOCALE_SETTINGS"
    - path: ".planning/REQUIREMENTS.md"
      provides: "COMPLY-07-3BTN + COMPLY-02-NAV-EVENT backlog entries in Deferred section"
      contains: "COMPLY-07-3BTN"
    - path: ".planning/phases/02-comply-platform-compliance/02-UAT.md"
      provides: "DEVICE_TESTING.md re-run results + REVIEWS-C4 ACCEPT line for 3-button-nav deferral"
      contains: "COMPLY-07-3BTN"
    - path: ".planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md"
      provides: "Screenshot pack index (12 PNG inventory + device/build/reviewer metadata)"
      min_lines: 30
  key_links:
    - from: "feature/settings/.../SettingsScreen.kt LanguageRow"
      to: "android.provider.Settings.ACTION_APP_LOCALE_SETTINGS"
      via: "Intent fired via context.startActivity()"
      pattern: "ACTION_APP_LOCALE_SETTINGS"
    - from: "app/build.gradle.kts androidResources block"
      to: "app/build/intermediates/.../locale_config.xml (AGP-generated)"
      via: "AGP scans values-*/ at build time"
      pattern: "generateLocaleConfig = true"
    - from: ".planning/REQUIREMENTS.md Deferred section"
      to: ".planning/phases/02-comply-platform-compliance/02-CONTEXT.md Accepted Risks"
      via: "Backlog entries cite Phase 2 CONTEXT.md as origin"
      pattern: "COMPLY-07-3BTN"

---

<objective>
Land the manifest-and-permission contracts that do not touch UI layout: orphan permission removal (COMPLY-03, COMPLY-05), per-app language picker (COMPLY-06), and the manual UAT artifact set + REVIEWS-C4 ACCEPT for 3-button-nav (COMPLY-07 partial). Parallel-safe with Plan 2.1 — disjoint surfaces.

Purpose: Reduce attack surface (two orphan permissions deleted), expose per-app locale via system dialog, and produce the screenshot pack + UAT writeup that gates Phase 2 exit. Backlog seeding for COMPLY-07-3BTN and COMPLY-02-NAV-EVENT closes the REVIEWS-C4 deferral hygiene loop established in Phase 1 (DEPS-16 pattern).

Output: 5 atomic commits — COMPLY-05, COMPLY-03, COMPLY-06, backlog-seed, and the final screenshot pack + UAT writeup (the 8th commit overall when combined with Plan 2.1's 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-comply-platform-compliance/02-SPEC.md
@.planning/phases/02-comply-platform-compliance/02-CONTEXT.md
@.planning/phases/02-comply-platform-compliance/02-RESEARCH.md
@.planning/phases/01-deps-foundation-bump/01-UAT.md
@app/src/main/AndroidManifest.xml
@app/build.gradle.kts
@app/src/main/res/values/strings.xml
@feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt
@DEVICE_TESTING.md

<interfaces>
<!-- Existing types/APIs Task executors will consume. -->

System Intent (already on Android since API 33):
```kotlin
// android.provider.Settings
const val ACTION_APP_LOCALE_SETTINGS: String = "android.settings.APP_LOCALE_SETTINGS"
// Used as: Intent(Settings.ACTION_APP_LOCALE_SETTINGS).apply { data = Uri.fromParts("package", packageName, null) }
```

Material3 ListItem (Compose BOM 2026.05.00):
```kotlin
@Composable
fun ListItem(
    headlineContent: @Composable () -> Unit,
    modifier: Modifier = Modifier,
    overlineContent: @Composable (() -> Unit)? = null,
    supportingContent: @Composable (() -> Unit)? = null,
    leadingContent: @Composable (() -> Unit)? = null,
    trailingContent: @Composable (() -> Unit)? = null,
)
```

material-icons-extended (already on classpath via Compose BOM):
```kotlin
// androidx.compose.material.icons.outlined
val Icons.Outlined.Language: ImageVector  // globe icon
```

AGP androidResources DSL (AGP 8.1+, current 8.7.3 supports):
```kotlin
android {
    androidResources {
        generateLocaleConfig: Boolean
    }
}
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → system Settings activity | LanguageRow fires an explicit-by-action Intent at android.settings; resolves to system component only |
| Manifest → install-time permission grant | Removing FGS_MEDIA_PLAYBACK + POST_NOTIFICATIONS shrinks the requested-permission set |
| AGP → merged manifest | generateLocaleConfig auto-writes android:localeConfig (build-time, not runtime) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Removed POST_NOTIFICATIONS permission | mitigate | Permission removed entirely; net-positive on attack surface. No notification code path exists in repo (verified pre-removal grep — RESEARCH A7). |
| T-02-06 | Information Disclosure | Removed FOREGROUND_SERVICE_MEDIA_PLAYBACK permission | mitigate | Permission removed; no MediaSessionService exists in repo. BG-MEDIA milestone owns re-introduction with proper service. |
| T-02-07 | Information Disclosure | LanguageRow Intent target | accept | Settings.ACTION_APP_LOCALE_SETTINGS resolves only to the system Settings activity; third-party apps cannot intercept (system component, not exported by any app). Uri.fromParts uses only context.packageName (constant, not user input). |
| T-02-08 | Tampering | generateLocaleConfig build flag | accept | Build-time read of values-*/ directories; no runtime effect; AGP's manifest merger writes the localeConfig reference. No security boundary. |
| T-02-09 | Denial of Service | LanguageRow click handler | accept | Synchronous startActivity call; no resource exhaustion path. If system Settings activity is missing (impossible on real device), ActivityNotFoundException would surface — acceptable failure mode (cosmetic, recoverable). |
</threat_model>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: COMPLY-03 + COMPLY-05 — Remove orphan permissions (POST_NOTIFICATIONS, FOREGROUND_SERVICE_MEDIA_PLAYBACK)</name>
  <files>app/src/main/AndroidManifest.xml</files>

  <read_first>
    - app/src/main/AndroidManifest.xml (entire file) — confirm lines 13 + 15 (per SPEC.md scout) match: `<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />` and `<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />`. Confirm line for `<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />` is PRESENT (must be preserved).
    - .planning/phases/02-comply-platform-compliance/02-CONTEXT.md — Decision §7 (sed-style line removal; default = two separate commits)
    - .planning/phases/02-comply-platform-compliance/02-RESEARCH.md — Assumption A7 (re-run notification grep before COMPLY-03 commit as a safety check)
  </read_first>

  <action>
**Implement per CONTEXT.md Decision 7 (two separate commits, locked default). RESEARCH A7 is a precondition for COMPLY-03 — re-run the notification call-site grep to confirm zero matches before deleting the permission.**

### Step 1 — Pre-removal safety grep (RESEARCH A7)

Run:
```bash
cd /home/yun/slopper
grep -rn 'NotificationManager\|NotificationCompat\|\.notify(' --include="*.kt" feature/ app/ core/
```

Expected output: empty (zero matches). If ANY match exists, STOP — this removal is no longer safe; the planner's assumption is wrong, and a runtime request flow must be designed instead. Per CONTEXT.md, no such call sites should exist; if grep reports any, escalate by appending findings to `.planning/phases/02-comply-platform-compliance/02-CONTEXT.md` `## Open Questions` and pausing for user input.

### Step 2 — COMPLY-05 commit: remove FOREGROUND_SERVICE_MEDIA_PLAYBACK

In `app/src/main/AndroidManifest.xml`, DELETE this exact line:
```xml
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
```

KEEP this line UNCHANGED (it sits immediately before the deleted line per SPEC.md scout):
```xml
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
```

Commit message:
```
chore(comply): COMPLY-05 — remove orphan FOREGROUND_SERVICE_MEDIA_PLAYBACK permission
```

### Step 3 — COMPLY-03 commit: remove POST_NOTIFICATIONS

In `app/src/main/AndroidManifest.xml`, DELETE this exact line:
```xml
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
```

Commit message:
```
chore(comply): COMPLY-03 — remove orphan POST_NOTIFICATIONS permission
```

### Why two separate commits (CONTEXT.md Decision 7)
Each removal is independently bisect-meaningful. The optional fold-to-one path (single `COMPLY-03+05` commit) is documented but DEFAULT is two atomic commits — the planner takes the default.

### Build verification between commits
After each commit run `./gradlew --configuration-cache assembleDebug` — both must succeed.
  </action>

  <verify>
    <automated>cd /home/yun/slopper && grep -c 'android.permission.POST_NOTIFICATIONS' app/src/main/AndroidManifest.xml ; grep -c 'FOREGROUND_SERVICE_MEDIA_PLAYBACK' app/src/main/AndroidManifest.xml ; grep -c '"android.permission.FOREGROUND_SERVICE"' app/src/main/AndroidManifest.xml ; grep -rn 'NotificationManager\|NotificationCompat\|\.notify(' --include="*.kt" feature/ app/ core/ ; echo --- ; ./gradlew --configuration-cache assembleDebug 2>&1 | tail -10 ; echo --- ; git log --format=%s -n 3 | head -3</automated>
  </verify>

  <acceptance_criteria>
- `grep -c 'android.permission.POST_NOTIFICATIONS' app/src/main/AndroidManifest.xml` returns `0`
- `grep -c 'FOREGROUND_SERVICE_MEDIA_PLAYBACK' app/src/main/AndroidManifest.xml` returns `0`
- `grep -c '"android.permission.FOREGROUND_SERVICE"' app/src/main/AndroidManifest.xml` returns `1` (base FGS preserved per CONTEXT.md §7)
- `grep -rn 'NotificationManager\|NotificationCompat\|\.notify(' --include="*.kt" feature/ app/ core/` returns no lines (RESEARCH A7 safety re-check)
- `./gradlew --configuration-cache assembleDebug` exits 0
- `git log -n 2 --format=%s` shows both `chore(comply): COMPLY-05 —` and `chore(comply): COMPLY-03 —` in the most recent 2 commits (order of the pair is not significant)
  </acceptance_criteria>

  <done>
Two atomic commits landed; grep set clean; assembleDebug green; base FGS preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: COMPLY-06 — Per-app language (generateLocaleConfig + LanguageRow + strings + assembleDebug verification of generated locale_config.xml)</name>
  <files>app/build.gradle.kts, app/src/main/res/values/strings.xml, feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt</files>

  <read_first>
    - app/build.gradle.kts (entire file) — locate the `android { … }` block, find `defaultConfig { }` and adjacent siblings; the `androidResources { }` block goes as a sibling of `defaultConfig` per RESEARCH Pattern B5
    - app/src/main/res/values/strings.xml (entire file) — confirm current content is `<string name="app_name">Stash</string>` only (RESEARCH grep verified this); identify the insertion point for the two new strings
    - feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt (entire file, 412 lines per SPEC scout) — inspect existing row composable patterns (e.g., SectionHeader / SwitchPref / SliderPref structure). Identify the existing top-level Column / scrollable content area where the new LanguageRow will be invoked. Per CONTEXT.md Decision 6: place LanguageRow as a top-level row above any "Advanced" / "Diagnostics" section, below the connection block. Per RESEARCH Open Question §4: if no "Connection" or "Advanced" header exists, place the LanguageRow at the very top of the scrollable Column, optionally introduced by a `SectionHeader("App")` if existing rows are grouped under section headers.
    - .planning/phases/02-comply-platform-compliance/02-RESEARCH.md — Pattern B5 (generateLocaleConfig build wiring); Pattern B6 (LanguageRow composable + strings); Pitfall E4 (single-locale dialog acceptable) + Pitfall E6 (do NOT manually add android:localeConfig to source manifest); Open Question §3 (resources.properties)
    - .planning/phases/02-comply-platform-compliance/02-CONTEXT.md — `<decisions>` section §6 (placement + API gate + string keys all locked)
  </read_first>

  <action>
**Implement per CONTEXT.md Decision 6 (top-level row, API-33 gated, ACTION_APP_LOCALE_SETTINGS — locked). Single atomic commit covering all 3 files.**

### Step 1 — `app/build.gradle.kts` — add `generateLocaleConfig`

Inside the `android { … }` block, add as a sibling of `defaultConfig { }`:
```kotlin
    // COMPLY-06 (per D-06): AGP scans values-*/ directories at build time and
    // generates app/build/intermediates/generated_res/.../locale_config.xml,
    // auto-merging <application android:localeConfig="@xml/locale_config" />
    // into the final manifest. No source-tree xml/locale_config.xml needed.
    // (Pitfall E6 warns against manually adding android:localeConfig to the
    // source manifest — AGP's merger handles it.)
    androidResources {
        generateLocaleConfig = true
    }
```

Do NOT touch `defaultConfig { }`. Do NOT manually edit `app/src/main/AndroidManifest.xml` to add `android:localeConfig` (Pitfall E6 — AGP writes it).

### Step 2 — `app/src/main/res/values/strings.xml` — add two new strings

Add the following two `<string>` entries inside the `<resources>` element (after the existing `app_name` entry):
```xml
    <!-- COMPLY-06 (per D-06): per-app language picker (API 33+) -->
    <string name="settings_language">Language</string>
    <string name="settings_language_description">Choose the language for this app independently from your device.</string>
```

### Step 3 — `feature/settings/.../SettingsScreen.kt` — add LanguageRow + call site

**Imports to ADD (alphabetical with existing):**
```kotlin
import android.content.Intent
import android.net.Uri
import android.os.Build
import android.provider.Settings
import androidx.compose.foundation.clickable
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.outlined.Language
import androidx.compose.material3.Icon
import androidx.compose.material3.ListItem
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.platform.LocalContext
import androidx.compose.ui.res.stringResource
import io.stashapp.android.R
```

(Remove duplicates if any of these are already imported. Confirm `R` reference path matches whatever pattern feature/settings uses — if the file uses a feature-module R class, use that instead; the string is in `app/` so `io.stashapp.android.R` is the canonical reference.)

**Composable to ADD** (place near other private row composables in the file — after existing SectionHeader / SwitchPref-style helpers, before the public SettingsScreen composable):

```kotlin
/**
 * COMPLY-06 (per D-06): system per-app language picker entry point.
 * Renders nothing below API 33 (the ACTION_APP_LOCALE_SETTINGS Intent only
 * resolves on Tiramisu+). With Slopper's English-only resources today, the
 * dialog opens but shows "App default" only — expected behavior per Pitfall E4;
 * translation work is out of scope for this milestone.
 */
@Composable
private fun LanguageRow() {
    if (Build.VERSION.SDK_INT < Build.VERSION_CODES.TIRAMISU) return

    val context = LocalContext.current
    ListItem(
        headlineContent = { Text(stringResource(R.string.settings_language)) },
        supportingContent = { Text(stringResource(R.string.settings_language_description)) },
        leadingContent = {
            Icon(
                imageVector = Icons.Outlined.Language,
                contentDescription = null,
            )
        },
        modifier = Modifier.clickable {
            val intent = Intent(Settings.ACTION_APP_LOCALE_SETTINGS).apply {
                data = Uri.fromParts("package", context.packageName, null)
            }
            context.startActivity(intent)
        },
    )
}
```

**Call site:** add `LanguageRow()` to the main settings list. Per RESEARCH Open Question §4 + CONTEXT.md Decision 6 (Claude's Discretion for exact placement): insert at the TOP of the scrollable Column inside SettingsScreen, BEFORE the first existing section. If the existing file groups sections under a `SectionHeader(...)`-style helper, introduce the LanguageRow under a `SectionHeader("App")` header for visual consistency; otherwise place the bare `LanguageRow()` as the first row. Inspect the actual file structure during execution and pick the placement that matches the file's existing voice — do NOT introduce a new section header pattern that doesn't already exist in the file.

**Anti-coupling reminder (CONTEXT.md `<spine_forward_compatibility>`):** Use Material3 `ListItem` + `Icons.Outlined.Language`. Do NOT introduce Spine palette/typography tokens; the row uses current Slopper visual identity. Phase 5 SPINE will re-style this row.

### Step 4 — Build & verify locale_config.xml generation

After the commit:
```bash
cd /home/yun/slopper
./gradlew clean assembleDebug
find app/build/intermediates -name 'locale_config.xml'
```

The `find` MUST return at least one file path. If it returns empty, re-check the build.gradle.kts indentation/scope (must be a direct child of `android { }`, not nested inside `defaultConfig { }`).

If the build log emits a warning like `WARN: generateLocaleConfig requires resources.properties to declare unqualifiedResLocale` (RESEARCH Open Question §3), create `app/src/main/res/resources.properties` with:
```
unqualifiedResLocale=en
```
and amend the COMPLY-06 commit (`git add` + `git commit --amend --no-edit`) so the warning doesn't ship. If no warning, do NOT create the file — keep the change minimal.

### Atomic commit
Stage all changes (3 files modified, optionally 1 new resources.properties) in ONE commit with message:
```
feat(comply): COMPLY-06 — per-app language picker (generateLocaleConfig + LanguageRow + ACTION_APP_LOCALE_SETTINGS)
```
  </action>

  <verify>
    <automated>cd /home/yun/slopper && ./gradlew clean assembleDebug 2>&1 | tail -30 ; echo --- ; grep -c 'generateLocaleConfig = true' app/build.gradle.kts ; grep -c 'settings_language' app/src/main/res/values/strings.xml ; grep -c 'settings_language_description' app/src/main/res/values/strings.xml ; grep -c 'ACTION_APP_LOCALE_SETTINGS' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt ; grep -c 'Build.VERSION.SDK_INT' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt ; grep -c 'Icons.Outlined.Language' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt ; grep -c 'LanguageRow' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt ; find app/build/intermediates -name 'locale_config.xml' | wc -l ; echo --- ; grep -c 'android:localeConfig' app/src/main/AndroidManifest.xml</automated>
  </verify>

  <acceptance_criteria>
- `grep -c 'generateLocaleConfig = true' app/build.gradle.kts` returns `1`
- `grep -c '<string name="settings_language">Language</string>' app/src/main/res/values/strings.xml` returns `1`
- `grep -c '<string name="settings_language_description">' app/src/main/res/values/strings.xml` returns `1`
- `grep -c 'ACTION_APP_LOCALE_SETTINGS' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt` returns `1`
- `grep -c 'Build.VERSION.SDK_INT < Build.VERSION_CODES.TIRAMISU' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt` returns `1` (API gate per CONTEXT.md §6)
- `grep -c 'Icons.Outlined.Language' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt` returns `1`
- `grep -c 'private fun LanguageRow' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt` returns `1`
- `grep -c 'LanguageRow()' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt` returns at least `1` (the call site)
- `grep -c 'Uri.fromParts("package"' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt` returns `1` (canonical form per RESEARCH Pitfall E6 / Assumption A5)
- `./gradlew clean assembleDebug` exits 0
- `find app/build/intermediates -name 'locale_config.xml' | wc -l` returns at least `1` (AGP generated the file)
- `grep -c 'android:localeConfig' app/src/main/AndroidManifest.xml` returns `0` (source manifest stays untouched; AGP merger writes the attribute into the final manifest — Pitfall E6)
- `git log -1 --format=%s` returns `feat(comply): COMPLY-06 — per-app language picker (generateLocaleConfig + LanguageRow + ACTION_APP_LOCALE_SETTINGS)`
  </acceptance_criteria>

  <done>
COMPLY-06 grep set passes; AGP generates locale_config.xml; clean assembleDebug green; one atomic commit landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Backlog seeding — add COMPLY-07-3BTN + COMPLY-02-NAV-EVENT to REQUIREMENTS.md Deferred section</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (entire file, ~140 lines) — locate the `## Deferred to Future Milestones` section. Existing structure: `### Phase 1 trim (DEPS-17, auto-logged 2026-05-16)` and `### Bump deferral (auto-logged 2026-05-16)`. New COMPLY entries get their own subsection.
    - .planning/phases/02-comply-platform-compliance/02-CONTEXT.md — `<decisions>` section §9 (Accepted Risks — Risk 1 = 3-button-nav, Risk 2 = NavigationBackHandler); `<deferred>` section (COMPLY-07-3BTN + COMPLY-02-NAV-EVENT formal entries)
    - .planning/phases/01-deps-foundation-bump/01-CONTEXT.md — DEPS-16 deferral pattern under "Deferred Ideas" — follow the same REVIEWS-C4 hygiene format for consistency
  </read_first>

  <action>
**Implement per CONTEXT.md Decision 9 / `<deferred>` section. Single atomic commit modifying only `.planning/REQUIREMENTS.md`.**

### Step 1 — Insert a new subsection in the `## Deferred to Future Milestones` section

After the existing `### Bump deferral (auto-logged 2026-05-16)` subsection (and before any later subsection such as `### Performance (POLISH)`), insert this new subsection EXACTLY:

```markdown
### Phase 2 COMPLY — partial-coverage carry-forward (auto-logged 2026-05-17)

Recorded via plan 02.2 Task 3. Source of truth for the rationale: `.planning/phases/02-comply-platform-compliance/02-CONTEXT.md` `## Accepted Risks` (Risk 1 + Risk 2). Both entries are REVIEWS-C4 ACCEPT — structural deferrals with documented revisit triggers; not silent omissions.

- **COMPLY-07-3BTN** — re-run `DEVICE_TESTING.md` on a 3-button-nav device (or properly-configured emulator with `persist.sys.navigation_mode 0`). Hardware unavailable at Phase 2 execution time; PITFALLS §7 partial verification. The S23+ gesture-nav UAT in `02-UAT.md` covers the gesture-nav side. Revisit trigger: hardware availability OR AGP-9 phase COMPLY re-verification gate (could be folded there since SDK floor moves at the same time). User ACCEPT 2026-05-17 (auto-mode discuss-phase Q2).

- **COMPLY-02-NAV-EVENT** — migrate the `PredictiveBackHandler { progress -> … }` call site in `feature/player/.../PlayerScreen.kt:187` to `NavigationBackHandler` / `NavigationEventState` once Activity Compose ≥ 1.10 is on the floor. `PredictiveBackHandler` is deprecated at Compose Multiplatform 1.10 / AndroidX Activity 1.10+, but `NavigationBackHandler` is NOT available at the Phase 2 floor (Activity Compose 1.9.3). Single call-site migration, low effort. Revisit trigger: AGP-9 phase landing Activity Compose 1.13.0+ (DEPS-17 successor).
```

### Step 2 — Atomic commit

Stage `.planning/REQUIREMENTS.md` ONLY (no code changes) and commit with message:
```
docs(comply): seed COMPLY-07-3BTN + COMPLY-02-NAV-EVENT in REQUIREMENTS Deferred section
```
  </action>

  <verify>
    <automated>cd /home/yun/slopper && grep -c '^### Phase 2 COMPLY — partial-coverage carry-forward' .planning/REQUIREMENTS.md ; grep -c 'COMPLY-07-3BTN' .planning/REQUIREMENTS.md ; grep -c 'COMPLY-02-NAV-EVENT' .planning/REQUIREMENTS.md ; grep -c 'PITFALLS §7' .planning/REQUIREMENTS.md ; grep -c '02-CONTEXT.md' .planning/REQUIREMENTS.md ; git log -1 --format=%s</automated>
  </verify>

  <acceptance_criteria>
- `grep -c '^### Phase 2 COMPLY — partial-coverage carry-forward' .planning/REQUIREMENTS.md` returns `1`
- `grep -c 'COMPLY-07-3BTN' .planning/REQUIREMENTS.md` returns at least `1`
- `grep -c 'COMPLY-02-NAV-EVENT' .planning/REQUIREMENTS.md` returns at least `1`
- `grep -c 'PITFALLS §7' .planning/REQUIREMENTS.md` returns at least `1` (3-button-nav rationale traceback)
- `grep -c 'REVIEWS-C4 ACCEPT' .planning/REQUIREMENTS.md` returns at least `1` (REVIEWS-C4 hygiene format consistent with Phase 1 DEPS-16 precedent)
- `git log -1 --format=%s` returns `docs(comply): seed COMPLY-07-3BTN + COMPLY-02-NAV-EVENT in REQUIREMENTS Deferred section`
- `git diff HEAD~1 HEAD --name-only` lists ONLY `.planning/REQUIREMENTS.md` (no other file modified in this commit)
  </acceptance_criteria>

  <done>
Backlog seeded; one docs commit landed; REQUIREMENTS.md cross-references CONTEXT.md Accepted Risks block.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: COMPLY-07 (capture) — Manual S23+ UAT capture (12 PNG screenshot pack + adb verification)</name>
  <what-built>
After Tasks 1-3 + all Plan 2.1 tasks have landed on `phase-2/comply-platform-compliance`, the executor must produce an installable debug APK and the user must capture 12 PNG screenshots on a Galaxy S23+ Android 16 (gesture-nav) device per CONTEXT.md Decision 8 layout. This checkpoint is unavoidable: no CLI/emulator can produce gesture-nav S23+-specific edge-to-edge verification screenshots; reviewer eyes on real chrome is the only correct mechanism.
  </what-built>
  <how-to-verify>
**Executor preparation (automated portion):**
1. `cd /home/yun/slopper && ./gradlew --configuration-cache assembleDebug` — exits 0
2. Record the commit SHA: `git rev-parse --short HEAD` → save as `BUILD_SHA`
3. Build APK path: `app/build/outputs/apk/debug/app-debug.apk`
4. With S23+ connected via USB (developer options + USB debugging enabled): `adb install -r app/build/outputs/apk/debug/app-debug.apk`
5. Confirm install: `adb shell pm path io.stashapp.android` returns the install path
6. Pre-create the screenshots directory: `mkdir -p .planning/phases/02-comply-platform-compliance/screenshots`

**User capture script (manual portion — exactly 12 screenshots):**

For LIGHT theme (system Settings → Display → Theme → Light):
1. Launch Slopper from launcher (cold start). Observe new splash (COMPLY-04). Verify no white flash.
2. Land on Home. Capture: `home-light.png`.
3. Navigate to Library. Capture: `library-light.png` (browse view, scrolled to show insets at top + bottom).
4. Open Settings. Capture: `settings-light.png` (scrolled so the new Language row is visible).
5. Tap Language row → confirm system per-app language dialog opens (COMPLY-06 evidence). Dismiss; no screenshot needed for the dialog itself.
6. Back to Library → open a scene → enter PlayerScreen. With controls visible, capture: `player-controls-visible-light.png`.
7. Tap once to hide controls; wait for fade. Capture: `player-controls-hidden-light.png` (video full-bleed; no inset bars).
8. Test back swipe: confirm animated predictive-back preview appears (COMPLY-02 evidence). Let go to commit; you should land back on Library.
9. Navigate to Connection screen. Capture: `connection-light.png`.

Toggle to DARK theme (system Settings → Display → Theme → Dark):
10. Capture: `home-dark.png`
11. Capture: `library-dark.png`
12. Capture: `player-controls-visible-dark.png`
13. Capture: `player-controls-hidden-dark.png`
14. Capture: `settings-dark.png`
15. Capture: `connection-dark.png`

= 12 PNGs total.

**Capture method (Claude's Discretion per CONTEXT.md):** `adb shell screencap -p /sdcard/shot.png && adb pull /sdcard/shot.png <name>.png` OR device built-in screenshot (Side + Vol-Down) + USB transfer. No Compose `captureToImage` instrumentation needed.

**Verification before resume:**
- All 12 files exist under `.planning/phases/02-comply-platform-compliance/screenshots/` with exact names: `home-light.png`, `home-dark.png`, `library-light.png`, `library-dark.png`, `player-controls-visible-light.png`, `player-controls-visible-dark.png`, `player-controls-hidden-light.png`, `player-controls-hidden-dark.png`, `settings-light.png`, `settings-dark.png`, `connection-light.png`, `connection-dark.png`
- File sizes >50KB each (sanity check — empty/black PNGs would be smaller)
- Reviewer (user) confirms:
  - [ ] No clipped chrome on any screen (no buttons under nav bar; no overlapping status bar text)
  - [ ] Cold launch shows splash, not white flash
  - [ ] Language row visible in Settings → tapping it opens the system locale dialog
  - [ ] PlayerScreen back swipe shows animated predictive preview (not snap-exit)
  - [ ] Three ModalBottomSheets (Library filter sheet, NavCustomize sheet, BottomNav More sheet) all render with their bottom content clear of the system nav bar
  </how-to-verify>
  <resume-signal>Type "approved" once all 12 PNGs are placed under `.planning/phases/02-comply-platform-compliance/screenshots/` and the 5 reviewer checks pass. If any check fails, describe the failure (which screen, what regression) and the executor will open a follow-up fix commit before re-capturing.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 5: COMPLY-07 (write-up) — SCREENSHOTS.md index + 02-UAT.md DEVICE_TESTING.md row results + atomic commit</name>
  <files>.planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md, .planning/phases/02-comply-platform-compliance/02-UAT.md, .planning/phases/02-comply-platform-compliance/screenshots/home-light.png, .planning/phases/02-comply-platform-compliance/screenshots/home-dark.png, .planning/phases/02-comply-platform-compliance/screenshots/library-light.png, .planning/phases/02-comply-platform-compliance/screenshots/library-dark.png, .planning/phases/02-comply-platform-compliance/screenshots/player-controls-visible-light.png, .planning/phases/02-comply-platform-compliance/screenshots/player-controls-visible-dark.png, .planning/phases/02-comply-platform-compliance/screenshots/player-controls-hidden-light.png, .planning/phases/02-comply-platform-compliance/screenshots/player-controls-hidden-dark.png, .planning/phases/02-comply-platform-compliance/screenshots/settings-light.png, .planning/phases/02-comply-platform-compliance/screenshots/settings-dark.png, .planning/phases/02-comply-platform-compliance/screenshots/connection-light.png, .planning/phases/02-comply-platform-compliance/screenshots/connection-dark.png</files>

  <read_first>
    - .planning/phases/01-deps-foundation-bump/01-UAT.md — Phase 1 UAT writeup; mirror its row format (Result/Notes/Evidence columns) for `02-UAT.md`
    - DEVICE_TESTING.md (repo root) — full checklist that drives the UAT row list. Read every checklist item so each gets a row in 02-UAT.md (PASS for gesture-nav rows the S23+ exercised; DEFERRED → COMPLY-07-3BTN for any row that is explicitly 3-button-nav-only).
    - .planning/phases/02-comply-platform-compliance/02-CONTEXT.md — Decision 8 (UAT + screenshot pack format + S23+ only + DEFERRED row format)
    - .planning/phases/02-comply-platform-compliance/02-SPEC.md — COMPLY-07 acceptance criteria (lines 80-86)
  </read_first>

  <action>
**Implement per CONTEXT.md Decision 8 (flat `<screen>-<theme>.png` layout + SCREENSHOTS.md index + 02-UAT.md row format mirroring 01-UAT.md). Single atomic commit covering the 12 PNGs (already on disk from Task 4) + new SCREENSHOTS.md + new 02-UAT.md.**

### Step 1 — Create `.planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md`

Write this exact content (replacing `<BUILD_SHA>` and `<CAPTURE_DATETIME>` with the values recorded in Task 4):

```markdown
# Phase 2 COMPLY — Screenshot Pack

**Captured:** <CAPTURE_DATETIME>
**Device:** Samsung Galaxy S23+ (SM-S916U1)
**OS:** Android 16
**Navigation:** Gesture nav (3-button-nav DEFERRED — see COMPLY-07-3BTN backlog)
**Build SHA:** <BUILD_SHA>
**Reviewer:** <user — fill in name/handle>
**Verdict:** PASS | PASS-WITH-NOTES | FAIL (delete as appropriate)

## Inventory

| Screen | Light Theme | Dark Theme | Verifies |
|--------|-------------|------------|----------|
| Home | home-light.png | home-dark.png | COMPLY-01 edge-to-edge (insets clean on top bar) |
| Library | library-light.png | library-dark.png | COMPLY-01 edge-to-edge (scroll insets) |
| Player (controls visible) | player-controls-visible-light.png | player-controls-visible-dark.png | COMPLY-01 (overlay safeDrawingPadding wrap), COMPLY-02 (visible chrome before back swipe) |
| Player (controls hidden) | player-controls-hidden-light.png | player-controls-hidden-dark.png | COMPLY-01 (video full-bleed under system bars) |
| Settings | settings-light.png | settings-dark.png | COMPLY-06 (Language row visible at API 33+) |
| Connection | connection-light.png | connection-dark.png | COMPLY-01 (edge-to-edge on auth screen) |

## Capture Notes

- Cold launch tested before each light/dark batch; splash (COMPLY-04) observed dismissing cleanly to first composed route — no white flash.
- PlayerScreen back gesture (COMPLY-02) tested between captures: animated predictive preview confirmed; onExit only fired on committed swipe (cancel returned to player without exit).
- Three ModalBottomSheets (FilterSheet, NavCustomizeSheet, BottomNav.MoreSheet) verified bottom content clear of system navigation area.
- Language row tap (COMPLY-06) opened the system per-app language dialog; dialog showed only "App default" (expected per RESEARCH Pitfall E4 — English-only resources).

## Accepted Limitations (REVIEWS-C4)

- 3-button-nav verification deferred → see `.planning/REQUIREMENTS.md` Deferred section entry `COMPLY-07-3BTN`. Revisit trigger: hardware availability OR AGP-9 phase COMPLY re-verification.
```

### Step 2 — Create `.planning/phases/02-comply-platform-compliance/02-UAT.md`

Write a DEVICE_TESTING.md row-by-row UAT writeup following the 01-UAT.md format exactly. The structure (header + table) must be:

```markdown
# Phase 2 COMPLY — UAT Result

**Run date:** <CAPTURE_DATETIME>
**Build SHA:** <BUILD_SHA>
**Device:** Samsung Galaxy S23+ (SM-S916U1), Android 16, gesture nav
**Tester:** <user>
**Source checklist:** `DEVICE_TESTING.md` (repo root)
**Verdict:** PASS | PASS-WITH-NOTES | FAIL (delete as appropriate)

## Result Rows

| # | Checklist item (from DEVICE_TESTING.md) | Result | Notes | Evidence |
|---|---|---|---|---|
| 1 | <first checklist row from DEVICE_TESTING.md verbatim> | PASS | <observation> | screenshots/home-light.png |
| 2 | <second checklist row verbatim> | PASS | <observation> | screenshots/library-light.png |
| ... | ... | ... | ... | ... |
| N | <any row explicitly 3-button-nav> | DEFERRED — see COMPLY-07-3BTN | Hardware unavailable; .planning/REQUIREMENTS.md Deferred section tracks. | n/a |

## REVIEWS-C4 Sign-Off

- **Accepted risk 1 (PITFALLS §7 — 3-button-nav edge-to-edge unverified):** ACCEPT per `.planning/phases/02-comply-platform-compliance/02-CONTEXT.md` `## Accepted Risks` Risk 1. Backlog: `COMPLY-07-3BTN`. Revisit trigger: hardware OR AGP-9 phase.
- **Accepted risk 2 (PITFALLS §8 — PredictiveBackHandler deprecated at CMP 1.10+):** ACCEPT per `.planning/phases/02-comply-platform-compliance/02-CONTEXT.md` `## Accepted Risks` Risk 2. Backlog: `COMPLY-02-NAV-EVENT`. Revisit trigger: AGP-9 phase / Activity Compose 1.13+.

## Reviewer Sign-Off

- **Reviewer:** <user>
- **Date:** <CAPTURE_DATETIME>
- **Statement:** I have reviewed all 12 screenshots and the per-row UAT results above. No clipped chrome, no unreadable status-bar text, no broken back-gesture or splash behavior observed on Galaxy S23+ Android 16 gesture-nav. 3-button-nav verification is explicitly DEFERRED per the REVIEWS-C4 ACCEPT block above.
```

**Row enumeration rule:** open `DEVICE_TESTING.md` and create ONE table row per checklist item. Mark each row PASS (with a one-sentence "Notes" observation from the actual UAT) or DEFERRED → `COMPLY-07-3BTN` (when the row is 3-button-nav-specific). Do NOT mark any row FAIL — if a regression is observed, STOP and open a fix commit before proceeding to Step 3.

### Step 3 — Atomic commit (the 8th overall — combined screenshot pack + UAT artifacts)

Stage all 14 files (12 PNGs + SCREENSHOTS.md + 02-UAT.md) in ONE commit with message:
```
docs(comply): COMPLY-07 — S23+ gesture-nav UAT + 12-PNG screenshot pack (3-button-nav DEFERRED → COMPLY-07-3BTN)
```

(This commit lands the screenshot+UAT artifacts ONLY. The 7 code/manifest commits from Plan 2.1 + this Plan 2.2 Tasks 1-3 already preceded it. Total Phase 2 commit count: 8 — matches CONTEXT.md Decision 2 budget.)
  </action>

  <verify>
    <automated>cd /home/yun/slopper && ls .planning/phases/02-comply-platform-compliance/screenshots/*.png | wc -l ; for f in home-light home-dark library-light library-dark player-controls-visible-light player-controls-visible-dark player-controls-hidden-light player-controls-hidden-dark settings-light settings-dark connection-light connection-dark; do test -s .planning/phases/02-comply-platform-compliance/screenshots/$f.png && echo "OK $f" || echo "MISSING $f"; done ; echo --- ; test -f .planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md && wc -l .planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md ; test -f .planning/phases/02-comply-platform-compliance/02-UAT.md && wc -l .planning/phases/02-comply-platform-compliance/02-UAT.md ; echo --- ; grep -c 'COMPLY-07-3BTN' .planning/phases/02-comply-platform-compliance/02-UAT.md ; grep -c 'COMPLY-02-NAV-EVENT' .planning/phases/02-comply-platform-compliance/02-UAT.md ; grep -c 'REVIEWS-C4' .planning/phases/02-comply-platform-compliance/02-UAT.md ; grep -c 'Reviewer Sign-Off' .planning/phases/02-comply-platform-compliance/02-UAT.md ; echo --- ; git log -1 --format=%s</automated>
  </verify>

  <acceptance_criteria>
- `ls .planning/phases/02-comply-platform-compliance/screenshots/*.png | wc -l` returns `12`
- Each of the 12 expected filenames (home-light.png, home-dark.png, library-light.png, library-dark.png, player-controls-visible-light.png, player-controls-visible-dark.png, player-controls-hidden-light.png, player-controls-hidden-dark.png, settings-light.png, settings-dark.png, connection-light.png, connection-dark.png) exists AND has non-zero size
- `test -f .planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md` succeeds
- `wc -l .planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md` returns at least `30`
- `grep -c 'SM-S916U1' .planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md` returns at least `1` (device identity present)
- `test -f .planning/phases/02-comply-platform-compliance/02-UAT.md` succeeds
- `grep -c 'COMPLY-07-3BTN' .planning/phases/02-comply-platform-compliance/02-UAT.md` returns at least `2` (DEFERRED rows + REVIEWS-C4 sign-off line)
- `grep -c 'COMPLY-02-NAV-EVENT' .planning/phases/02-comply-platform-compliance/02-UAT.md` returns at least `1` (REVIEWS-C4 sign-off line)
- `grep -c 'REVIEWS-C4' .planning/phases/02-comply-platform-compliance/02-UAT.md` returns at least `1`
- `grep -c '## Reviewer Sign-Off' .planning/phases/02-comply-platform-compliance/02-UAT.md` returns `1`
- `grep -cE '\|.*PASS.*\|' .planning/phases/02-comply-platform-compliance/02-UAT.md` returns at least `5` (sanity — at least 5 PASS rows in the result table)
- `git log -1 --format=%s` returns `docs(comply): COMPLY-07 — S23+ gesture-nav UAT + 12-PNG screenshot pack (3-button-nav DEFERRED → COMPLY-07-3BTN)`
- `git log --format=%s phase-1..HEAD | wc -l` returns `8` (3 from Plan 2.1 + 5 from Plan 2.2 = 8 Phase 2 commits total, matching CONTEXT.md Decision 2 budget)
  </acceptance_criteria>

  <done>
12 PNGs committed; SCREENSHOTS.md + 02-UAT.md written with REVIEWS-C4 ACCEPT block + reviewer sign-off; Phase 2 commit count = 8; Phase 2 UAT artifacts complete and Phase 2 exit gate passed.
  </done>
</task>

</tasks>

<verification>
After all 5 tasks complete, the following invariants MUST hold for Plan 2.2:

```bash
# COMPLY-03 + COMPLY-05 (orphan permission removal)
grep -c 'android.permission.POST_NOTIFICATIONS' app/src/main/AndroidManifest.xml            # → 0
grep -c 'FOREGROUND_SERVICE_MEDIA_PLAYBACK' app/src/main/AndroidManifest.xml                # → 0
grep -c '"android.permission.FOREGROUND_SERVICE"' app/src/main/AndroidManifest.xml          # → 1

# COMPLY-06 (per-app language)
grep -c 'generateLocaleConfig = true' app/build.gradle.kts                                   # → 1
grep -c 'ACTION_APP_LOCALE_SETTINGS' feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt # → 1
grep -c 'settings_language' app/src/main/res/values/strings.xml                              # → 2 (two new strings)
find app/build/intermediates -name 'locale_config.xml' | wc -l                               # → ≥ 1
grep -c 'android:localeConfig' app/src/main/AndroidManifest.xml                              # → 0 (source manifest untouched)

# Backlog seeding
grep -c 'COMPLY-07-3BTN' .planning/REQUIREMENTS.md                                           # → ≥ 1
grep -c 'COMPLY-02-NAV-EVENT' .planning/REQUIREMENTS.md                                      # → ≥ 1

# COMPLY-07 (UAT + screenshots)
ls .planning/phases/02-comply-platform-compliance/screenshots/*.png | wc -l                  # → 12
test -f .planning/phases/02-comply-platform-compliance/screenshots/SCREENSHOTS.md            # → exit 0
test -f .planning/phases/02-comply-platform-compliance/02-UAT.md                             # → exit 0
grep -c 'COMPLY-07-3BTN' .planning/phases/02-comply-platform-compliance/02-UAT.md            # → ≥ 2

# Build
./gradlew --configuration-cache assembleDebug                                                # exit 0
./gradlew --configuration-cache assembleRelease                                              # exit 0

# Commits — Plan 2.2 added 5 commits (COMPLY-05, COMPLY-03, COMPLY-06, backlog, UAT)
git log --format=%s phase-1..HEAD | grep -cE '^(feat|chore|docs)\(comply\):'                 # → 8
```

Reachability check (goal-backward):
- Smaller attack surface → 2 manifest lines deleted; base FGS preserved for future BG-MEDIA use.
- Language row visible at API 33+ → `Build.VERSION.SDK_INT < TIRAMISU` early return + Material3 ListItem + `Icons.Outlined.Language` + `ACTION_APP_LOCALE_SETTINGS` intent fire.
- AGP locale_config.xml generated → `androidResources { generateLocaleConfig = true }` block in `android { }` scope (NOT `defaultConfig { }`).
- Backlog hygiene → REQUIREMENTS.md Deferred subsection cross-references CONTEXT.md `## Accepted Risks` and follows the DEPS-16 / REVIEWS-C4 precedent.
- Phase 2 exit gate → 8 commits total + 12 PNGs + 02-UAT.md with PASS or DEFERRED rows + reviewer sign-off.
</verification>

<success_criteria>
Plan 2.2 is complete when:
- 5 atomic commits landed on `phase-2/comply-platform-compliance`: `chore(comply): COMPLY-05`, `chore(comply): COMPLY-03`, `feat(comply): COMPLY-06`, `docs(comply): seed COMPLY-07-3BTN + COMPLY-02-NAV-EVENT`, `docs(comply): COMPLY-07 — S23+ gesture-nav UAT + 12-PNG screenshot pack`
- Combined with Plan 2.1's 3 commits, Phase 2 has exactly 8 commits (matching CONTEXT.md Decision 2 budget)
- `./gradlew --configuration-cache assembleDebug assembleRelease` green
- All grep invariants in the verification block above hold
- 12 PNG screenshots committed under `screenshots/` with locked `<screen>-<theme>.png` naming
- `02-UAT.md` exists with full DEVICE_TESTING.md row coverage (PASS for gesture-nav; DEFERRED for 3-button-nav)
- `.planning/REQUIREMENTS.md` Deferred section gains `COMPLY-07-3BTN` + `COMPLY-02-NAV-EVENT` entries
- No Spine palette/typography tokens introduced (anti-coupling rule from CONTEXT.md `<spine_forward_compatibility>`)
- Each task's atomic acceptance criteria passed (verified per-task)
</success_criteria>

<output>
After completion, create `.planning/phases/02-comply-platform-compliance/02-02-SUMMARY.md` documenting:
- The 5 commits' SHAs (COMPLY-05, COMPLY-03, COMPLY-06, backlog, UAT)
- Cumulative Phase 2 commit count (8 = 3 from Plan 2.1 + 5 from Plan 2.2)
- Verdict from `02-UAT.md` (PASS / PASS-WITH-NOTES / FAIL)
- Whether `resources.properties` was created (per RESEARCH Open Question §3)
- Whether the splash icon needed the `@drawable/splash_icon` fallback (per RESEARCH Pitfall E5 / Claude's Discretion)
- Any UAT row that was downgraded from PASS to PASS-WITH-NOTES with a justification
- Pointer to `screenshots/SCREENSHOTS.md` for the reviewer sign-off line + capture metadata
- Note that Phase 2 exit is now reachable: rebase `phase-2/comply-platform-compliance` onto `master` once Phase 1 PR #5 merges, then open the Phase 2 PR per CONTEXT.md Claude's Discretion "PR description structure" guidance
</output>
