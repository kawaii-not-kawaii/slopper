---
phase: 08-agp-9-atomic-build-logic-migration-compilesdk-36
plan: 08.1
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/wrapper/gradle-wrapper.properties
  - gradle/libs.versions.toml
  - build.gradle.kts
  - build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/KotlinAndroid.kt
  - build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/AndroidApplicationConventionPlugin.kt
  - build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/AndroidLibraryConventionPlugin.kt
  - baselineprofile/build.gradle.kts
  - .github/workflows/ci.yml
autonomous: false
requirements: [AGP9-02, AGP9-03, SDK-01]

# NOTE (folded-forward AGP9-01): this plan also discharges the "green-on-Gradle-9"
# clause of AGP9-01 carried forward from Phase 7 — commit 1 flips the wrapper to
# 9.4.1 and the final gate (task 9) asserts green on Gradle 9 + AGP 9. See must_haves.truths[0].

must_haves:
  truths:
    # Derived directly from the 5 ROADMAP Phase-8 success criteria.
    - "AGP resolves to 9.2.1 and `./gradlew compileDebugSources` is green across all ~14 modules with the version-isolation opt-out flags (builtInKotlin/newDsl/enableLegacyVariantApi) ABSENT, on Gradle 9.4.1 (folded-forward AGP9-01)."
    - "`org.jetbrains.kotlin.android` is removed from every site (both convention plugins, root build.gradle.kts, baselineprofile, catalog alias); `CommonExtension` has no generics; `kotlinOptions{}` is migrated to `kotlin{compilerOptions{}}` — no 'extension already registered' and no generics compile error."
    - "Hilt/Dagger resolves to exactly 2.59.2 (never bare 2.59+), and a Hilt+KSP DI graph compiles on Kotlin 2.2.20 / KSP 2.2.20-2.0.4."
    - "compileSdk is 36 in both touchpoints (KotlinAndroid.kt + baselineprofile) AND targetSdk = 35 stays verifiably explicit in all 3 sites — no silent Android-16 runtime opt-in."
    - "The full phase gate passes: `compileDebugSources detekt ktlintCheck test` green, and the CI cache key is bumped agp8→agp9 (zero `agp8` hits in ci.yml)."
  artifacts:
    - path: "gradle/wrapper/gradle-wrapper.properties"
      provides: "Gradle 9.4.1 distribution pin + live-fetched distributionSha256Sum; validateDistributionUrl=true retained"
      contains: "gradle-9.4.1-bin.zip"
    - path: "gradle/libs.versions.toml"
      provides: "agp=9.2.1, hilt=2.59.2; kotlin-android plugin alias REMOVED"
      contains: "agp = \"9.2.1\""
    - path: "build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/KotlinAndroid.kt"
      provides: "Choke-point configureKotlinAndroid(commonExtension: CommonExtension) with compileSdk=36"
      contains: "commonExtension: CommonExtension,"
    - path: "baselineprofile/build.gradle.kts"
      provides: "kotlin{compilerOptions{jvmTarget.set(JvmTarget.JVM_17)}}, compileSdk=36, targetSdk=35; no kotlin.android alias"
      contains: "compilerOptions"
    - path: ".github/workflows/ci.yml"
      provides: "Gradle cache key token bumped agp8→agp9 (both key + restore-keys)"
      contains: "agp9"
  key_links:
    - from: "AndroidApplicationConventionPlugin / AndroidLibraryConventionPlugin"
      to: "configureKotlinAndroid(this) in KotlinAndroid.kt"
      via: "extensions.configure<Application|LibraryExtension>{ configureKotlinAndroid(this) }"
      pattern: "configureKotlinAndroid\\(this\\)"
    - from: "configureKotlinAndroid(commonExtension: CommonExtension)"
      to: "all ~14 modules (compileSdk=36, compilerOptions via KotlinAndroidProjectExtension)"
      via: "shared choke point — one edit configures every module"
      pattern: "commonExtension: CommonExtension,"
    - from: "AndroidHiltConventionPlugin + catalog hilt ref"
      to: "hilt-android + hilt-compiler 2.59.2"
      via: "version.ref = hilt in libs.versions.toml"
      pattern: "hilt = \"2.59.2\""
---

<objective>
Land the ATOMIC AGP-9 critical-path change-set so the `build-logic/convention` choke point
(`configureKotlinAndroid`) configures all ~14 modules on AGP 9.2.1 / Gradle 9.4.1, and the build
is green again. The change-set, in strict bisectable order (D-01): Gradle 9.4.1 wrapper →
AGP 9.2.1 → drop `org.jetbrains.kotlin.android` (5 sites) → `CommonExtension` generics fix →
`kotlinOptions{}`→`kotlin{compilerOptions{}}` (baselineprofile only) → Hilt 2.59.2 → compileSdk 36,
with `targetSdk = 35` held explicit everywhere and the version-isolation opt-out flags absent.

Purpose: This is THE one indivisible phase of v1.1. The build is only green at the END of the
sequence; every intermediate commit is non-green-but-bisectable so `git bisect` can isolate which
concern broke configuration.

Output: 7 atomic commits + grep-verifiable guards + a full green phase gate
(`compileDebugSources detekt ktlintCheck test`), discharging AGP9-02, AGP9-03, SDK-01, and the
folded-forward green-on-Gradle-9 clause of AGP9-01.

CONSTRAINTS (locked, NON-NEGOTIABLE):
- ONE concern per commit, in the exact D-01 order. Do NOT merge commits. The build going non-green
  in isolation for commits 1–3 is EXPECTED and correct.
- Do NOT add `android.enableLegacyVariantApi`, `android.newDsl=false`, or `android.builtInKotlin=false`
  (D-09 — forbidden crutches). The gate is only "done" with these ABSENT.
- Do NOT bump Kotlin (2.2.20) or KSP (2.2.20-2.0.4) — they already satisfy AGP-9's KGP 2.2.10 floor (D-04b).
- Do NOT bump `targetSdk` (stays 35 at all 3 sites). Do NOT bump detekt to 2.0.0-alpha (D-08).
- Do NOT touch the deferred items: `settings.gradle.kts` duplicate `include`, Media3/nextlib, lint re-enables (DEPS-07).
- NEVER hardcode the Gradle sha256 from memory — fetch it live at execution (threat T-08-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agp-9-atomic-build-logic-migration-compilesdk-36/08-CONTEXT.md
@.planning/phases/08-agp-9-atomic-build-logic-migration-compilesdk-36/08-RESEARCH.md
@docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md

<interfaces>
<!-- The exact current state of every migration site, extracted live. The executor works from this
     block + the plan text alone. Line numbers are as-of-planning; re-verify with grep before editing. -->

gradle/wrapper/gradle-wrapper.properties (current — lines 3,4):
```
distributionUrl=https\://services.gradle.org/distributions/gradle-8.11.1-bin.zip
distributionSha256Sum=f397b287023acdba1e9f6fc5ea72d22dd63669d59ed4a289a29b1a76eee151c6
```
(validateDistributionUrl=true at L6, networkTimeout=10000 at L5 — KEEP both unchanged.)

gradle/libs.versions.toml (current):
```
L3:   agp = "8.7.3"
L4:   kotlin = "2.2.20"        # UNCHANGED
L5:   ksp = "2.2.20-2.0.4"     # UNCHANGED
L6:   hilt = "2.56.2"
L150: kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }   # REMOVE entire line
L151: kotlin-compose = { ... }          # KEEP
L152: kotlin-serialization = { ... }    # KEEP
```

build.gradle.kts (current — root):
```
L5: alias(libs.plugins.kotlin.android) apply false     # REMOVE entire line
L6: alias(libs.plugins.kotlin.compose) apply false     # KEEP
L7: alias(libs.plugins.kotlin.serialization) apply false  # KEEP
```
NOTE: detekt toolVersion = "1.23.8" (L53) and ktlint version.set("1.6.0") (L41) live in the
root subprojects{} block — do NOT change them this phase (D-08, Pitfall 5).

KotlinAndroid.kt (current):
```
L18: commonExtension: CommonExtension<*, *, *, *, *, *>,   # → bare CommonExtension (commit 4)
L21: compileSdk = 35                                        # → 36 (commit 7)
L47-56: extensions.configure<KotlinAndroidProjectExtension> { jvmToolchain(17); compilerOptions { ... } }
        # ← UNCHANGED. Already on compilerOptions. KotlinAndroidProjectExtension STILL resolves under
        #   AGP-9 built-in Kotlin (RESEARCH Pattern 2, NiA-verified). Imports unchanged.
```

AndroidApplicationConventionPlugin.kt (current):
```
L13: apply("org.jetbrains.kotlin.android")   # REMOVE entire line (commit 3)
L17: defaultConfig.targetSdk = 35            # KEEP explicit (D-05)
```

AndroidLibraryConventionPlugin.kt (current):
```
L14: apply("org.jetbrains.kotlin.android")   # REMOVE entire line (commit 3)
L18: defaultConfig.targetSdk = 35            # KEEP explicit (D-05)
```

baselineprofile/build.gradle.kts (current):
```
L3:  alias(libs.plugins.kotlin.android)      # REMOVE entire line (commit 3)
L9:  compileSdk = 35                          # → 36 (commit 7)
L15-17: kotlinOptions { jvmTarget = "17" }    # → kotlin{compilerOptions{...}} (commit 5)
L21: targetSdk = 35                           # KEEP explicit (D-05)
```

.github/workflows/ci.yml (current — agp8 token at L39 + L41):
```
L39: key: gradle-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties') }}-jdk17-agp8-${{ hashFiles('gradle/libs.versions.toml') }}
L41:   gradle-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties') }}-jdk17-agp8-
```
</interfaces>
</context>

<tasks>

<!-- ════════════════ COMMIT 1 ════════════════ -->
<task type="auto">
  <name>Task 1 (commit 1): Flip Gradle wrapper 8.11.1 → 9.4.1 with a LIVE-fetched sha256</name>
  <read_first>
    - gradle/wrapper/gradle-wrapper.properties (the file being edited)
    - docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md §"distributionSha256Sum fetched at Phase-8 execution"
    - 08-RESEARCH.md §"Sha256 mandate (commit 1)" + §"Pattern 0"
    - 08-CONTEXT.md D-01 step 1
  </read_first>
  <action>
    Flip the Gradle wrapper to 9.4.1, re-pinning the distribution hash fetched LIVE (never from memory).
    Preferred mechanism (atomic + validates):
      `./gradlew wrapper --gradle-version 9.4.1 --gradle-distribution-sha256-sum "$(curl -sSL https://services.gradle.org/distributions/gradle-9.4.1-bin.zip.sha256)"`
    If the wrapper task cannot run under AGP 8.7.3, edit gradle-wrapper.properties directly:
      - L3 distributionUrl → `https\://services.gradle.org/distributions/gradle-9.4.1-bin.zip`
      - L4 distributionSha256Sum → the value from `curl -sSL https://services.gradle.org/distributions/gradle-9.4.1-bin.zip.sha256`
        (the .sha256 URL 301-redirects via Cloudflare; `-sSL` follows it).
    KEEP validateDistributionUrl=true (L6) and networkTimeout=10000 (L5) unchanged.
    EXPECTED: this commit is NON-GREEN in isolation — AGP 8.7.3 hard-fails the Gradle-9 version check.
    That is correct and intended (bisect attributes this to the Gradle-version check). Do NOT try to make it green.
    Commit message: `chore(gradle): flip wrapper 8.11.1 → 9.4.1 (live sha256, validateDistributionUrl kept)`
  </action>
  <acceptance_criteria>
    - `grep -c 'gradle-9.4.1-bin.zip' gradle/wrapper/gradle-wrapper.properties` == 1
    - `grep -c 'gradle-8.11.1-bin.zip' gradle/wrapper/gradle-wrapper.properties` == 0
    - `grep -c 'f397b287023acdba1e9f6fc5ea72d22dd63669d59ed4a289a29b1a76eee151c6' gradle/wrapper/gradle-wrapper.properties` == 0  (old 8.11.1 hash gone)
    - `grep -c 'validateDistributionUrl=true' gradle/wrapper/gradle-wrapper.properties` == 1
    - distributionSha256Sum is a fresh 64-hex value fetched at execution (NOT the f397b287… string above).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'gradle-9.4.1-bin.zip' gradle/wrapper/gradle-wrapper.properties && ! grep -q 'f397b287023acdba1e9f6fc5ea72d22dd63669d59ed4a289a29b1a76eee151c6' gradle/wrapper/gradle-wrapper.properties && grep -q 'validateDistributionUrl=true' gradle/wrapper/gradle-wrapper.properties && echo PASS</automated>
  </verify>
  <done>Wrapper points at gradle-9.4.1-bin.zip with a freshly-fetched sha256; validateDistributionUrl=true retained; committed alone.</done>
</task>

<!-- ════════════════ COMMIT 2 ════════════════ -->
<task type="auto">
  <name>Task 2 (commit 2): Bump AGP 8.7.3 → 9.2.1 in the version catalog</name>
  <read_first>
    - gradle/libs.versions.toml (the file being edited — L3)
    - 08-RESEARCH.md §"Standard Stack" (AGP 9.2.1 live-verified) + §"Sequencing Landmines" commit 2 row
    - 08-CONTEXT.md D-03
  </read_first>
  <action>
    Re-confirm 9.2.1 is the newest stable AGP 9.2.x at execution time via Google Maven metadata:
      `curl -sSL https://dl.google.com/dl/android/maven2/com/android/tools/build/gradle/maven-metadata.xml | grep -oE '9\.2\.[0-9]+' | sort -u`
    If 9.2.1 is present → use `9.2.1`. Fall back to `9.2.0` ONLY if 9.2.1 is unpublished (D-03). Never use 9.3.0-alpha.
    Edit gradle/libs.versions.toml L3: `agp = "8.7.3"` → `agp = "9.2.1"` (this drives android-application,
    android-library, android-test plugins AND the plugin-android classpath — single edit).
    EXPECTED: NON-GREEN in isolation — AGP 9 enables built-in Kotlin, which collides with the still-applied
    `org.jetbrains.kotlin.android` ("extension 'kotlin' already registered"). That diagnostic failure is
    attributable to commit 2 and is fixed by commit 3. Do NOT remove kotlin.android here (keep commits separate).
    Commit message: `build(agp): bump AGP 8.7.3 → 9.2.1`
  </action>
  <acceptance_criteria>
    - `grep -c 'agp = "9.2.1"' gradle/libs.versions.toml` == 1   (or "9.2.0" iff 9.2.1 unpublished — document the fallback)
    - `grep -c 'agp = "8.7.3"' gradle/libs.versions.toml` == 0
    - kotlin/ksp lines unchanged: `grep -c 'kotlin = "2.2.20"' gradle/libs.versions.toml` == 1 AND `grep -c 'ksp = "2.2.20-2.0.4"' gradle/libs.versions.toml` == 1
  </acceptance_criteria>
  <verify>
    <automated>grep -Eq 'agp = "9\.2\.[01]"' gradle/libs.versions.toml && ! grep -q 'agp = "8.7.3"' gradle/libs.versions.toml && grep -q 'kotlin = "2.2.20"' gradle/libs.versions.toml && echo PASS</automated>
  </verify>
  <done>Catalog pins agp=9.2.1 (or documented 9.2.0 fallback); Kotlin/KSP untouched; committed alone.</done>
</task>

<!-- ════════════════ COMMIT 3 ════════════════ -->
<task type="auto">
  <name>Task 3 (commit 3): Drop org.jetbrains.kotlin.android from all 5 sites (built-in Kotlin)</name>
  <read_first>
    - gradle/libs.versions.toml (L150), build.gradle.kts (L5), baselineprofile/build.gradle.kts (L3),
      AndroidApplicationConventionPlugin.kt (L13), AndroidLibraryConventionPlugin.kt (L14) — the 5 sites
    - 08-RESEARCH.md §"Pattern 3" (the 5-site table) + §"Pattern 2" (KotlinAndroidProjectExtension survives)
    - 08-CONTEXT.md D-06 + D-06b
  </read_first>
  <action>
    Remove `org.jetbrains.kotlin.android` from ALL 5 sites (AGP 9 registers Kotlin itself; any leftover
    apply/alias → "extension already registered"):
      1. build.gradle.kts:5     — delete line `alias(libs.plugins.kotlin.android) apply false`
      2. AndroidApplicationConventionPlugin.kt:13 — delete line `apply("org.jetbrains.kotlin.android")`
      3. AndroidLibraryConventionPlugin.kt:14     — delete line `apply("org.jetbrains.kotlin.android")`
      4. baselineprofile/build.gradle.kts:3       — delete line `alias(libs.plugins.kotlin.android)`
      5. libs.versions.toml:150 — delete entry `kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }`
    KEEP `kotlin-compose` and `kotlin-serialization` aliases + applications (built-in Kotlin replaces
    kotlin-android ONLY — D-06b). Do NOT touch the `KotlinAndroidProjectExtension` block in KotlinAndroid.kt
    (it still resolves under built-in Kotlin — verified next task at the smoke).
    EXPECTED: still NON-GREEN — build-logic fails to compile because CommonExtension generics (L18) are still
    `<*,…>` under AGP 9. That is commit 4's job. Keep separate.
    Commit message: `build(kotlin): drop org.jetbrains.kotlin.android (AGP-9 built-in Kotlin), 5 sites`
  </action>
  <acceptance_criteria>
    NOTE: the 5 sites use TWO spellings — 3 are the literal id `org.jetbrains.kotlin.android`
    (both convention plugins + the catalog `kotlin-android = { id = ... }` entry), and 2 are the
    alias `libs.plugins.kotlin.android` (root build.gradle.kts:5 + baselineprofile:3). A single grep
    on the literal id MISSES the 2 alias lines, so assert BOTH:
    - Literal id gone (3 sites): `grep -rn "org.jetbrains.kotlin.android" build.gradle.kts baselineprofile/build.gradle.kts build-logic gradle/libs.versions.toml | wc -l` == 0
    - Alias gone (2 sites): `grep -rn "libs.plugins.kotlin.android" build.gradle.kts baselineprofile/build.gradle.kts build-logic | wc -l` == 0
    - Catch-all (all 5, comments stripped so prose can't trip it): `grep -rn "kotlin[.-]android" build.gradle.kts baselineprofile/build.gradle.kts build-logic gradle/libs.versions.toml | grep -v '^[^:]*:[0-9]*: *//' | grep -v '#' | wc -l` == 0
    - `grep -c 'libs.plugins.kotlin.compose' build.gradle.kts` >= 1  (compose KEPT)
    - `grep -c 'kotlin-compose' gradle/libs.versions.toml` == 1 AND `grep -c 'kotlin-serialization' gradle/libs.versions.toml` == 1  (both KEPT)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -rn 'org.jetbrains.kotlin.android' build.gradle.kts baselineprofile/build.gradle.kts build-logic gradle/libs.versions.toml | wc -l)" -eq 0 && test "$(grep -rn 'libs.plugins.kotlin.android' build.gradle.kts baselineprofile/build.gradle.kts build-logic | wc -l)" -eq 0 && grep -q 'kotlin-compose' gradle/libs.versions.toml && grep -q 'kotlin-serialization' gradle/libs.versions.toml && echo PASS</automated>
  </verify>
  <done>Zero `org.jetbrains.kotlin.android` references repo-wide; compose + serialization plugins retained; committed alone.</done>
</task>

<!-- ════════════════ COMMIT 4 ════════════════ -->
<task type="auto">
  <name>Task 4 (commit 4): Fix CommonExtension generics — drop the 6 star-projections</name>
  <read_first>
    - build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/KotlinAndroid.kt (L17-18, the edit site)
    - build-logic/convention/.../AndroidComposeConventionPlugin.kt (re-verify getByType(CommonExtension::class.java) — class literal, unaffected)
    - 08-RESEARCH.md §"Pattern 1" + §"Code Examples" (the complete migrated choke point)
    - 08-CONTEXT.md D-01 step 4
  </read_first>
  <action>
    AGP 9.0 removed ALL type parameters from `com.android.build.api.dsl.CommonExtension` (new arity = zero).
    In KotlinAndroid.kt, change the `configureKotlinAndroid` parameter (L17-18) from:
      ```kotlin
      internal fun Project.configureKotlinAndroid(
          commonExtension: CommonExtension<*, *, *, *, *, *>,
      ) {
      ```
    to bare:
      ```kotlin
      internal fun Project.configureKotlinAndroid(
          commonExtension: CommonExtension,
      ) {
      ```
    ONE token change. The `import com.android.build.api.dsl.CommonExtension` stays unchanged.
    Do NOT touch the `extensions.configure<KotlinAndroidProjectExtension> { ... }` block (L47-56) — it is
    correct as-is under built-in Kotlin (RESEARCH Pattern 2). Do NOT touch compileSdk yet (commit 7).
    `configureKotlinAndroid(this)` stays callable from both ApplicationExtension and LibraryExtension
    (both still extend the now-non-generic CommonExtension). AndroidComposeConventionPlugin's
    `getByType(CommonExtension::class.java)` is a class literal — unaffected; confirm at the smoke.
    Commit message: `build(agp): drop CommonExtension generics (AGP-9 removed type params)`
  </action>
  <acceptance_criteria>
    - `grep -c 'commonExtension: CommonExtension,' build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/KotlinAndroid.kt` == 1
    - `grep -c 'CommonExtension<' build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/KotlinAndroid.kt` == 0  (no star-projections remain)
    - `grep -c 'import com.android.build.api.dsl.CommonExtension' build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/KotlinAndroid.kt` == 1  (import unchanged)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'commonExtension: CommonExtension,' build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/KotlinAndroid.kt && ! grep -q 'CommonExtension<' build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/KotlinAndroid.kt && echo PASS</automated>
  </verify>
  <done>CommonExtension is bare (no generics); import unchanged; KotlinAndroidProjectExtension block untouched; committed alone.</done>
</task>

<!-- ════════════════ CHOKE-POINT SMOKE (D-02) ════════════════ -->
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 (checkpoint, D-02): First green — choke-point smoke after the generics commit</name>
  <what-built>
    Commits 1–4 are in place: Gradle 9.4.1, AGP 9.2.1, built-in Kotlin (kotlin.android dropped), and the
    bare-CommonExtension choke point. Per D-02/Pitfall 4, before attempting a full multi-module build we
    validate the convention-plugin choke point on a single module — `:core:common` — which is the fastest
    reproduction of any generics / built-in-Kotlin / extension-registration break. This is the FIRST point
    the build should go green.
  </what-built>
  <how-to-verify>
    Run the choke-point smoke (stop any stale 8.11.1 daemon first if it clings to the old wrapper):
      1. `./gradlew --stop`   (clears a daemon pinned to Gradle 8.11.1)
      2. `./gradlew :core:common:compileDebugKotlin --stacktrace`
    EXPECTED: BUILD SUCCESSFUL. The Gradle banner should report `Gradle 9.4.1`.
    If it FAILS with `io.stashapp.android.buildlogic.*` in the stack frame:
      - "extension 'kotlin' already registered" → a kotlin.android site was missed (re-check task 3's grep == 0).
      - CommonExtension generics / arity error → re-check task 4 (must be bare `CommonExtension`).
      - KotlinAndroidProjectExtension unresolved → report it (RESEARCH expects it to resolve; this would be a finding).
    If the smoke fails for a reason NOT in the list above, capture the full `--stacktrace` and report before proceeding.
  </how-to-verify>
  <resume-signal>Type "approved" once `:core:common:compileDebugKotlin` is BUILD SUCCESSFUL on Gradle 9.4.1, or paste the failing stacktrace.</resume-signal>
</task>

<!-- ════════════════ COMMIT 5 ════════════════ -->
<task type="auto">
  <name>Task 6 (commit 5): Migrate baselineprofile kotlinOptions{} → kotlin{compilerOptions{}}</name>
  <read_first>
    - baselineprofile/build.gradle.kts (L15-17 kotlinOptions block, inside android{}; L1-5 plugins block)
    - 08-RESEARCH.md §"Pattern 4" (the exact before/after) + §"Code Examples"
    - 08-CONTEXT.md D-01 step 5
  </read_first>
  <action>
    This is the ONLY remaining `kotlinOptions{}` site (the choke point already uses compilerOptions).
    In baselineprofile/build.gradle.kts, REMOVE the `kotlinOptions { jvmTarget = "17" }` block (currently
    L15-17, inside the `android { }` block) and ADD a top-level `kotlin { }` block (a sibling of `android { }`,
    NOT nested inside it):
      ```kotlin
      kotlin {
          compilerOptions {
              jvmTarget.set(org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_17)
          }
      }
      ```
    Either fully-qualify `org.jetbrains.kotlin.gradle.dsl.JvmTarget` as above, OR add
    `import org.jetbrains.kotlin.gradle.dsl.JvmTarget` at the top and use `JvmTarget.JVM_17`. The `android.test`
    plugin gets built-in Kotlin from AGP 9, so the `kotlin {}` extension is available here.
    Do NOT touch compileSdk (commit 7) or targetSdk (stays 35) in this commit.
    Commit message: `build(baselineprofile): migrate kotlinOptions → kotlin{compilerOptions}`
  </action>
  <acceptance_criteria>
    - `grep -c 'kotlinOptions' baselineprofile/build.gradle.kts` == 0  (old DSL gone)
    - `grep -c 'compilerOptions' baselineprofile/build.gradle.kts` == 1
    - `grep -c 'JvmTarget.JVM_17' baselineprofile/build.gradle.kts` == 1
    - the new `kotlin {` block is top-level (a sibling of `android {`), not nested inside `android {`.
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'kotlinOptions' baselineprofile/build.gradle.kts && grep -q 'compilerOptions' baselineprofile/build.gradle.kts && grep -q 'JvmTarget.JVM_17' baselineprofile/build.gradle.kts && echo PASS</automated>
  </verify>
  <done>baselineprofile uses top-level kotlin{compilerOptions{jvmTarget.set(JvmTarget.JVM_17)}}; no kotlinOptions remain; committed alone.</done>
</task>

<!-- ════════════════ COMMIT 6 ════════════════ -->
<task type="auto">
  <name>Task 7 (commit 6): Bump Hilt/Dagger 2.56.2 → 2.59.2 (after AGP — pinned exactly)</name>
  <read_first>
    - gradle/libs.versions.toml (L6 hilt; L93-94 hilt-android/hilt-compiler refs; L154 hilt plugin alias)
    - build-logic/.../AndroidHiltConventionPlugin.kt (applies ksp + hilt; pulls hilt-android + hilt-compiler)
    - 08-RESEARCH.md §"Pitfall 3" (2.59 drops AGP 8; 2.59.0/2.59.1 broken; pin 2.59.2) + §"Standard Stack"
    - 08-CONTEXT.md D-04 (never bare 2.59+)
  </read_first>
  <action>
    Re-confirm 2.59.2 is current/fixed at execution via Maven Central:
      `curl -sSL https://repo1.maven.org/maven2/com/google/dagger/dagger/maven-metadata.xml | grep -oE '2\.59(\.[0-9]+)?' | sort -u`
    Edit gradle/libs.versions.toml L6: `hilt = "2.56.2"` → `hilt = "2.59.2"` EXACTLY (never `2.59`, `2.59.1`, or a
    floating `2.59+` — a range re-introduces resolution nondeterminism, D-04). This single ref drives hilt-android,
    hilt-compiler (L93-94), and the hilt plugin (L154). 2.59 requires AGP 9 + Gradle 9.1, which is why this lands
    strictly AFTER commit 2.
    Commit message: `build(hilt): bump Hilt/Dagger 2.56.2 → 2.59.2 (AGP-9 required)`
  </action>
  <acceptance_criteria>
    - `grep -c 'hilt = "2.59.2"' gradle/libs.versions.toml` == 1
    - `grep -c 'hilt = "2.56.2"' gradle/libs.versions.toml` == 0
    - No floating range: `grep -Ec 'hilt = "2\.59"|hilt = "2\.59\+"' gradle/libs.versions.toml` == 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'hilt = "2.59.2"' gradle/libs.versions.toml && ! grep -q 'hilt = "2.56.2"' gradle/libs.versions.toml && ! grep -Eq 'hilt = "2\.59(\+)?"' gradle/libs.versions.toml && echo PASS</automated>
  </verify>
  <done>Catalog pins hilt=2.59.2 exactly (no range); committed alone.</done>
</task>

<!-- ════════════════ COMMIT 7 ════════════════ -->
<task type="auto">
  <name>Task 8 (commit 7): Raise compileSdk 35 → 36 in both sites (targetSdk HELD at 35)</name>
  <read_first>
    - build-logic/convention/.../KotlinAndroid.kt (L21 compileSdk)
    - baselineprofile/build.gradle.kts (L9 compileSdk, L21 targetSdk)
    - 08-RESEARCH.md §"Pattern 5" + §"Pitfall 1" (silent targetSdk flip)
    - 08-CONTEXT.md D-05a (targetSdk stays explicit) + D-01 step 7
  </read_first>
  <action>
    Raise compileSdk to 36 in EXACTLY the two compileSdk sites:
      1. KotlinAndroid.kt:21         `compileSdk = 35` → `compileSdk = 36`
      2. baselineprofile/build.gradle.kts:9   `compileSdk = 35` → `compileSdk = 36`
    DO NOT touch any `targetSdk` line — all 3 (AndroidApplicationConventionPlugin.kt:17,
    AndroidLibraryConventionPlugin.kt:18, baselineprofile/build.gradle.kts:21) MUST stay `= 35`.
    AGP 9's `defaultTargetSdkToCompileSdkIfUnset` is true; an UNSET targetSdk would silently inherit 36
    (a forbidden Android-16 opt-in). Since all 3 sites set it explicitly today, the job is to LEAVE them at 35.
    Commit message: `build(sdk): raise compileSdk 35 → 36 (targetSdk held at 35)`
  </action>
  <acceptance_criteria>
    - `grep -rn 'compileSdk = 36' build-logic baselineprofile/build.gradle.kts | wc -l` == 2  (exactly the 2 sites)
    - `grep -rn 'compileSdk = 35' build-logic baselineprofile/build.gradle.kts | wc -l` == 0
    - `grep -rn 'targetSdk = 35' build-logic baselineprofile/build.gradle.kts | wc -l` == 3  (all held)
    - `grep -rn 'targetSdk = 36' build-logic baselineprofile/build.gradle.kts | wc -l` == 0  (no silent flip)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -rn 'compileSdk = 36' build-logic baselineprofile/build.gradle.kts | wc -l)" -eq 2 && test "$(grep -rn 'targetSdk = 35' build-logic baselineprofile/build.gradle.kts | wc -l)" -eq 3 && test "$(grep -rn 'targetSdk = 36' build-logic baselineprofile/build.gradle.kts | wc -l)" -eq 0 && echo PASS</automated>
  </verify>
  <done>compileSdk=36 in exactly 2 sites; targetSdk=35 explicit in all 3; no targetSdk=36 anywhere; committed alone.</done>
</task>

<!-- ════════════════ GUARDS: CI cache key + crutch-flag + SDK grep guard ════════════════ -->
<task type="auto">
  <name>Task 9: Guards — CI cache key agp8→agp9, no crutch flags, SDK grep assertions (D-05b, D-07, D-09)</name>
  <read_first>
    - .github/workflows/ci.yml (L39 key, L41 restore-keys — agp8 token at both)
    - gradle.properties (assert clean — no crutch flags) and all build scripts
    - 08-RESEARCH.md §"Validation Architecture" (the grep guards) + §"Anti-Patterns"
    - 08-CONTEXT.md D-05b, D-07, D-09
  </read_first>
  <action>
    Three guard edits/assertions (this task is the explicit, grep-verifiable guard required by D-05b):
    1. CI cache key (D-07): in .github/workflows/ci.yml bump the `agp8` token to `agp9` at BOTH sites —
       L39 (the `key:` line) and L41 (the `restore-keys:` fallback). A stale AGP-8 Gradle home poisons AGP-9 config.
    2. Forbidden crutch flags (D-09): assert `android.enableLegacyVariantApi`, `android.newDsl`, and
       `android.builtInKotlin` do NOT appear in gradle.properties or any build script. If any are present, REMOVE
       them — they are forbidden. (The repo is expected clean; this is a guard, not an add.)
    3. SDK grep guard (D-05b): assert compileSdk=36 ×2 and targetSdk=35 ×3 (re-asserted here as a single gate).
    Commit message: `ci(cache): bump Gradle cache key agp8 → agp9; assert no AGP-9 crutch flags`
  </action>
  <acceptance_criteria>
    - `grep -c 'agp8' .github/workflows/ci.yml` == 0  (token bumped at both L39 + L41)
    - `grep -c 'agp9' .github/workflows/ci.yml` == 2
    - `grep -rn 'enableLegacyVariantApi\|android.newDsl\|android.builtInKotlin' . --include='*.properties' --include='*.kts' | wc -l` == 0  (no crutch flags)
    - `grep -rn 'compileSdk = 36' build-logic baselineprofile/build.gradle.kts | wc -l` == 2 AND `grep -rn 'targetSdk = 35' build-logic baselineprofile/build.gradle.kts | wc -l` == 3
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'agp8' .github/workflows/ci.yml)" -eq 0 && test "$(grep -c 'agp9' .github/workflows/ci.yml)" -eq 2 && test "$(grep -rn 'enableLegacyVariantApi\|android.newDsl\|android.builtInKotlin' . --include='*.properties' --include='*.kts' | wc -l)" -eq 0 && echo PASS</automated>
  </verify>
  <done>CI cache key on agp9 (zero agp8 hits); zero crutch flags repo-wide; SDK guards re-asserted; committed.</done>
</task>

<!-- ════════════════ FINAL PHASE GATE (D-08) ════════════════ -->
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10 (checkpoint, D-08): Full phase gate — compileDebugSources + detekt + ktlintCheck + test green</name>
  <what-built>
    All 7 commits + guards are in place: Gradle 9.4.1, AGP 9.2.1, built-in Kotlin, bare CommonExtension,
    baselineprofile compilerOptions, Hilt 2.59.2, compileSdk 36 (targetSdk 35), CI cache key agp9, no crutch flags.
    This is the definition-of-done gate (D-08): the full multi-module build, static analysis, and tests must be
    green across all ~14 modules with the version-isolation opt-out flags ABSENT.
  </what-built>
  <how-to-verify>
    Run the full phase gate (stop any stale daemon first):
      1. `./gradlew --stop`
      2. `./gradlew compileDebugSources detekt ktlintCheck test --stacktrace`
    EXPECTED: BUILD SUCCESSFUL across all modules; banner reports `Gradle 9.4.1`; AGP resolves to 9.2.1; a
    `:feature:*` module's Hilt+KSP DI graph compiles (AGP9-03). Confirm via the build log / a quick
    `./gradlew :app:dependencies --configuration debugRuntimeClasspath | grep -i 'dagger\|hilt'` that Hilt is 2.59.2.

    detekt empirical test (Pitfall 6, D-08): detekt 1.23.8 is expected to RUN (Slopper is parse-only, Kotlin 2.2.20,
    not 2.3.0). IF detekt HARD-FAILS on Gradle 9 + AGP 9, apply the RESEARCH §Pitfall-6 fallback ladder IN ORDER —
    (1) confirm no type-resolution classpath is wired; (2) scope detekt off the failing surface via the existing
    baseline/config; (3) run detekt with `--continue` isolated from the compile gate and TRACK as a documented
    deviation; (4) last resort: temporarily exclude the offending module's detekt task and file tech debt.
    DO NOT adopt detekt 2.0.0-alpha and DO NOT bump Kotlin/KSP (D-08, D-04b). Document any fallback applied in the SUMMARY.

    Final grep gate (paste output):
      - `grep -c 'agp8' .github/workflows/ci.yml` → 0
      - `grep -rn 'org.jetbrains.kotlin.android' build.gradle.kts baselineprofile/build.gradle.kts build-logic gradle/libs.versions.toml | wc -l` → 0
      - `grep 'hilt = ' gradle/libs.versions.toml` → 2.59.2
      - `grep -rn 'compileSdk = 36' build-logic baselineprofile/build.gradle.kts | wc -l` → 2; `targetSdk = 35` → 3
  </how-to-verify>
  <resume-signal>Type "approved" once `compileDebugSources detekt ktlintCheck test` is BUILD SUCCESSFUL on Gradle 9.4.1 / AGP 9.2.1 / Hilt 2.59.2 (and any detekt fallback is documented), or paste the failing output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local/CI → Gradle distribution server (services.gradle.org) | The Gradle 9.4.1 zip is fetched over the network; a tampered/mismatched distribution would execute on the build host. |
| build → Google Maven (dl.google.com) | AGP 9.2.1 plugin artifacts cross into the build classpath. |
| build → Maven Central (repo1.maven.org) | Hilt/Dagger 2.59.2 + KSP artifacts cross into the build classpath. |
| repo → CI runner cache (actions/cache) | A poisoned/stale Gradle home cache can alter configuration outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Gradle 9.4.1 distribution (wrapper) | mitigate | Fetch `distributionSha256Sum` LIVE from the official `.sha256` at execution (task 1); retain `validateDistributionUrl=true`. NEVER hardcode a remembered hash — a stale value fails validation loudly and never ships silently. |
| T-08-02 | Tampering / Spoofing | AGP 9.2.1 + Hilt 2.59.2 provenance | mitigate | Resolve only from Google Maven (AGP) and Maven Central (Hilt) over HTTPS; re-confirm exact published versions via maven-metadata before pinning (tasks 2, 7). Versions pinned exactly — no floating ranges (D-04) that could resolve to an unexpected artifact. |
| T-08-03 | Elevation of Privilege (runtime) | compileSdk 36 vs targetSdk silent flip | mitigate | Keep `targetSdk = 35` explicit at all 3 sites + grep guard (task 9). Prevents AGP 9's `defaultTargetSdkToCompileSdkIfUnset` from silently opting the app into unreviewed Android-16 runtime behavior. |
| T-08-04 | Tampering | CI Gradle-home cache (actions/cache, agp8 token) | mitigate | Bump cache key token agp8→agp9 (task 9) so a stale AGP-8 Gradle home cannot poison AGP-9 configuration; the key also hashes wrapper + catalog (self-invalidates on the version edits). |
| T-08-05 | Denial of Service (gate weakening) | OWASP dependency-check / detekt gate | accept | This phase does NOT weaken the OWASP `failBuildOnCVSS=7.0` gate or adopt a pre-release detekt; detekt fallback ladder explicitly forbids 2.0.0-alpha and any Kotlin/KSP bump. No new dependency is introduced (version bumps only). LOW residual risk for a toolchain-bump phase. |

**ASVS L1 note:** No application code, secrets, auth surface, or user input is touched this phase (CI signing/EdEC is Phase 10). The real surface is supply-chain integrity (T-08-01/02/04), all mitigated above; no HIGH findings.
</threat_model>

<verification>
Phase-level checks (all grep-verifiable + the two build gates):
- `grep 'gradle-9.4.1-bin.zip' gradle/wrapper/gradle-wrapper.properties` → present; old 8.11.1 hash absent; validateDistributionUrl=true kept.
- `grep 'agp = "9.2.1"' gradle/libs.versions.toml` → present (or documented 9.2.0 fallback).
- `grep -rn 'org.jetbrains.kotlin.android' build.gradle.kts baselineprofile/build.gradle.kts build-logic gradle/libs.versions.toml | wc -l` → 0.
- `grep 'commonExtension: CommonExtension,' KotlinAndroid.kt` → present; `CommonExtension<` → absent.
- `grep 'compilerOptions' baselineprofile/build.gradle.kts` → present; `kotlinOptions` → absent.
- `grep 'hilt = "2.59.2"' gradle/libs.versions.toml` → present; no floating range.
- compileSdk=36 ×2, targetSdk=35 ×3, targetSdk=36 ×0 (build-logic + baselineprofile).
- `grep -c 'agp8' .github/workflows/ci.yml` → 0; `agp9` → 2.
- No `enableLegacyVariantApi` / `android.newDsl` / `android.builtInKotlin` anywhere.
- Choke-point smoke (task 5): `:core:common:compileDebugKotlin` BUILD SUCCESSFUL on Gradle 9.4.1.
- Full gate (task 10): `compileDebugSources detekt ktlintCheck test` BUILD SUCCESSFUL across all ~14 modules; detekt either passes or a documented Pitfall-6 fallback (NOT 2.0.0-alpha) is applied.
</verification>

<success_criteria>
Maps 1:1 to the 5 ROADMAP Phase-8 success criteria (the gate contract):
1. AGP resolves to 9.2.1 and `compileDebugSources` is green across all ~14 modules with the version-isolation opt-out flags REMOVED (on Gradle 9.4.1 — folded-forward AGP9-01).
2. `org.jetbrains.kotlin.android` removed from all 5 sites, `CommonExtension` generics gone, `kotlinOptions{}`→`kotlin{compilerOptions{}}` — no "extension already registered" / generics error.
3. Hilt/Dagger resolves to exactly 2.59.2 (never bare 2.59+); a Hilt+KSP DI graph compiles on Kotlin 2.2.20 / KSP 2.2.20-2.0.4.
4. compileSdk=36 in both touchpoints AND targetSdk=35 verifiably explicit in every site — no silent Android-16 opt-in.
5. Full phase gate green (`compileDebugSources detekt ktlintCheck test`) and CI cache key bumped agp8→agp9.
</success_criteria>

<output>
After completion, create `.planning/phases/08-agp-9-atomic-build-logic-migration-compilesdk-36/08-01-SUMMARY.md`.
Record: the final resolved versions (AGP, Gradle, Hilt, the fetched sha256), the 7 commit SHAs (for bisect provenance),
the choke-point smoke result, the full-gate result, and — critically — whether detekt 1.23.8 passed on Gradle 9 + AGP 9
or required a Pitfall-6 fallback (and which rung of the ladder), so the ADR's deferred empirical question is resolved on record.
</output>
