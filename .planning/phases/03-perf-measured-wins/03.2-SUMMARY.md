---
phase: 03-perf-measured-wins
plan: 2
wave: 2
autonomous: true
executed_at: 2026-05-18T23:58:00+09:00
branch: phase-2/comply-platform-compliance
requirements_completed: [PERF-03, PERF-04, PERF-09]
commits:
  - sha: 3daa87e
    type: feat
    scope: perf
    title: "PERF-03 — ImmutableList migration for HomeRail, HomeUiState, markers (HomeScreen fix included)"
  - sha: d0e9d76
    type: docs+feat
    scope: perf
    title: "PERF-04 + PERF-09 — LaunchedEffect key audit (all 5 STABLE) + applyVideoFrameRate relocated"
build:
  assembleDebug_home: green
  assembleDebug_player: green
deviations:
  - id: DEV-01
    task: 1
    type: scope
    summary: "HomeScreen.kt needed ImmutableList migration too — `RailScenes` composable parameter `scenes: List<SceneSummary>` was a compile-time type mismatch. Fixed inline."
---

# Phase 3 Plan 3.2 Summary: Compose Hygiene Audit

## Commits Landed

| # | SHA | Title |
|---|-----|-------|
| 1 | `3daa87e` | feat(perf): PERF-03 — ImmutableList migration for HomeRail, HomeUiState, markers (HomeScreen fix included) |
| 2 | `d0e9d76` | docs(perf): PERF-04 — LaunchedEffect key audit — all 5 sites STABLE + PERF-09 applyVideoFrameRate relocated |

## PERF-03: ImmutableList Migration

### Files changed
- `feature/home/build.gradle.kts` — `implementation(libs.kotlinx.collections.immutable)` added
- `feature/player/build.gradle.kts` — `implementation(libs.kotlinx.collections.immutable)` added
- `feature/home/.../HomeViewModel.kt` — `HomeRail.scenes: ImmutableList<SceneSummary>`, `HomeUiState.rails: ImmutableList<HomeRail>`, `Initial` companion `.toPersistentList()`, 2 update sites `.toPersistentList()`
- `feature/home/.../HomeScreen.kt` — `RailScenes` composable parameter `scenes: ImmutableList<SceneSummary>` + import
- `feature/player/.../PlayerScreen.kt` — `PlayerControls.markers: ImmutableList<Marker>`, `TimelineBar.markers: ImmutableList<Marker>`, call sites `.orEmpty().toPersistentList()`

### Deviation: HomeScreen.kt composite parameter (DEV-01)
`RailScenes(scenes: List<SceneSummary>)` in `HomeScreen.kt` is called with `homeRail.scenes` (now `ImmutableList<SceneSummary>`). Needed to migrate the parameter type. This was a compile-time type mismatch caught during build verification. Fixed in the same commit.

### Verify results
- `grep -rn 'List<SceneSummary>\|List<HomeRail>\|List<Marker>' feature/` → 0 hits ✓
- `./gradlew :feature:home:assembleDebug :feature:player:assembleDebug` → BUILD SUCCESSFUL ✓

## PERF-04: LaunchedEffect Key Audit (documentation only)

All 5 sites are STABLE. No code changes needed.

| Line | Type | Keys | Verdict |
|------|------|------|---------|
| 150 | DisposableEffect | `activity, rotationLocked` | STABLE — Activity ref stable for VM lifetime; Boolean primitive |
| 168 | LaunchedEffect | `resizeMode, playerView` | STABLE — playerView is top-level `remember { mutableStateOf<PlayerView?>(null) }`, set once by AndroidView.factory; stable after first frame |
| 170 | LaunchedEffect | `controlsVisible, state.isPlaying, lastInteraction, locked` | STABLE — Boolean/Long primitives from state |
| 177 | LaunchedEffect | `stepLeft?.generation` | STABLE — Long? counter |
| 183 | LaunchedEffect | `stepRight?.generation` | STABLE — Long? counter |

Stability comment added above line 168 (the only non-trivial case).

## PERF-09: applyVideoFrameRate Relocated

Removed from `AndroidView(update = { ... })` block (was firing on every recomposition).
Added as `LaunchedEffect(state.videoFrameRate)` at PlayerScreen.kt:228, targeting the top-level `playerView` remember ref (line 144).

```kotlin
// BEFORE (line 224, in AndroidView.update):
applyVideoFrameRate(it, state.videoFrameRate)

// AFTER (line 228, LaunchedEffect):
LaunchedEffect(state.videoFrameRate) {
    playerView?.let { applyVideoFrameRate(it, state.videoFrameRate) }
}
```

## Anti-coupling check
`grep -rn 'import .*Spine' feature/ core/ app/` → 0 hits ✓
