---
phase: 01-deps-foundation-bump
plan: 01.1
status: HALTED — stop_phase triggered before DEPS-01 commit
type: execute
wave: 1
halted_at: 2026-05-16T16:05+09:00
halted_during: Task 1 (DEPS-01) — lint baseline generation prerequisite
halted_reason: codebase at branch HEAD does not compile (multiple pre-existing build breakages)
on_failure_directive: stop_phase (lockstep chain — CONTEXT.md Decision 3)
completed_requirements: []
attempted_requirements: [DEPS-01]
commits_landed_this_run:
  - cf5f1f0 fix(build):add missing :core:data dep to :core:ui for UiPreferences injection
  - 5f924be fix(build):remove duplicate parsePreset() function in LibraryViewModel
deps_commits_landed: []
---

# Phase 1 Plan 01.1: Lockstep Toolchain Floor — HALT REPORT (2nd halt)

## One-line summary

Plan 01.1 halted before any DEPS-NN commit landed: the codebase at branch HEAD `3783973` does not compile because of multiple pre-existing build breakages discovered when running `./gradlew :app:updateLintBaseline` (the first verification step of Task 1 / DEPS-01). Two precondition fixes were landed inline (Rule 3 blocking-fix) but a third corruption — an orphaned 17-line snippet prepended before the `package` declaration in `feature/player/.../PlayerScreen.kt` — requires architectural judgment about where the misplaced code was meant to live. Per Rule 4 (architectural decision) and the plan's `on_failure: stop_phase` directive on the lockstep chain, the executor halts rather than guess.

This SUPERSEDES the prior halt summary (which had identified missing Apollo schema, since fixed by orchestrator commit `3783973`).

## Environment verification (PASS)

- `java -version` → `openjdk 17.0.17` ✓
- `ls $ANDROID_SDK_ROOT/platforms` → `android-35 android-36` ✓
- `./gradlew :core:network:generateStashApolloSources` was the only orchestrator-verified target at handoff. It passes, but it does NOT prove the rest of the codebase compiles. A full `./gradlew assembleDebug` had never been attempted before this executor run.

## What was attempted

Task 1 (DEPS-01) — generate pre-bump lint baseline against AGP 8.7.3:

```bash
./gradlew :app:updateLintBaseline
```

Failed with KSP errors in `:core:ui` (`error.NonExistentClass` in the `StashImageLoaderFactory @Inject` constructor) caused by a missing `implementation(project(":core:data"))` dependency.

## Precondition fixes applied (Rule 3 — blocking)

These are inline auto-fixes for pre-existing bugs that blocked DEPS-01 verification. They are NOT part of any DEPS-NN scope; they are precondition repairs analogous to the orchestrator's earlier `3b16767` and `3783973`.

### Fix 1 — commit `cf5f1f0`

`core/ui/build.gradle.kts` — added `implementation(project(":core:data"))`.

Reason: `StashImageLoaderFactory @Inject` constructor takes `UiPreferences` which lives in `:core:data`, but `:core:ui` did not depend on `:core:data`. Hilt KSP exploded with `error.NonExistentClass`. Resolved by adding the missing module dep. Verified with `./gradlew :core:ui:compileDebugKotlin` → BUILD SUCCESSFUL.

### Fix 2 — commit `5f924be`

`feature/library/src/main/java/io/stashapp/android/feature/library/LibraryViewModel.kt` — removed duplicate `parsePreset(String?): SceneFilter` (was declared twice at lines 110 and 129 — identical bodies).

Reason: Conflicting overloads compile error. Resolved by deleting the duplicate copy. Verified with `./gradlew :feature:library:compileDebugKotlin` → BUILD SUCCESSFUL.

## Halting blocker — Rule 4 / `stop_phase`

`feature/player/src/main/java/io/stashapp/android/feature/player/PlayerScreen.kt` has a 17-line orphan snippet prepended BEFORE the `package` declaration:

```
Line  1–17: dangling `AndroidView(...)` factory/update lambda body, referencing
            `resizeMode`, `viewModel`, `state`, `applyVideoFrameRate`, `playerView`
            — all undefined at file scope.
Line 18:    `)package io.stashapp.android.feature.player`
            ← stray `)` immediately followed by the proper `package` declaration.
Line 19+:   imports + the actual file content (1122 lines total).
```

Sample compiler errors:

```
e: PlayerScreen.kt:17:14 Expecting a top level declaration
e: PlayerScreen.kt:18:09 Expecting a top level declaration
e: PlayerScreen.kt:18:52 imports are only allowed in the beginning of file
```

This corruption originated in the initial restore commit `bf01b34 feat: restore Slopper Android app from Claude session history` (the only commit touching the file per `git log --oneline -3 -- feature/player/.../PlayerScreen.kt`).

The orphan references a composable context — the `AndroidView { factory = …, update = … }` block was probably meant to be inserted **inside** an existing `@Composable` function elsewhere in the file. Naive deletion may lose intended functionality; correct repair requires deciding whether (a) the same block already exists further down (so the orphan is a stale duplicate that should be deleted), or (b) the orphan must be relocated to its intended insertion point.

Per Rule 4 (architectural / non-trivial repair) and the plan's `on_failure: stop_phase` directive on the lockstep chain, the executor MUST stop rather than guess. DEPS-01 cannot run a clean lint baseline pass while the codebase fails `assembleDebug`.

## Must-haves status (all FAIL — plan halted before DEPS-01 commit)

| # | Truth | Status | Evidence |
| - | ----- | ------ | -------- |
| 1 | `app/lint-baseline.xml` exists and was committed before any version bump | FAIL | File not generated — Task 1 aborted by `:feature:player:kspDebugKotlin` failure |
| 2 | `./gradlew -q javaToolchains` resolves exactly one JDK 17 | NOT REACHED | DEPS-02 not started |
| 3 | Gradle wrapper distribution is 9.4.1 + SHA256 pin + cc reuse | NOT REACHED | DEPS-03 not started |
| 4 | AGP 9.2.0 + compileSdk/targetSdk 36 atomic | NOT REACHED | DEPS-04 not started |
| 5 | Kotlin 2.2.20, KSP 2.2.20-2.0.4, deprecated `-X` flags gone | NOT REACHED | DEPS-05 not started |
| 6 | Compose BOM 2026.05.00 + deprecation-clean compile | NOT REACHED | DEPS-06 not started |

## Deviations from Plan

### Rule 3 (blocking-fix) deviations

1. Added missing module dependency `:core:ui → :core:data` (commit `cf5f1f0`).
2. Removed duplicate function declaration in `LibraryViewModel.kt` (commit `5f924be`).

### Rule 4 (architectural / halt) deviation

3. `feature/player/PlayerScreen.kt` orphan-snippet corruption — NOT FIXED. Requires owner judgment about whether the orphan is a stale duplicate to delete or a fragment to relocate.

## Failures & Reverts

| Task        | Outcome      | Commits | Notes |
| ----------- | ------------ | ------- | ----- |
| DEPS-01     | HALTED       | none    | Blocked by pre-existing `:feature:player` corruption (Rule 4) |
| DEPS-02     | NOT STARTED  | none    | Lockstep — cannot proceed past DEPS-01 |
| DEPS-03+04  | NOT STARTED  | none    | Lockstep — cannot proceed past DEPS-01 |
| DEPS-05     | NOT STARTED  | none    | Lockstep — cannot proceed past DEPS-01 |
| DEPS-06     | NOT STARTED  | none    | Lockstep — cannot proceed past DEPS-01 |

## Recommended next action (for human / orchestrator)

1. **Triage the orphan in `feature/player/.../PlayerScreen.kt`** (lines 1–18). Use `git show bf01b34:feature/player/src/main/java/io/stashapp/android/feature/player/PlayerScreen.kt | head -40` to inspect the restore-commit form. Two plausible repairs:
   - **(a) Delete lines 1–17 + the stray `)` on line 18** if the same `AndroidView(...)` block already exists later in the file (most likely — restore appears to have accidentally pre-pended a fragment).
   - **(b) Relocate the orphan** into the composable it was severed from.
2. **Run a full sweep**: `./gradlew assembleDebug` to surface any other pre-existing breakages, fix as precondition commits.
3. **Resume Plan 01.1 from Task 1 (DEPS-01)**: `./gradlew :app:updateLintBaseline` against the still-current AGP 8.7.3 toolchain, then DEPS-02..06 as written.

## Self-Check: PASSED

- Precondition fix commits exist on branch `phase-1/deps-bump`:
  - `cf5f1f0` → present in `git log --oneline -5` ✓
  - `5f924be` → present in `git log --oneline -5` ✓
- No DEPS-NN commits claimed (none landed) — verified `git log --format=%s phase-1/deps-bump | grep -cE '^chore\(deps\): DEPS-' = 0`.
- `app/lint-baseline.xml` claimed missing — verified absent on disk and untracked.
- HEAD on branch `phase-1/deps-bump`.
