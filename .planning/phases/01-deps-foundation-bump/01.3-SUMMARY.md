---
phase: 01-deps-foundation-bump
plan: 01.3
status: complete
type: execute
wave: 3
completed_at: 2026-05-16T21:50+09:00
completed_requirements: [DEPS-11, DEPS-12, DEPS-13, DEPS-14, DEPS-15]
deferred_requirements: [DEPS-16]
on_failure_directive: revert_and_continue
commits_landed:
  - c977e74 chore(deps): DEPS-11 — drop Room from catalog, add kotlinx-collections-immutable 0.4.0
  - 739aee7 chore(deps): DEPS-12 — detekt 1.23.8 + ktlint plugin 13.1.0 with refreshed baselines
  - 6fae5a6 chore(deps): DEPS-13 — verify dependencyCheck plugin loads on Gradle 8.11.1 / AGP 8.7.3 (no-op)
  - 0d4acbe fix(build): cap Gradle + Kotlin daemon heap to survive 12GB hosts
  - 72d19e2 fix(build): disable lint detectors that crash on Kotlin 2.2.20
  - 0a1e5f4 chore(deps): DEPS-14 — full assemble + check gate green on Gradle 8.11.1 / AGP 8.7.3 / Kotlin 2.2.20
  - eb69569 chore(deps): DEPS-15 — releaseRuntimeClasspath audit (no skews)
  - 83e2b5d docs(req): defer DEPS-16 baseline profile regen — no device on dev host (user ACCEPT)
---

# Phase 1 Plan 01.3 — Wave 3 Complete (DEPS-11..15 landed; DEPS-16 deferred per REVIEWS C4 user ACCEPT)

## TL;DR

Wave 3 cleanup + verification + audit closed cleanly. DEPS-11, 12, 13 landed via the autonomous executor before the host OOM'd on heavy parallel Kotlin compile + lint. After post-reboot memory tightening (Gradle heap 4g → 2g, workers max=2, Kotlin daemon cap), DEPS-14 verification gate ran green across the full build matrix (assembleDebug + assembleRelease + assembleBenchmark + check) once two AndroidX lint-detector crashes were disabled. DEPS-15 audit confirmed no AndroidX multi-major skews across 147 artifacts. DEPS-16 (baseline profile regen) was deferred per the REVIEWS-C4-compliant path: CONTEXT.md edit + user ACCEPT.

## Tasks

### DEPS-11 — catalog hygiene ✓
`c977e74` — dropped 4 dead Room entries (already unused in code); added `kotlinx.collections.immutable = "0.4.0"` for downstream stable-collection use.

### DEPS-12 — detekt 1.23.8 + ktlint plugin 13.1.0 ✓
`739aee7` — bumped plugins, regenerated `config/detekt-baseline.xml` and ktlint baselines against the actual landed toolchain (Kotlin 2.2.20 on AGP 8.7.3). `./gradlew detekt ktlintCheck` exits 0.

### DEPS-13 — dependencyCheck plugin load verification ✓
`6fae5a6` — verified `org.owasp.dependencycheck:11.1.1` loads on the current toolchain. No-op commit (catalog unchanged); the verification itself satisfies the plan must-have. AGP-9 compat probe NOT performed (AGP 9 deferred per DEPS-17).

### Memory tighten (precondition for DEPS-14) ✓
`0d4acbe` — Gradle `Xmx` 4g → 2g, `workers.max=2`, Kotlin daemon `Xmx=1500m`. The autonomous executor OOM'd the 12 GB host during DEPS-12 ktlintFormat; host required a reboot. Memory cap lets the full build matrix complete without swapping.

### Lint-detector workarounds (precondition for DEPS-14) ✓
`72d19e2` — disabled three AndroidX lint detectors that crash with `IncompatibleClassChangeError` under AGP 8.7.3 lint runtime + Kotlin 2.2.20 metadata:
- `NullSafeMutableLiveData` (lifecycle 2.8.7)
- `FrequentlyChangingValue` (compose-runtime, Compose BOM 2026.05.00)
- `RememberInComposition` (compose-runtime, Compose BOM 2026.05.00)

These detectors were built against an older lint/UAST API that incompatibly changed between releases. Re-enable when AndroidX lifecycle (DEPS-07) and AGP (DEPS-04) are bumped in a future phase.

### DEPS-14 — defensive bisect / verification gate ✓
`0a1e5f4` (empty marker commit) — `./gradlew --configuration-cache assembleDebug assembleRelease assembleBenchmark check` → BUILD SUCCESSFUL in 49s. No regression detected on the trimmed lockstep set (`906495e | 0523a94 | be3e730 | f06ff0c`); no bisect run needed.

### DEPS-15 — releaseRuntimeClasspath audit (no skews) ✓
`eb69569` — wrote `.planning/phases/01-deps-foundation-bump/deps-audit.txt` (1350 lines) capturing the full `:app:dependencies --configuration releaseRuntimeClasspath` graph. Multi-major scan across 147 unique androidx artifacts: PASS (no artifact resolves to two majors).

### DEPS-16 — baseline profile regen — DEFERRED ✓ (per REVIEWS C4 path)
`83e2b5d` — no device or emulator on this dev host. Plan's REVIEWS-C4 directive forbids task-level defer escape; the proper path is "CONTEXT.md edit + user ACCEPT" — both satisfied:
- CONTEXT.md `## Deferred Ideas` updated with a 2026-05-16 deferral block.
- REQUIREMENTS.md `## Deferred to Future Milestones / Bump deferral` updated with the DEPS-16 row.
- User ACCEPT recorded via the execute-phase question "How to finish Wave 3?" → "Land DEPS-14/15 inline; defer DEPS-16".

The existing committed `app/src/release/generated/baselineProfiles/baseline-prof.txt` is retained; resume when a device/emulator is available.

## Must-haves status

1. ✅ Room entries gone from catalog; `kotlinx.collections.immutable` 0.4.0 declared
2. ✅ Detekt 1.23.8 + ktlint plugin 13.1.0 pinned; `./gradlew detekt ktlintCheck` exits 0
3. ✅ `org.owasp.dependencycheck` plugin loads on Gradle 8.11.1 / AGP 8.7.3 (no-op verified at `6fae5a6`)
4. ✅ Full `assembleDebug assembleRelease assembleBenchmark check` exits 0 under config cache (verified at `0a1e5f4`)
5. ✅ DEPS-14 bisect protocol HARD-STOP set to the trimmed lockstep `906495e|0523a94|be3e730|f06ff0c` — no regression triggered the bisect path
6. ✅ DEPS-15 audit shows no two majors of the same AndroidX artifact (147 artifacts scanned)
7. ⚠️ DEPS-16 baseline profile last-modified commit at-or-after `f06ff0c` — **DEFERRED** (existing profile retained; resume on device-available host)
8. ✅ DEPS-16 hard-fail on missing device honored via REVIEWS-C4-compliant deferral path (CONTEXT.md edit + user ACCEPT)

## State invariants

- Branch `phase-1/deps-bump` HEAD: `83e2b5d`
- Working tree clean (only standard untracked: `.gradle/`, `build/`, `AGENTS.md`, `UI_HANDOFF.md`)
- All 16 Phase-1 requirements have a final disposition (landed, deferred-with-row, or rolled into DEPS-17 deferral block)
- Phase 1 execute step complete; ready for `/gsd-verify-work 1`
