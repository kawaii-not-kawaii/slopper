---
phase: 07-gradle-9-core-version-bump-deprecation-sweep
plan: "07.2"
subsystem: infra
tags: [gradle, gradle-9, agp, detekt, ktlint, adr, fold-forward, build-toolchain]

# Dependency graph
requires:
  - phase: 07.1
    provides: "gradle9-deprecations.log — the empty Gradle-9 deprecation surface on 8.11.1 + the detekt-tasks-SKIPPED evidence cited for the detekt verdict"
  - phase: 07-research
    provides: "Fold-forward decision, per-plugin Gradle-9 verdicts, the AGP-8.7.3 × Gradle-9 incompatibility finding, the 9.4.1 pinned target"
provides:
  - "docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md — the single authoritative Gradle-9 readiness/decision record (pinned target, plugin verdicts, detekt decision, KGP floor, Phase-8 fold-forward hand-off)"
  - "Established docs/adr/ ADR convention (ADR 0001 — first record in the repo)"
  - "Recorded detekt-on-Gradle-9 governance decision (keep stable 1.23.8, defer empirical test to Phase 8, reject 2.0.0-alpha)"
affects: [08-agp-9, detekt-on-gradle-9 decision]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "ADR convention established at docs/adr/ (Status/Context/Decision/Consequences)"
    - "Fold-forward recorded: Gradle 9.4.1 wrapper flip + live sha256 fetch = Phase 8 commit 1"

key-files:
  created:
    - "docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md"
  modified: []

key-decisions:
  - "Pinned Gradle 9.4.1 target recorded with sha256 fetched-at-Phase-8-execution (never hard-coded); live wrapper stays 8.11.1"
  - "detekt 1.23.8 kept (stable); accept/track Gradle-9 warnings, defer empirical run-vs-fail to Phase 8, reject 2.0.0-alpha"
  - "KGP 2.2.20 / KSP 2.2.20-2.0.4 satisfy AGP-9's 2.2.10 floor — no Kotlin/KSP bump"
  - "AGP-8.7.3 hard-fails on Gradle 9 → wrapper flip folds forward to Phase 8 commit 1; AGP9-01 delivered PARTIALLY"

patterns-established:
  - "First ADR in the repo — docs/adr/ now the home for architecture decision records"

requirements-completed: [AGP9-01]

# Metrics
duration: 3min
completed: 2026-05-30
---

# Phase 7 Plan 07.2: Gradle-9 Readiness ADR + AGP-8.7.3 Fold-Forward Summary

**The single authoritative Gradle-9 readiness/decision record written as `docs/adr/0001` — pins the Gradle 9.4.1 target (sha256 fetched at Phase-8 execution, never hard-coded), records all four toolchain-plugin Gradle-9 verdicts, makes the detekt keep-stable/defer/reject-alpha decision, confirms the KGP 2.2.10 floor is already met, and documents the AGP-8.7.3 fold-forward hand-off to Phase 8 — with zero edits to live build config.**

## Performance

- **Duration:** ~3 min
- **Tasks:** 1
- **Files created:** 1 (`docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md`)
- **Files modified:** 0 (no live build config touched)

## Accomplishments

- Established the `docs/adr/` convention (first ADR in the repo) and wrote **ADR 0001** with all 8 required sections in a standard Status / Context / Decision / Consequences structure (162 lines).
- **Pinned target:** recorded `distributionUrl=…/gradle-9.4.1-bin.zip` and mandated the `distributionSha256Sum` be **fetched at Phase-8 execution** from the official `.sha256` URL (never a remembered hash); kept `validateDistributionUrl=true`; recorded 9.4.1 (exact AGP-9.2 floor), not 9.5.x.
- **Plugin verdicts table** (ktlint 14.2.0 READY ✓; OWASP dependency-check 12.2.2 runs only via `--no-configuration-cache`; baseline-profile 1.4.1 verified/not-exercised; detekt 1.23.8 = the open decision).
- **detekt decision** recorded verbatim: keep stable 1.23.8, accept/track Gradle-9 warnings, defer the empirical run-vs-fail test to Phase 8 (untestable in Phase 7 — detekt tasks were SKIPPED in 07.1's dry-run), explicitly REJECT 2.0.0-alpha.x; cites `gradle9-deprecations.log`.
- **KGP floor** confirmed: Kotlin 2.2.20 / KSP 2.2.20-2.0.4 already satisfy AGP-9's KGP ≥ 2.2.10 → no bump.
- **Fold-forward hand-off** documented: AGP 8.7.3 hard-fails on Gradle 9, so the wrapper flip + live sha256 fetch become Phase 8 commit 1; AGP9-01's "green on Gradle 9" clause is asserted at the Phase-8 gate. AGP9-01 delivered PARTIALLY.
- **Explicitly rejected** Gradle 9.5.x, detekt 2.0.0-alpha, running AGP 8.7.3 on Gradle 9, and the `android.enableLegacyVariantApi`/`android.newDsl=false` crutches.
- Verified **no live build config touched**: `gradle-wrapper.properties` still reads `gradle-8.11.1-bin.zip`; `agp = "8.7.3"` still in the catalog.

## Task Commits

1. **Task 1: Write the Gradle-9 readiness ADR** — `24a66f7` (docs)

**Plan metadata:** _(final docs commit — see below)_

## Files Created/Modified

- `docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md` — the Gradle-9 readiness/decision record: pinned 9.4.1 target, four plugin verdicts, the verbatim detekt decision, KGP-floor confirmation, the Phase-8 fold-forward hand-off, and the explicitly-rejected options. Establishes the `docs/adr/` convention.

## Decisions Made

- **Pinned Gradle 9.4.1, sha256 fetched-at-execution.** The integrity control (official `.sha256` fetch + `validateDistributionUrl=true`) is RECORDED in Phase 7, ENFORCED in Phase 8 — no hash is hard-coded from memory (threat T-07.2-01).
- **detekt 1.23.8 kept; alpha rejected; empirical test deferred.** detekt applies Kotlin/AGP as `compileOnly` and its tasks were SKIPPED in 07.1's Gradle-8.11.1 dry-run, so the Gradle-9 surface is empirically unknown until AGP 9 lands. Stable-only policy forbids 2.0.0-alpha (threat T-07.2-03, disposition accept).
- **No Kotlin/KSP bump.** 2.2.20 > the 2.2.10 AGP-9 floor; Phase 8 carries no Kotlin/KSP change.
- **Fold-forward.** Wrapper flip is Phase 8 commit 1; AGP9-01 is PARTIALLY delivered (pin + sweep + audit + decision + floor here; green-on-Gradle-9 asserted at the Phase-8 gate).

## Deviations from Plan

None — plan executed exactly as written. The single task produced the ADR with all 8 required sections; every grep-based acceptance criterion passed on the first write; no live build config was edited.

## Threat Surface

The ADR records (does not introduce) two mitigations carried into Phase 8: the Gradle-distribution integrity pin (official sha256 fetch + `validateDistributionUrl=true`, T-07.2-01) and the OWASP dependency-check coverage continuity (`--no-configuration-cache` contract + `failBuildOnCVSS=7.0` preserved, T-07.2-02). It explicitly rejects the detekt-2.0.0-alpha supply-chain risk (T-07.2-03, accept). No new security-relevant surface introduced — the ADR is read-only knowledge touching no executable build surface.

## Known Stubs

None — the ADR is a complete decision record with all recorded facts cross-checked against `07-RESEARCH.md`, `gradle9-deprecations.log`, `gradle/libs.versions.toml`, and `gradle/wrapper/gradle-wrapper.properties`.

## User Setup Required

None.

## Next Phase Readiness

- **Phase 8 fully de-risked and specified:** the ADR is the authoritative hand-off. Commit 1 of Phase 8 = flip `gradle-wrapper.properties` to `gradle-9.4.1-bin.zip` + fetch the live sha256, then the atomic AGP-9 change-set (AGP 9.2.1 → drop `kotlin.android` → `CommonExtension` fix → `compilerOptions` → Hilt 2.59.2 → compileSdk 36).
- **One open item carried to Phase 8:** the detekt-1.23.8-on-Gradle-9 empirical verdict (run-with-warnings vs. hard-fail) — testable only once AGP 9 lands and the build configures on Gradle 9.
- **AGP9-01 status:** the pin + sweep (07.1) + plugin audit + detekt decision + KGP-floor sub-goals are satisfied across Phase 7; the "build runs green ON Gradle 9" clause is asserted at the Phase-8 gate per the fold-forward.

## Self-Check: PASSED

- Created file exists: `docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md` — FOUND (162 lines).
- Task commit exists: `24a66f7` (docs) — FOUND.
- All 8 grep acceptance criteria PASS: 9.4.1 target, `gradle-9.4.1-bin.zip.sha256` fetched-at-execution note, all four plugin verdicts, detekt 1.23.8 + alpha rejection, KGP 2.2.10 + 2.2.20, Phase 8 + fold.
- No live build config touched: `gradle-wrapper.properties` STILL `gradle-8.11.1-bin.zip` — PASS; `agp = "8.7.3"` STILL in catalog — PASS.

---
*Phase: 07-gradle-9-core-version-bump-deprecation-sweep*
*Completed: 2026-05-30 — Phase 7 complete at 2/2.*
