---
phase: 07-gradle-9-core-version-bump-deprecation-sweep
plan: "07.1"
subsystem: infra
tags: [gradle, gradle-9, agp, detekt, ktlint, deprecation-sweep, build-toolchain]

# Dependency graph
requires:
  - phase: 07-research
    provides: "Fold-forward decision + enumerate-on-old-Gradle-first pattern + per-plugin Gradle-9 verdicts"
provides:
  - "gradle9-deprecations.log — the Gradle-9 deprecation inventory captured on the current (green) Gradle 8.11.1 + AGP 8.7.3 toolchain"
  - "Empirical finding: ZERO Gradle-9 deprecations emitted by the current build (help + detekt + ktlint enumeration)"
  - "Independent grep confirmation that repo-authored build scripts use NO Gradle-9-removed APIs (only layout.buildDirectory)"
  - "No-regression baseline: the v1.0 CI gate (compileDebugSources detekt ktlintCheck test) still green on Gradle 8.11.1"
affects: [08-agp-9, detekt-on-gradle-9 decision]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Enumerate-on-the-old-Gradle-first: run ./gradlew help/detekt/ktlint --warning-mode=all on the CURRENT Gradle before any wrapper flip"
    - "Fold-forward: the Gradle 9.4.1 wrapper flip is deferred to Phase 8 commit 1 (AGP 8.7.3 hard-fails on Gradle 9)"

key-files:
  created:
    - ".planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log"
  modified: []

key-decisions:
  - "Wrapper NOT flipped to 9.4.1 — stays on gradle-8.11.1-bin.zip; flip folds forward to Phase 8 commit 1"
  - "detekt 1.23.8 kept; its Gradle-9 warning behavior is empirically untestable on 8.11.1 and is deferred to Phase 8's first green-on-Gradle-9 build (do NOT adopt the 2.0.0-alpha line)"
  - "OWASP dependencyCheck excluded from the regression gate (out-of-band --no-configuration-cache task); its config left untouched"

patterns-established:
  - "Enumerate-on-the-old-Gradle-first: surface Gradle-9 deprecations via --warning-mode=all on the running toolchain before bumping the wrapper"
  - "Force-add scoped planning artifacts that match a global *.log gitignore (single-path -f, no gitignore edit)"

requirements-completed: [AGP9-01]

# Metrics
duration: 4min
completed: 2026-05-30
---

# Phase 7 Plan 07.1: Gradle-9 Readiness + Deprecation Sweep Summary

**Gradle-9 deprecation inventory captured on the current Gradle 8.11.1 + AGP 8.7.3 toolchain — ZERO deprecations emitted, repo build scripts independently confirmed Gradle-9-clean, and the v1.0 CI gate still green with zero regression; the wrapper flip correctly deferred to Phase 8.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-05-30T10:04:24Z
- **Completed:** 2026-05-30T10:08:09Z
- **Tasks:** 2
- **Files modified:** 1 (gradle9-deprecations.log — created)

## Accomplishments
- Ran the deprecation enumeration on the live toolchain (`./gradlew help --warning-mode=all` + `./gradlew detekt ktlintCheck --warning-mode=all --dry-run`) — both BUILD SUCCESSFUL with **zero** Gradle-9 deprecation lines across all ~19 modules.
- Wrote `gradle9-deprecations.log` with four sections: raw enumeration output, per-plugin Attribution, Own-Script API Audit, and No-Regression Baseline.
- Independently confirmed (grep audit) the repo's own build scripts contain **no** Gradle-9-removed APIs — the only `buildDir`-family token is `layout.buildDirectory` (the correct replacement) in `AndroidComposeConventionPlugin.kt:27-28`.
- Proved no regression: the established CI gate `compileDebugSources detekt ktlintCheck test` is **green** on Gradle 8.11.1 (841 actionable tasks, BUILD SUCCESSFUL).
- Verified the live `gradle-wrapper.properties` still resolves `gradle-8.11.1-bin.zip` — the forbidden 9.4.1 flip was NOT performed.

## Task Commits

Each task was committed atomically:

1. **Task 1: Enumerate and attribute Gradle-9 deprecations on the current toolchain** - `9cba2e0` (feat)
2. **Task 2: Confirm build scripts Gradle-9-clean and prove no regression on 8.11.1** - `1b5b460` (feat)

**Plan metadata:** _(final docs commit — see below)_

## Files Created/Modified
- `.planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log` — the Gradle-9 deprecation inventory: enumeration output + per-plugin Attribution + Own-Script API Audit + No-Regression Baseline. Force-added (`-f`) because it matches the global `*.log` gitignore rule.

## Decisions Made
- **Wrapper left on 8.11.1 (fold-forward).** Per 07-RESEARCH.md HIGH-confidence finding, AGP 8.7.3 hard-fails on Gradle 9, so the wrapper flip to 9.4.1 + sha256 re-pin is Phase 8 commit 1, not this plan.
- **detekt 1.23.8 retained; Gradle-9 test deferred.** detekt applies Kotlin/AGP as `compileOnly`, so its Gradle-8.x-targeted internal API usage does not trip the deprecation reporter on Gradle 8.11.1. The empirical run-vs-fail test is impossible until AGP 9 is present (Phase 8). Decision intent recorded: keep 1.23.8, accept/track warnings if they surface in Phase 8, do NOT pre-emptively adopt the 2.0.0-alpha line.
- **OWASP dependencyCheck excluded from the regression gate.** It is an out-of-band `--no-configuration-cache` task; including it in the compile/test path is wrong. Its scan config (`build.gradle.kts:18-32`) was left untouched (threat T-07.1-03 mitigation preserved).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Force-added the log artifact past the global `*.log` gitignore**
- **Found during:** Task 1 (committing the deprecation log)
- **Issue:** `.gitignore:32` has a global `*.log` rule that ignores `gradle9-deprecations.log`. A plain `git add` failed with "paths are ignored". The plan's `files_modified` frontmatter and `<output>` spec both designate this exact `.log` as the deliverable to commit, so it must enter version control.
- **Fix:** Used `git add -f` scoped to the single artifact path. Did NOT edit `.gitignore` (that would un-ignore all build logs — out of scope and undesirable for an Android Gradle repo).
- **Files modified:** none beyond the intended log; `.gitignore` left untouched.
- **Verification:** `git status --short` shows the log staged as `A`; both task commits include it; `git check-ignore` still reports the global rule intact for other logs.
- **Committed in:** `9cba2e0` (Task 1) and `1b5b460` (Task 2).

---

**Total deviations:** 1 auto-fixed (1 blocking).
**Impact on plan:** Minimal — the force-add is the only viable way to commit the designated deliverable without weakening the repo's gitignore. No scope creep; no build config touched.

## Issues Encountered
- One **pre-existing, out-of-scope** warning observed during the gate: a kotlin-reflect compilation-classpath warning in `feature/settings/src/test/.../SettingsViewModelTest.kt:31` (a unit-test reflection warning, NOT a Gradle-9 deprecation, NOT introduced by this phase). Left unfixed per the executor scope boundary; noted in the log's No-Regression Baseline section.
- No config-cache fallback was needed — `--no-configuration-cache` was not required for any enumeration or gate run.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- **Phase 8 (AGP-9) de-risked:** the Gradle-9 deprecation surface on the current toolchain is empirically **empty**, and the repo's own scripts are confirmed Gradle-9-clean. Phase 8 carries no surprise repo-authored migration items into its atomic commit.
- **Fold-forward handoff is explicit:** the wrapper flip to `gradle-9.4.1-bin.zip` + live sha256 fetch is Phase 8 commit 1 (per ROADMAP). The pinned target diff is documented in 07-RESEARCH.md.
- **One open item carries to Phase 8:** the detekt-1.23.8-on-Gradle-9 empirical verdict (run-with-warnings vs. hard-fail) — testable only once AGP 9 lands and the build configures on Gradle 9.
- **AGP9-01 status:** the enumeration + attribution + own-script-audit + no-regression sub-goals are satisfied here; the "build runs green ON Gradle 9" clause is asserted at the Phase 8 gate (fold-forward), as the requirement and research intend.

## Self-Check: PASSED

- Created files exist: `gradle9-deprecations.log`, `07.1-SUMMARY.md` — both FOUND.
- Task commits exist: `9cba2e0` (Task 1), `1b5b460` (Task 2) — both FOUND.
- Wrapper integrity: `gradle-wrapper.properties` STILL reads `gradle-8.11.1-bin.zip` — PASS (forbidden flip not performed).
- Log sections present: Attribution, Own-Script API Audit, No-Regression Baseline, and the explicit "NO Gradle-9 deprecations" finding — all PASS.

---
*Phase: 07-gradle-9-core-version-bump-deprecation-sweep*
*Completed: 2026-05-30*
