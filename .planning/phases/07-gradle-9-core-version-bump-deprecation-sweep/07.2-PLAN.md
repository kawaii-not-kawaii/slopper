---
phase: 07-gradle-9-core-version-bump-deprecation-sweep
plan: "07.2"
type: execute
wave: 2
depends_on: ["07.1"]
files_modified:
  - docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md
autonomous: true
requirements: [AGP9-01]
user_setup: []

must_haves:
  truths:
    - "The Gradle 9.4.1 target is pinned/recorded in the ADR with the confirmed distribution URL and a note that the fresh distributionSha256Sum is fetched at Phase-8 execution time"
    - "Each toolchain plugin has a recorded Gradle-9 verdict: ktlint 14.2.0 ready, OWASP dependency-check 12.2.2 runs via --no-configuration-cache, baseline-profile 1.4.1 verified, detekt 1.23.8 = the one open decision"
    - "The detekt-on-Gradle-9 decision is explicitly recorded: keep stable 1.23.8, accept its Gradle-9 deprecation warnings (tracked), defer the empirical run-vs-fail test to Phase 8; do NOT adopt detekt 2.0.0-alpha"
    - "Kotlin 2.2.20 / KSP 2.2.20-2.0.4 are recorded as already satisfying AGP-9's KGP >= 2.2.10 floor — no Kotlin/KSP bump needed"
    - "The AGP-8.7.3-incompatible-with-Gradle-9 finding and the fold-forward (wrapper flip becomes step 1 of Phase 8's atomic commit) are documented as the hand-off to Phase 8"
  artifacts:
    - path: "docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md"
      provides: "The Gradle-9 readiness/decision record: pinned target, plugin verdicts, detekt decision, KGP floor confirmation, and the Phase-8 fold-forward hand-off"
      min_lines: 50
      contains: "9.4.1"
  key_links:
    - from: "docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md"
      to: "gradle9-deprecations.log"
      via: "the detekt verdict cites the deprecation evidence captured in 07.1"
      pattern: "gradle9-deprecations"
    - from: "docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md"
      to: "Phase 8 plan"
      via: "documented hand-off: wrapper flip + sha256 fetch sequenced as Phase-8 commit 1"
      pattern: "Phase 8"
---

<objective>
Write the single authoritative Gradle-9 readiness/decision record for the Slopper toolchain — the KNOWLEDGE deliverable of the re-scoped Phase 7. It pins the Gradle 9.4.1 target, records each toolchain plugin's Gradle-9 verdict, makes and documents the one open decision (detekt 1.23.8 on Gradle 9), confirms the KGP floor, and documents the AGP-8.7.3 fold-forward hand-off to Phase 8.

Purpose: AGP9-01's "build runs green on Gradle 9" clause is unsatisfiable while AGP stays 8.7.3 (it hard-fails the Gradle-version check). Per the ROADMAP escape clause and the HIGH-confidence research finding, the wrapper ACTIVATION folds forward into Phase 8's atomic AGP-9 change-set. This phase therefore delivers AGP9-01 PARTIALLY: target pinned + deprecations swept (07.1) + plugin compatibility audited & decided + KGP floor confirmed + activation handed off. This ADR is the record of that partial delivery and the de-risking hand-off.

Output: `docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md`. This plan makes ZERO edits to live build config (no wrapper flip, no catalog bump).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/07-RESEARCH.md
@.planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log

<interfaces>
<!-- Exact values to record verbatim in the ADR. Do NOT apply them to live config. -->
Gradle target (PIN, do not flip the live wrapper this phase):
  distributionUrl=https\://services.gradle.org/distributions/gradle-9.4.1-bin.zip   (confirmed live, HTTP 307)
  distributionSha256Sum = FETCH AT PHASE-8 EXECUTION via:
    curl -sSL https://services.gradle.org/distributions/gradle-9.4.1-bin.zip.sha256
  Keep validateDistributionUrl=true, networkTimeout=10000 when Phase 8 applies it.

Current catalog facts (gradle/libs.versions.toml — verbatim, unchanged this phase):
  agp = "8.7.3"          (frozen — Phase 8 → 9.2.1)
  kotlin = "2.2.20"      (>= AGP-9 KGP floor 2.2.10 → satisfied, NO bump)
  ksp = "2.2.20-2.0.4"   (locked to Kotlin → NO bump)
  ktlint = "14.2.0"      (Gradle-9 ready ✓)
  detekt = "1.23.8"      (NOT officially Gradle-9; the open decision)
  dependencyCheck = "12.2.2"   (Gradle-9 via --no-configuration-cache ✓)
  baselineProfilePlugin = "1.4.1"  (verify; not exercised in Phase 7's frozen scope)

Plugin verdicts (from 07-RESEARCH.md Supporting stack table):
  ktlint 14.2.0          → Gradle-9 ready (13.1.0 added Gradle-9; 14.x current)
  OWASP dep-check 12.2.2 → runs on Gradle 9 ONLY via --no-configuration-cache (already the repo contract: build.gradle.kts:19)
  baseline-profile 1.4.1 → AGP-9 variant-API soft spot is a Phase-8 concern, not a Gradle-9 one; not exercised here
  detekt 1.23.8          → officially Gradle 8.12.1 only; first Gradle-9 line is 2.0.0-alpha.1 (forbidden by stable-only policy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Gradle-9 readiness ADR — pinned target, plugin verdicts, KGP floor, fold-forward hand-off</name>
  <read_first>
    - .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/07-RESEARCH.md (Summary, Standard Stack tables, Pattern 1 "Fold-Forward Decision", Open Questions — all source-of-truth values)
    - .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log (from 07.1 — cite the actual detekt/plugin deprecation lines, or the recorded no-finding, as evidence for the detekt verdict)
    - gradle/libs.versions.toml (confirm the verbatim current versions to record: agp 8.7.3, kotlin 2.2.20, ksp 2.2.20-2.0.4, ktlint 14.2.0, detekt 1.23.8, dependencyCheck 12.2.2, baselineProfilePlugin 1.4.1)
    - gradle/wrapper/gradle-wrapper.properties (confirm it is STILL 8.11.1 — the ADR records the target but does not flip it)
    - .planning/STATE.md (Key Locked Decisions: stable-only policy, no Kotlin/KSP bump, Gradle target 9.4.1)
  </read_first>
  <action>
    Create the directory `docs/adr/` (new — no ADR convention exists yet; this establishes one) and write `docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md`. Use a standard ADR structure (Status / Context / Decision / Consequences) and include these REQUIRED sections with these EXACT recorded facts:

    1. **Status:** Accepted — 2026-05-30 (Phase 7, milestone v1.1).

    2. **Context — the AGP-8.7.3 × Gradle-9 incompatibility:** State that AGP 8.7's compatibility table lists Gradle 8.9 as min/default with no Gradle-9 tier; Gradle 9 is tested only against AGP 9.0–9.2.x; AGP hard-fails the Gradle-version check at configuration start. Conclusion: a green build on Gradle 9.4.1 with AGP 8.7.3 is IMPOSSIBLE.

    3. **Pinned Gradle target:** Record the exact target block (do NOT apply it to the live wrapper):
       `distributionUrl=https\://services.gradle.org/distributions/gradle-9.4.1-bin.zip` (confirmed live, HTTP 307).
       `distributionSha256Sum` = "fetched at Phase-8 execution from `https://services.gradle.org/distributions/gradle-9.4.1-bin.zip.sha256` — never hard-coded from memory." Keep `validateDistributionUrl=true`. Note 9.4.1 is the exact AGP-9.2 Gradle floor; NOT 9.5.x (minimal-surface + stable-only policy).

    4. **Plugin Gradle-9 compatibility verdicts** (a markdown table with columns Plugin | Catalog version | Gradle-9 verdict | Action):
       - ktlint `org.jlleitschuh.gradle.ktlint` 14.2.0 → READY ✓ → no action.
       - OWASP dependency-check `org.owasp.dependencycheck` 12.2.2 → RUNS on Gradle 9 ONLY via `--no-configuration-cache` (already the repo contract at build.gradle.kts:19) → keep contract, no bump.
       - baseline-profile `androidx.baselineprofile` 1.4.1 → VERIFIED for Phase 7 (not exercised in the frozen scope; its AGP-9 variant-API soft spot is a Phase-8 concern) → no action this phase.
       - detekt `io.gitlab.arturbosch.detekt` 1.23.8 → THE OPEN DECISION (see section 5).

    5. **Decision — detekt on Gradle 9:** Record explicitly: KEEP stable detekt 1.23.8. It is officially Gradle-8.12.1-targeted; the only Gradle-9 line is 2.0.0-alpha.1 which the stable-only policy FORBIDS. detekt applies Kotlin/AGP as `compileOnly`, so it typically runs on a newer Gradle with deprecation warnings. Decision text to record verbatim: "Keep detekt 1.23.8 (stable). Accept its anticipated Gradle-9 deprecation warnings as a tracked item. Defer the empirical run-vs-fail determination to Phase 8's first green-on-Gradle-9 build (it is UNTESTABLE in Phase 7 because nothing configures on Gradle 9 until AGP 9 lands). Do NOT adopt detekt 2.0.0-alpha.x. Revisit a narrow stable-only-policy exception ONLY if detekt 1.23.8 hard-fails in Phase 8." Cite the 07.1 deprecation log evidence (the detekt-attributed lines, or the recorded absence of them) by referencing `gradle9-deprecations.log`.

    6. **KGP floor confirmation:** Record that Kotlin 2.2.20 and KSP 2.2.20-2.0.4 already satisfy AGP-9's KGP ≥ 2.2.10 floor (2.2.20 > 2.2.10) → NO Kotlin/KSP bump is needed; Phase 8 carries no Kotlin/KSP change.

    7. **Consequences / Hand-off to Phase 8 (the fold-forward):** Record that the `gradle-wrapper.properties` flip to 9.4.1 + the live sha256 fetch become STEP 1 of Phase 8's atomic AGP-9 change-set (Gradle → AGP 9.2.1 → drop kotlin.android → CommonExtension fix → compilerOptions → Hilt 2.59.2 → compileSdk 36). The "build runs green on Gradle 9" clause of AGP9-01 is therefore asserted at the PHASE-8 gate, not Phase 7. Phase 7 delivers AGP9-01 PARTIALLY: target pinned + deprecations swept + plugins audited + detekt decided + KGP floor confirmed.

    8. **Explicitly-rejected options:** Gradle 9.5.x (minimal-surface), detekt 2.0.0-alpha (stable-only), running AGP 8.7.3 on Gradle 9 for "one isolating commit" (disproven — hard-fails), and adding `android.enableLegacyVariantApi`/`android.newDsl=false` (forbidden AGP-9 crutches, irrelevant here).

    Do NOT edit gradle-wrapper.properties, libs.versions.toml, build.gradle.kts, or any CI workflow. This task writes ONLY the ADR. Do NOT bump the CI cache key agp8→agp9 (AGP is still 8.7.3).
  </action>
  <verify>
    <automated>test -f docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md && grep -q '9.4.1' docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md && grep -q 'gradle-9.4.1-bin.zip.sha256' docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md && grep -qi 'detekt' docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md && grep -q '2.2.10' docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md && grep -qi 'Phase 8' docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md</automated>
  </verify>
  <acceptance_criteria>
    - ADR exists: `test -f docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md`
    - Pinned target recorded: `grep -q 'gradle-9.4.1-bin.zip' docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md`
    - sha256 fetched-at-execution note (not a hard-coded hash): `grep -q 'gradle-9.4.1-bin.zip.sha256' docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md`
    - All four plugin verdicts present: `grep -qi 'ktlint' ... && grep -qi 'dependency' ... && grep -qi 'baseline' ... && grep -qi 'detekt' ...` each return a match
    - detekt decision keeps stable + rejects alpha: `grep -qi '1.23.8' <adr>` AND `grep -qi 'alpha' <adr>` (the rejection) both match
    - KGP floor recorded: `grep -q '2.2.10' <adr>` AND `grep -q '2.2.20' <adr>`
    - Fold-forward hand-off recorded: `grep -qi 'Phase 8' <adr>` AND `grep -qi 'fold' <adr>`
    - No live build config touched: `grep -q 'gradle-8.11.1-bin.zip' gradle/wrapper/gradle-wrapper.properties` AND `grep -q 'agp = "8.7.3"' gradle/libs.versions.toml`
  </acceptance_criteria>
  <done>`docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md` exists with all 8 required sections, the exact pinned 9.4.1 target (sha256 fetched-at-execution, not hard-coded), all four plugin verdicts, the verbatim detekt decision citing the 07.1 deprecation evidence, the KGP-floor confirmation, and the Phase-8 fold-forward hand-off. No live build config was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services.gradle.org → future wrapper (Phase 8) | The ADR pins the distribution URL + mandates fetching the official sha256 at Phase-8 execution; the integrity contract is RECORDED here, ENFORCED in Phase 8 |
| documentation → build config | The ADR is read-only knowledge; it touches no executable build surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-01 | Tampering | future Gradle 9.4.1 distribution integrity | mitigate | ADR mandates `distributionSha256Sum` be fetched from the official `.sha256` URL at Phase-8 execution (never a remembered hash) and `validateDistributionUrl=true` be kept — recording the supply-chain control before it is applied. |
| T-07.2-02 | Repudiation / Info-disclosure | OWASP dependency-check coverage continuity | mitigate | ADR records that the `--no-configuration-cache` dependency-check contract is preserved on Gradle 9 (no weakening of `failBuildOnCVSS=7.0`), making the requirement explicit for Phase 8 so the scan is not silently dropped during the toolchain churn. |
| T-07.2-03 | Elevation of privilege | detekt 2.0.0-alpha adoption pressure | accept | ADR explicitly REJECTS pulling in a pre-release plugin (unvetted supply-chain provenance) to "fix" a Gradle-9 warning, upholding the stable-only policy; risk of running stable 1.23.8 with warnings is accepted and tracked. |
</threat_model>

<verification>
- `docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md` exists with all 8 required sections.
- Pinned target (9.4.1 URL + fetched-at-execution sha256 note), all four plugin verdicts, the detekt decision, the KGP-floor confirmation, and the Phase-8 fold-forward hand-off are all present and grep-verifiable.
- No live build config edited: `gradle-wrapper.properties` still 8.11.1, `agp = "8.7.3"` still in the catalog, CI cache key unchanged.
</verification>

<success_criteria>
- The Gradle-9 readiness decision record is the single authoritative artifact pinning the target, recording plugin verdicts, making the detekt decision, confirming the KGP floor, and documenting the fold-forward hand-off.
- AGP9-01 is explicitly recorded as PARTIALLY delivered with the green-on-Gradle-9 activation handed to Phase 8.
- Repo stays green on Gradle 8.11.1 — no live build config touched.
</success_criteria>

<output>
After completion, create `.planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/07.2-SUMMARY.md`
</output>
