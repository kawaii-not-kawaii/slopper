---
phase: 07-gradle-9-core-version-bump-deprecation-sweep
plan: "07.1"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log
autonomous: true
requirements: [AGP9-01]
user_setup: []

must_haves:
  truths:
    - "Every Gradle-8→9 deprecation the current build emits is enumerated and attributed to its source plugin in gradle9-deprecations.log"
    - "The repo's own build scripts are confirmed free of Gradle-9-removed APIs (buildDir, Project.exec, getConvention, JavaPluginConvention, archivesBaseName)"
    - "The existing CI gate (compileDebugSources detekt ktlintCheck test) is still green on Gradle 8.11.1 — zero regression introduced by this phase"
    - "The live gradle-wrapper.properties still resolves Gradle 8.11.1 (NOT flipped to 9.4.1) at the end of this plan"
  artifacts:
    - path: ".planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log"
      provides: "The enumerated, plugin-attributed Gradle-9 deprecation inventory captured on the current toolchain"
      contains: "warning-mode"
  key_links:
    - from: "gradle9-deprecations.log"
      to: "07.2-PLAN.md detekt decision"
      via: "detekt-attributed deprecation lines feed the detekt-on-Gradle-9 governance decision"
      pattern: "detekt"
---

<objective>
Produce the Gradle-9 deprecation inventory for the Slopper build, captured on the CURRENT (green) toolchain — Gradle 8.11.1 + AGP 8.7.3 — and prove nothing regresses. This is the evidence-gathering half of the re-scoped Phase 7.

Purpose: AGP 8.7.3 hard-fails on Gradle 9.4.1 (HIGH-confidence research finding), so a green-on-Gradle-9 build is IMPOSSIBLE this phase and folds forward into Phase 8's atomic AGP-9 commit. What IS achievable now, AGP-safely, is enumerating every Gradle-8→9 deprecation via `--warning-mode=all` on the running 8.11.1 toolchain and attributing each to its source plugin. This de-risks Phase 8 by making the deprecation surface known before the wrapper flip.

Output: `gradle9-deprecations.log` (the enumeration artifact) + a confirmed no-regression baseline. This plan does NOT edit any live build config and does NOT flip the wrapper to 9.4.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/07-RESEARCH.md

<interfaces>
<!-- Current toolchain state the executor must NOT change in this plan. -->
gradle/wrapper/gradle-wrapper.properties (MUST stay 8.11.1 this plan):
  distributionUrl=https\://services.gradle.org/distributions/gradle-8.11.1-bin.zip
  distributionSha256Sum=f397b287023acdba1e9f6fc5ea72d22dd63669d59ed4a289a29b1a76eee151c6
  validateDistributionUrl=true

The established CI gate (from 07-RESEARCH.md Validation Architecture):
  ./gradlew compileDebugSources detekt ktlintCheck test
Fast smoke (convention-plugin-applies):
  ./gradlew :core:common:compileDebugKotlin

Build scripts already audited clean — only layout.buildDirectory is used
(build-logic/convention/.../AndroidComposeConventionPlugin.kt:27-28).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and attribute Gradle-9 deprecations on the current toolchain</name>
  <read_first>
    - .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/07-RESEARCH.md (Pattern 2 "Enumerate-on-the-old-Gradle-first" + Code Examples — exact commands)
    - gradle/wrapper/gradle-wrapper.properties (confirm it reads 8.11.1 BEFORE running — do NOT change it)
    - build.gradle.kts (root — the detekt/ktlint/dependency-check application surface that produces the warnings)
  </read_first>
  <action>
    Run the deprecation enumeration on the CURRENT Gradle 8.11.1 + AGP 8.7.3 toolchain (zero-risk — this is the existing green state). Write all output to the phase directory.

    Step 1 — main enumeration:
      ./gradlew help --warning-mode=all --stacktrace 2>&1 | tee .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log

    Step 2 — quality-task surface (where detekt/ktlint deprecations show), appended:
      ./gradlew detekt ktlintCheck --warning-mode=all --dry-run 2>&1 | tee -a .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log

    Step 3 — append an ATTRIBUTION section to the log. For each "Deprecated Gradle features were used" / "incompatible with Gradle 9.0" / "will fail with an error in Gradle 9" line, attribute it to its source plugin (expected sources per research: detekt 1.23.8 and/or OWASP dependency-check; ktlint 14.2.0 and the repo's own scripts are expected clean). If the build emits ZERO Gradle-9 deprecations, explicitly write the line "NO Gradle-9 deprecations emitted by the current build" into the log — an empty/absent finding is itself a recorded result, not a skipped task.

    Do NOT edit gradle-wrapper.properties, libs.versions.toml, or any build script. Do NOT pass `--no-configuration-cache` here unless a config-cache error blocks `help`; if it does, re-run with `--no-configuration-cache` and note in the log that the config-cache fallback was needed for enumeration.
  </action>
  <verify>
    <automated>test -s .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log && grep -q 'warning-mode\|Deprecated Gradle features\|NO Gradle-9 deprecations' .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log` exists and is non-empty: `test -s <log>`
    - Log contains the warning-mode run output OR an explicit no-finding line: `grep -E 'warning-mode|Deprecated Gradle features|NO Gradle-9 deprecations' <log>` returns a match
    - Log contains an attribution section header: `grep -i 'attribut' <log>` returns a match
    - Wrapper untouched: `grep -q 'gradle-8.11.1-bin.zip' gradle/wrapper/gradle-wrapper.properties`
  </acceptance_criteria>
  <done>gradle9-deprecations.log exists with the enumeration output (or an explicit no-deprecations result) plus a per-plugin attribution section, captured on Gradle 8.11.1 with the live wrapper unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm build scripts are Gradle-9-clean and prove no regression on 8.11.1</name>
  <read_first>
    - .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/07-RESEARCH.md (Runtime State Inventory "Verified clean" + Validation Architecture → Sampling Rate)
    - build-logic/convention/src/main/kotlin/io/stashapp/android/buildlogic/AndroidComposeConventionPlugin.kt (the one file using layout.buildDirectory — confirm it is the correct API, not buildDir)
    - .planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log (the log from Task 1 — append the clean-grep result here)
  </read_first>
  <action>
    Independently confirm the research's "build scripts are clean" claim with a grep audit, then prove the no-regression baseline.

    Step 1 — clean-API grep across all build scripts (exclude the correct replacement API from the match). Run from repo root:
      grep -rn -E 'buildDir|Project\.exec|getConvention|JavaPluginConvention|archivesBaseName' --include='*.kts' --include='*.kt' build-logic/ *.kts 2>/dev/null | grep -v 'layout.buildDirectory'
    Expected: ZERO matching lines (the repo uses `layout.buildDirectory`, the Gradle-9-correct API). Append the command and its result to gradle9-deprecations.log under an "## Own-Script API Audit" heading. If ANY match appears, record it as a Gradle-9 migration item in the log (do NOT fix it here — that is build-script surgery for Phase 8; flag it).

    Step 2 — no-regression baseline on the CURRENT toolchain. Run the established CI gate (still on Gradle 8.11.1):
      ./gradlew compileDebugSources detekt ktlintCheck test
    It MUST pass (this is the v1.0-shipped green state; Phase 7 must not regress it). For OWASP dependency-check, do NOT run it in the main gate (it needs `--no-configuration-cache` per build.gradle.kts:19 and is an out-of-band task) — it is out of scope for the regression gate.

    Append a "## No-Regression Baseline" section to gradle9-deprecations.log recording the exact gate command run and its PASS/FAIL outcome.
  </action>
  <verify>
    <automated>grep -rn -E 'buildDir|Project\.exec|getConvention|JavaPluginConvention|archivesBaseName' --include='*.kts' --include='*.kt' build-logic/ *.kts 2>/dev/null | grep -v 'layout.buildDirectory' | grep -vc '^#' | grep -q '^0$' && ./gradlew compileDebugSources detekt ktlintCheck test</automated>
  </verify>
  <acceptance_criteria>
    - Own-script API audit returns zero Gradle-9-removed-API usages: `grep -rn -E 'buildDir|Project\.exec|getConvention|JavaPluginConvention|archivesBaseName' --include='*.kts' --include='*.kt' build-logic/ *.kts | grep -v 'layout.buildDirectory'` prints nothing
    - The CI gate is green on 8.11.1: `./gradlew compileDebugSources detekt ktlintCheck test` exits 0
    - gradle9-deprecations.log gained both sections: `grep -q 'Own-Script API Audit' <log>` AND `grep -q 'No-Regression Baseline' <log>`
    - Live wrapper STILL on 8.11.1: `grep -q 'gradle-8.11.1-bin.zip' gradle/wrapper/gradle-wrapper.properties`
  </acceptance_criteria>
  <done>Build scripts confirmed free of Gradle-9-removed APIs (independently grep-verified), the CI gate is green on Gradle 8.11.1 (no regression), and both audit results are appended to gradle9-deprecations.log. No live build config was edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services.gradle.org → local wrapper | Gradle distribution download integrity (NOT exercised this plan — wrapper stays 8.11.1, no new dist fetched) |
| third-party Gradle plugins → build runtime | Plugin code runs at configuration time; enumeration surfaces (does not change) their behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Tampering | gradle-wrapper.properties distribution pin | accept | Plan does NOT alter the wrapper; existing `distributionSha256Sum` for 8.11.1 and `validateDistributionUrl=true` remain intact. No new distribution is fetched, so no new tampering surface is introduced. |
| T-07.1-02 | Information disclosure | gradle9-deprecations.log contents | accept | Log captures build-tooling deprecation text only — no secrets, keystore values, or PII. `keystore.properties` is not read by `help`/`detekt`/`ktlint` tasks. LOW/INFO. |
| T-07.1-03 | Denial of service | OWASP dependency-check coverage | mitigate | Regression gate intentionally excludes `dependencyCheckAnalyze` (out-of-band, `--no-configuration-cache` task) so the scan config is left UNTOUCHED and not weakened; its continued execution is asserted at the Phase-8/CI gate, not regressed here. |
</threat_model>

<verification>
- `.planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/gradle9-deprecations.log` exists, non-empty, with enumeration + attribution + own-script-audit + no-regression sections.
- Own-script API grep returns zero Gradle-9-removed-API usages.
- `./gradlew compileDebugSources detekt ktlintCheck test` is green on Gradle 8.11.1.
- `gradle/wrapper/gradle-wrapper.properties` STILL points at `gradle-8.11.1-bin.zip` (the wrapper flip is deferred to Phase 8 — this plan must not perform it).
</verification>

<success_criteria>
- The Gradle-9 deprecation surface is enumerated and plugin-attributed (or recorded as empty) on the current green toolchain.
- The repo's own build scripts are independently confirmed Gradle-9-clean.
- Zero regression: the established CI gate still passes on Gradle 8.11.1.
- No live build config edited; wrapper not flipped to 9.4.1.
</success_criteria>

<output>
After completion, create `.planning/phases/07-gradle-9-core-version-bump-deprecation-sweep/07.1-SUMMARY.md`
</output>
