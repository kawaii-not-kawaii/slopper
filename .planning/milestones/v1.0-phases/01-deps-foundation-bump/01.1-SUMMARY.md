---
phase: 01-deps-foundation-bump
plan: 01.1
status: HALTED — stop_phase triggered after DEPS-01+02; AGP 9 / Hilt incompatibility blocks DEPS-03+04
type: execute
wave: 1
halted_at: 2026-05-16T20:15+09:00
halted_during: DEPS-03+04 atomic commit attempt (post-bump verification)
halted_reason: AGP 9.2.0 requires Hilt Gradle plugin with AGP 9 support; latest published Hilt (2.56.2 on Maven Central) does not provide it. Plan referenced Hilt 2.57.1 which does not yet exist publicly. Lockstep cannot complete without a working DI plugin on AGP 9.
on_failure_directive: stop_phase (CONTEXT.md Decision 3 — lockstep chain)
completed_requirements: [DEPS-01, DEPS-02]
attempted_requirements: [DEPS-03, DEPS-04, DEPS-05]
commits_landed_this_run:
  - cf5f1f0 fix(build): add missing :core:data dep to :core:ui (precondition)
  - 5f924be fix(build): remove duplicate parsePreset() (precondition)
  - 18d545f fix(build): remove orphan AndroidView block before package decl (precondition)
  - 76bb19e fix(build): remove orphan PlayerGestureSettings composable (precondition)
  - 214c94f fix(build): remove orphan brace pair in AppNavHost (precondition)
  - 906495e chore(deps): DEPS-01 — establish pre-bump lint baseline
  - 0523a94 chore(deps): DEPS-02 — pin JDK 17 toolchain and disable auto-download
deps_commits_landed: [DEPS-01, DEPS-02]
stashed_attempt: "stash@{0} — wave-1-attempt: AGP9+Kotlin2.2.20+Hilt-staged (BLOCKED on Hilt AGP9 compat)"
---

# Phase 1 Plan 01.1 — HALT REPORT (DEPS-03+04 blocked on Hilt AGP-9 incompatibility)

## TL;DR

DEPS-01 and DEPS-02 landed. DEPS-03+04 (Gradle 9.4.1 + AGP 9.2.0 + compileSdk 36, atomic) cannot complete because **no published Hilt Gradle plugin version supports AGP 9 yet**. Plan target was "Hilt 2.57.1" but the latest version on Maven Central is `2.56.2`, and `2.56.2` fails at plugin-apply time with `IllegalStateException: Android BaseExtension not found` against AGP 9.2.0.

This is a planning-vs-reality gap, not an executor failure. Per CONTEXT.md Decision 3 (`on_failure: stop_phase` on the lockstep chain), halting rather than guessing.

## Environment (PASS — orchestrator-provisioned)

- JDK 17.0.17 (apt openjdk-17-jdk)
- Android SDK at `~/Android/Sdk` with platforms 35/36, build-tools 35.0.0/36.0.0
- Gradle wrapper 8.11.1 (bootstrap.sh)
- Stash GraphQL schema vendored from `stashapp/stash@develop` for Apollo codegen
- `./gradlew :app:assembleDebug` was green at commit `214c94f` (pre-DEPS-03 baseline)

## Precondition fixes (orchestrator + first executor attempts)

Pre-existing corruption from restore commit `bf01b34` blocked all Gradle invocation. Iteratively surfaced and fixed:

1. `3b16767 fix(build): remove orphan dependency block + restore Properties import in app/build.gradle.kts` — duplicate `implementation(project(":feature:settings"))` block with `import java.util.Properties` mashed onto its last line.
2. `3783973 fix(build): vendor Stash GraphQL schema for Apollo codegen` — `core/network` declared `service("stash")` but no schema file in branch history; concatenated upstream Stash schema into 4916-line `schema.graphqls`.
3. `cf5f1f0 fix(build): add missing :core:data dep to :core:ui` — `:core:ui` needed `:core:data` for UiPreferences Hilt resolution.
4. `5f924be fix(build): remove duplicate parsePreset()` — duplicate function in LibraryViewModel.
5. `18d545f fix(build): remove orphan AndroidView block before package decl in PlayerScreen.kt` — same corruption class as `3b16767`.
6. `76bb19e fix(build): remove orphan PlayerGestureSettings composable` — dead composable referencing nonexistent VM members.
7. `214c94f fix(build): remove orphan brace pair in AppNavHost` — 2 stray closing braces with comments copied from `StashAppContent`'s scope.

## DEPS commits landed

- `906495e chore(deps): DEPS-01 — establish pre-bump lint baseline` ✓
- `0523a94 chore(deps): DEPS-02 — pin JDK 17 toolchain and disable auto-download` ✓

## DEPS-03+04 attempt — staged in stash

Edits made and stashed at `stash@{0}` for recovery:

- `gradle/libs.versions.toml` → agp 9.2.0, kotlin 2.2.20, ksp 2.2.20-2.0.4, hilt 2.56.2 (downgraded from plan's hypothetical 2.57.1 to actual latest)
- `gradle/wrapper/gradle-wrapper.properties` → Gradle 9.4.1 + `distributionSha256Sum=2ab2958f2a1e51120c326cad6f385153bb11ee93b3c216c5fccebfdfbb7ec6cb` (verified against official SHA)
- `build-logic/convention/.../KotlinAndroid.kt` → AGP 9 API migration:
  - `CommonExtension<*, *, *, *, *, *>` → `CommonExtension` (generics removed in AGP 9.2)
  - DSL lambda blocks (`compileOptions { ... }`, `defaultConfig { ... }`) → property access (Action-taking overloads removed in AGP 9.2)
  - Dropped deprecated `-Xcontext-receivers` and `-Xskip-metadata-version-check`
- `build-logic/convention/.../AndroidApplicationConventionPlugin.kt` → removed `apply("org.jetbrains.kotlin.android")` (AGP 9 has built-in Kotlin support; manual apply → `Cannot add extension with name 'kotlin'` conflict per `issuetracker.google.com/438678642`)
- `build-logic/convention/.../AndroidLibraryConventionPlugin.kt` → same `kotlin.android` removal + dropped `defaultConfig.targetSdk = 36` (AGP 9 removed `targetSdk` from `LibraryExtension.defaultConfig`)
- `baselineprofile/build.gradle.kts` → compileSdk/targetSdk 36

### Failure mode

`./gradlew wrapper --gradle-version 9.4.1 --distribution-type bin`:

```
> Failed to apply plugin 'com.google.dagger.hilt.android'.
   > Android BaseExtension not found.
Caused by: java.lang.IllegalStateException: Android BaseExtension not found.
```

Tried Hilt 2.57.1 (plan target) — does not exist on Maven Central. Latest published: `2.56.2`. Tried 2.56.2 — same failure (uses the `BaseExtension` API that AGP 9 removed).

## Plan-vs-reality gaps surfaced

| Plan assumption | Reality |
|---|---|
| Hilt 2.57.1 exists | Latest on Maven Central is 2.56.2 (May 2026) |
| Hilt 2.56.x supports AGP 9 | Fails at plugin-apply on AGP 9.2.0 — relies on removed `BaseExtension` |
| DEPS-03+04 is atomic | Practical lockstep requires DEPS-03+04+05+08 atomic (Gradle 9 ↔ AGP 9 ↔ Kotlin 2.2.20 ↔ Hilt-with-AGP-9-support) |
| AGP 8→9 is "minor bump, no breaking changes" (CONTEXT.md L55) | AGP 9 removed `CommonExtension` generics, Action-taking DSL overloads, `LibraryExtension.defaultConfig.targetSdk`, and made `org.jetbrains.kotlin.android` plugin both unnecessary AND incompatible to apply |
| Wave 1 and Wave 2 are independent | Hilt (Wave 2 DEPS-08) is on the Wave 1 critical path because AGP 9 demands AGP-9-aware DI |

## Must-haves status (plan frontmatter)

1. ✓ "app/lint-baseline.xml committed before any version bump" → `906495e`
2. ✗ "`./gradlew -q javaToolchains` resolves exactly one JDK 17" → not reverified post-bump
3. ✗ "Gradle wrapper 9.4.1 with SHA pin, config cache reuses" → wrapper-properties bump staged, regeneration blocked
4. ✗ "AGP 9.2.0 and compileSdk/targetSdk 36 in all four touchpoints" → staged in stash, not committed
5. ✗ "Kotlin 2.2.20, KSP 2.2.20-2.0.4, two deprecated `-X` flags gone" → staged in stash, not committed
6. ✗ "Compose BOM 2026.05.00 + `compileReleaseKotlin` deprecation-clean" → not attempted

## Recommended next actions

**A. Re-scope Phase 1 to drop AGP 9.** Keep Gradle on 8.x (or bump to 8.12). Land DEPS-05 (Kotlin 2.2.20 — works on AGP 8 too) and DEPS-06 (Compose BOM 2026.05.00). Defer AGP 9 + compileSdk 36 to a later phase once Hilt publishes AGP-9 support. Likely the cleanest unblock.

**B. Wait on upstream Hilt.** Monitor `dagger` releases; resume Wave 1 when a Hilt with AGP 9 support ships (likely 2.57.x or later).

**C. Switch DI.** Replace Hilt with KSP-only Dagger or another DI framework that supports AGP 9. Scope-changing.

**D. Pin AGP to a transitional release.** Some AGP 9.x preview builds may still expose the legacy `BaseExtension` for Hilt; probe required.

The stashed edits (`stash@{0}`) preserve the AGP-9 migration work for whichever path resumes.

## State invariants

- Branch `phase-1/deps-bump` HEAD: `0523a94`
- Working tree clean (standard untracked: `.gradle/`, `build/`, `AGENTS.md`, `UI_HANDOFF.md`, regenerated `gradlew*` from bootstrap)
- Stash count: 1 (recoverable Wave 1 attempt)
- SPEC-LAYER step 5: still `pending` (correct — execute did not complete)
