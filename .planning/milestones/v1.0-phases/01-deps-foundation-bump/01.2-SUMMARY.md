---
phase: 01-deps-foundation-bump
plan: 01.2
status: complete (with one prior revert from a previous executor session)
type: execute
wave: 2
depends_on: [01.1]
on_failure: revert_and_continue
completed_requirements: [DEPS-09, DEPS-10, DEPS-17]
reverted_requirements: [DEPS-07]
commits_landed_this_run:
  - 0e82c02 chore(deps): DEPS-09 — bump Apollo to 4.4.3, retain OkHttp 4.12.0
  - db63bd7 chore(deps): DEPS-10 — retain Media3 1.9.1 (CASE B forced; 1.10 deferred with AGP 9)
  - 3bf3bbf docs(req): DEPS-17 — defer DEPS-03 + DEPS-04 + Media3 1.10 upgrade path (Phase 1 trim)
commits_landed_prior_session:
  - 0f725dc docs(req): defer DEPS-07 — AndroidX sweep requires AGP 8.9.1+/compileSdk 36 (blocked by DEPS-04)
files_modified:
  - gradle/libs.versions.toml
  - .planning/REQUIREMENTS.md
branch: phase-1/deps-bump
head: 3bf3bbf
build_status: assembleDebug BUILD SUCCESSFUL
---

# Phase 1 Plan 01.2 — Trimmed library sweep landed (3/4 forward + 1 prior-session deferral)

## TL;DR

Trimmed library sweep complete on `phase-1/deps-bump`. **DEPS-09** (Apollo 4.4.3) and **DEPS-10** (Media3 1.9.1 retained, CASE B forced) landed cleanly. **DEPS-17** logged the AGP-9 / compileSdk-36 / Media3-1.10 deferrals into `REQUIREMENTS.md ## Deferred to Future Milestones`. **DEPS-07** (AndroidX sweep) was already reverted-with-defer in a prior executor session (`0f725dc`) — picked up that state and continued per `on_failure: revert_and_continue`.

`./gradlew --configuration-cache assembleDebug` is green at HEAD `3bf3bbf`.

## Per-requirement outcomes

| Req     | Outcome              | Commit    | Notes                                                                 |
|---------|----------------------|-----------|-----------------------------------------------------------------------|
| DEPS-07 | reverted-deferred    | `0f725dc` | Landed in prior session before this run. AndroidX bumps require AGP 8.9.1+/compileSdk 36 (blocked by DEPS-04). Catalog values unchanged from HEAD `f06ff0c`. |
| DEPS-08 | n/a (not in plan)    | —         | Subsumed by `be3e730` (Kotlin 2.2.20 + KSP + Hilt 2.56.2) in plan 01.1.|
| DEPS-09 | landed               | `0e82c02` | Apollo 4.1.0 → 4.4.3; OkHttp retained at 4.12.0; deferral comment block inserted above the okhttp line. |
| DEPS-10 | landed (CASE B forced) | `db63bd7` | Media3 1.9.1 + nextlibMedia3Ext 1.9.1-0.11.0 unchanged. Comment block refreshed to record CASE B forcing (dated 2026-05-16). No Maven Central probe was performed — CASE B was the forced disposition per the plan. |
| DEPS-17 | landed               | `3bf3bbf` | New `### Phase 1 trim (DEPS-17, auto-logged 2026-05-16)` block inserted under `## Deferred to Future Milestones`. Three bullets: DEPS-03, DEPS-04, DEPS-10 (1.10.0 upgrade path). |

## Media3 disposition (DEPS-10)

**CASE B FORCED — not probed.** The plan explicitly disallowed Maven-Central probing for Media3 1.10 because the 1.10 upgrade path requires `compileSdk = 36`, which is deferred along with AGP 9 (Option A unblock from `01.1-SUMMARY.md`).

Catalog comment that landed (`gradle/libs.versions.toml`, above the `media3 = "1.9.1"` line):

```toml
# Media3 — kept in sync with nextlib for the FFmpeg extension.
# CASE B (forced 2026-05-16, Phase 1 DEPS, Option A unblock from 01.1-SUMMARY.md):
# Media3 1.10.0 is deferred along with AGP 9 + compileSdk 36 (DEPS-17 in REQUIREMENTS.md).
# Revisit once Hilt publishes AGP-9 support and the AGP-9 transition phase ships.
```

Values unchanged: `media3 = "1.9.1"`, `nextlibMedia3Ext = "1.9.1-0.11.0"`.

## DEPS-17 disposition

Inserted block (verbatim) in `.planning/REQUIREMENTS.md` under `## Deferred to Future Milestones`, at line 65:

```markdown
### Phase 1 trim (DEPS-17, auto-logged 2026-05-16)

Recorded via plan 01.2 Task 4. Source of truth for the why: `.planning/phases/01-deps-foundation-bump/01.1-SUMMARY.md` (Option A unblock). Phase 1 stays on Gradle 8.11.1 / AGP 8.7.3 / compileSdk 35 because no published Hilt supports AGP 9 yet (latest is 2.56.2; the plan referenced a nonexistent 2.57.1).

- **DEPS-03** — Gradle 9.4.1 — blocked on Hilt AGP-9 support. Revisit once Dagger publishes a Hilt release that supports AGP 9.
- **DEPS-04** — AGP 9.2.0 + compileSdk/targetSdk 36 — same blocker. AGP 9 also broke `CommonExtension` generics, Action-taking DSL overloads, `LibraryExtension.targetSdk`, and the `org.jetbrains.kotlin.android` plugin; the migration deserves a dedicated phase.
- **DEPS-10 (1.10.0 upgrade path)** — Media3 1.10.0 + nextlibMedia3Ext 1.10.0-0.12.1 — requires compileSdk 36 (blocked by DEPS-04). Phase 1 keeps Media3 1.9.1 / nextlibMedia3Ext 1.9.1-0.11.0 (CASE B forced).
```

Resulting commit SHA: `3bf3bbf`.

The new block lands above the pre-existing `### Bump deferral (auto-logged 2026-05-16)` subsection (which holds the DEPS-07 entry from `0f725dc`) — natural top of the deferred-list per the plan's guardrail.

## Must-haves status (frontmatter `must_haves.truths`)

| # | Truth                                                                                       | PASS / FAIL | Evidence                                                                                                                  |
|---|---------------------------------------------------------------------------------------------|-------------|---------------------------------------------------------------------------------------------------------------------------|
| 1 | AndroidX (Lifecycle/Activity/Nav/DataStore/Coil) hit targets **OR** `docs(req): defer DEPS-07` landed | PASS        | `docs(req): defer DEPS-07` exists at `0f725dc` (prior session, on this branch). `git log --format=%s phase-1/deps-bump \| grep 'docs(req): defer DEPS-07'` matches. Catalog values unchanged. |
| 2 | Apollo resolves to 4.4.3 from a single source; OkHttp 4.12.0 from a single source; inline deferral comment present | PASS        | `dependencyInsight --configuration releaseRuntimeClasspath` → `com.apollographql.apollo:apollo-runtime:4.4.3` and `com.squareup.okhttp3:okhttp:4.12.0` (single resolution chain). Comment block present above `okhttp =` line containing `Apollo 5 (May 2026) split`. |
| 3 | Media3 stays on 1.9.1 + nextlibMedia3Ext 1.9.1-0.11.0; dated catalog comment recording the deferral | PASS        | `grep -E '^media3 = "1\.9\.1"'` and `grep -E '^nextlibMedia3Ext = "1\.9\.1-0\.11\.0"'` both match. Comment contains `CASE B (forced 2026-05-16` and `Option A unblock`.            |
| 4 | REQUIREMENTS.md `## Deferred to Future Milestones` records DEPS-03 / DEPS-04 / Media3 1.10 upgrade path as a single DEPS-17 entry | PASS        | `### Phase 1 trim (DEPS-17, auto-logged 2026-05-16)` heading at line 65; three bullets at lines 69–71 covering DEPS-03, DEPS-04, DEPS-10 (1.10.0 upgrade path). |
| 5 | Any reverted requirement has a concrete row in REQUIREMENTS.md and a corresponding `docs(req):` commit | PASS        | DEPS-07 row exists at line 73–74 (`### Bump deferral (auto-logged 2026-05-16)` → `**DEPS-07**: Lifecycle 2.10.0 ...`). Backing commit: `0f725dc docs(req): defer DEPS-07`.       |

End-of-plan build gate: `./gradlew --configuration-cache assembleDebug` → **BUILD SUCCESSFUL** (configuration cache reused) at HEAD `3bf3bbf`.

## Failures & Reverts

| Req ID  | Attempted version                                  | Failure mode                                                                                  | Resolution                       | Backlog commit SHA |
|---------|----------------------------------------------------|-----------------------------------------------------------------------------------------------|----------------------------------|--------------------|
| DEPS-07 | Lifecycle 2.10.0 / Activity Compose 1.13.0 / Nav 2.9.6 / DataStore 1.2.1 / Coil 3.4.0 | Requires AGP 8.9.1+ / compileSdk 36 (blocked by DEPS-04 deferral; Phase 1 stays on AGP 8.7.3 / compileSdk 35) | uncommitted-discard (prior session) | `0f725dc`          |

(One revert recorded — DEPS-07 from prior executor session. DEPS-09, DEPS-10, DEPS-17 all landed without revert in this run.)

## Commit list (this plan, in execution order)

| Order | SHA       | Subject                                                                                       |
|-------|-----------|-----------------------------------------------------------------------------------------------|
| 1     | `0f725dc` | `docs(req): defer DEPS-07 — AndroidX sweep requires AGP 8.9.1+/compileSdk 36 (blocked by DEPS-04)` *(prior session — included for completeness)* |
| 2     | `0e82c02` | `chore(deps): DEPS-09 — bump Apollo to 4.4.3, retain OkHttp 4.12.0`                            |
| 3     | `db63bd7` | `chore(deps): DEPS-10 — retain Media3 1.9.1 (CASE B forced; 1.10 deferred with AGP 9)`         |
| 4     | `3bf3bbf` | `docs(req): DEPS-17 — defer DEPS-03 + DEPS-04 + Media3 1.10 upgrade path (Phase 1 trim)`       |

Commit count audit (per plan `<verification>`):
- `chore(deps): DEPS-(07|09|10)` → 2 (DEPS-09, DEPS-10). DEPS-07 produced a `docs(req): defer` instead — within the 0..3 allowed range.
- `docs(req): DEPS-17` → exactly 1 (`3bf3bbf`).
- `docs(req): defer DEPS-` → 1 (DEPS-07).

## State at plan close

- Branch: `phase-1/deps-bump`
- HEAD: `3bf3bbf`
- Tracked working tree: clean
- Untracked (documented allow-list): `.gradle/`, `AGENTS.md`, `UI_HANDOFF.md`, regenerated `gradlew*`, `gradle/wrapper/gradle-wrapper.jar`, per-module `build/` outputs
- Build gate: `./gradlew --configuration-cache assembleDebug` → BUILD SUCCESSFUL
- DEPS counters landed in Phase 1: DEPS-01, DEPS-02, DEPS-05, DEPS-06, DEPS-08 (subsumed in DEPS-05 commit), DEPS-09, DEPS-10, DEPS-17
- DEPS counters deferred (logged in REQUIREMENTS.md `## Deferred to Future Milestones`): DEPS-03, DEPS-04, DEPS-07, DEPS-10 (1.10.0 upgrade path)

## Self-Check: PASSED

Verified post-write:
- File exists: `/home/yun/slopper/.planning/phases/01-deps-foundation-bump/01.2-SUMMARY.md`
- Commits all reachable on `phase-1/deps-bump`: `0e82c02`, `db63bd7`, `3bf3bbf`, and prior `0f725dc`
- Catalog assertions: `apollo = "4.4.3"`, `okhttp = "4.12.0"`, `media3 = "1.9.1"`, `nextlibMedia3Ext = "1.9.1-0.11.0"`, `CASE B (forced` and `Option A unblock` strings present
- REQUIREMENTS.md `### Phase 1 trim (DEPS-17` heading + three target bullets present
- Final `assembleDebug` BUILD SUCCESSFUL
