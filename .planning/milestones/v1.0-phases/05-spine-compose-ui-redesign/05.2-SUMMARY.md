---
phase: 05-spine-compose-ui-redesign
plan: 2
subsystem: ui
tags: [android, compose, material3, spine-design, jetbrains-mono, space-grotesk]

# Dependency graph
requires:
  - phase: 05.1
    provides: SpineColors, MetaMono, MonoSmall, ShapeSmall/Medium/Large, SpineResumeCard composable, StashColors→SpineColors migration in detail/browse

provides:
  - HomeScreen with Spine inline Row top bar (logo diamond, server badge, search/refresh icons)
  - HomeScreen SpineResumeCard slot above rails (gated on if(false) placeholder per plan)
  - LibraryScreen always-visible search field with MonoSmall placeholder and MetaMono ⌘K hint
  - LibraryScreen filter chip row with AccentPrimary active tint
  - LibraryScreen results count row (MetaMono, OnSurfaceMuted)
  - FilterSheet Spine redesign — containerColor=Bg, 84% height, AccentPrimary chip colors, no Save view button
  - DetailScreen card-style hero (8dp/18dp padding, 16:10 aspect, ShapeMedium clip/border)
  - DetailScreen title block, full-width AccentPrimary CTA, 2-col non-lazy metadata grid
  - DetailScreen cast rows (Surface ShapeSmall, 36dp circle avatar), chapter rows, tags FlowRow
  - BrowseScreen 4-tab SpineSegmentedControl (not M3 TabRow)
  - BrowseScreen studios 2-col grid with AccentCool scene count labels
  - BrowseScreen performers list with 44dp circle avatars
  - BrowseScreen tags grid with AccentCool text

affects: [05.3, Phase 5 summary, any future plan touching these four feature screens]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Card-style hero with padding + aspectRatio + clip + border (DetailScreen)"
    - "Non-lazy 2-col metadata grid via chunked rows (avoids nested scroll issue T-05-10)"
    - "SpineSegmentedControl: Row with background/border/padding(3dp) + per-tab weight(1f) + conditional Modifier chain"
    - "Filter chip active state via FilterChipDefaults.filterChipColors(selectedContainerColor=AccentPrimary.copy(0.12f))"
    - "Inline top bar as LazyColumn item (key='top_bar') — not Scaffold topBar"

key-files:
  created: []
  modified:
    - feature/home/src/main/java/io/stashapp/android/feature/home/HomeScreen.kt
    - feature/library/src/main/java/io/stashapp/android/feature/library/LibraryScreen.kt
    - feature/library/src/main/java/io/stashapp/android/feature/library/FilterSheet.kt
    - feature/detail/src/main/java/io/stashapp/android/feature/detail/DetailScreen.kt
    - feature/browse/src/main/java/io/stashapp/android/feature/browse/BrowseScreen.kt

key-decisions:
  - "SpineResumeCard slot uses if(false) gate per plan — HomeUiState has no resume field yet; future plan wires it"
  - "Non-lazy 2-col metadata grid (chunked Row/Column) used instead of LazyVerticalGrid to avoid nested scroll crash (T-05-10)"
  - "FilterSheet onSaveAsDefault parameter retained in signature for backward compat; Save view button removed from UI only (D-01 Q4)"
  - "organizedCount in LibraryScreen hardcoded to 0 per plan — LibraryUiState field not yet defined"
  - "Markers tab in BrowseScreen shows 'coming soon' placeholder — data source not implemented"

patterns-established:
  - "Spine inline top bar: LazyColumn item(key='top_bar') containing SpineTopBar composable — not Scaffold topBar slot"
  - "SpineSegmentedControl: reusable private composable pattern for 4-tab navigation without M3 TabRow"
  - "MetadataGrid: non-lazy chunked Row approach for 2-col key/value with hairline border via SpineColors.Border background"

requirements-completed: [SPINE-05, SPINE-06, SPINE-07, SPINE-08]

# Metrics
duration: pre-committed (continuation agent — commits from prior session)
completed: 2026-05-19
---

# Phase 5 Plan 2: HomeScreen + LibraryScreen + DetailScreen + BrowseScreen Spine Redesign Summary

**Full Spine redesign of four Group-1 feature screens: card-style hero with 16:10 aspect, custom 4-tab segmented control replacing M3 TabRow, inline Row top bar with AccentPrimary diamond logo, and AccentCool studio/tag labels throughout**

## Performance

- **Duration:** pre-committed (work completed in prior session; this agent is verification + summary)
- **Started:** (prior session)
- **Completed:** 2026-05-19
- **Tasks:** 2 (SPINE-05/06 + SPINE-07/08)
- **Files modified:** 5

## Accomplishments

- HomeScreen redesigned with Spine inline Row top bar (rotated-diamond logo, server badge, 32dp search/refresh icons) and SpineResumeCard slot above rails
- LibraryScreen redesigned with always-visible search field (MonoSmall placeholder, MetaMono ⌘K hint), AccentPrimary filter chip row, results count row, and FilterSheet Spine styling with no Save view button
- DetailScreen redesigned with card-style hero (padding 8dp/18dp, 16:10 AspectRatio, ShapeMedium), title block with AccentPrimary studio label, full-width CTA, non-lazy 2-col metadata grid, cast/chapter rows, and tags FlowRow
- BrowseScreen redesigned with custom SpineSegmentedControl (Studios/Performers/Tags/Markers), studios 2-col grid with AccentCool counts, performers list with 44dp circle avatars, and tags grid with AccentCool text

## Task Commits

1. **Task 1: HomeScreen Spine redesign (SPINE-05)** — `b2cf1b0` (feat)
2. **Task 1: LibraryScreen + FilterSheet Spine redesign (SPINE-06)** — `0ce60f7` (feat)
3. **Task 2: DetailScreen Spine redesign (SPINE-07)** — `a35e64a` (feat)
4. **Task 2: BrowseScreen Spine redesign (SPINE-08)** — `c222f6c` (feat)

## Files Created/Modified

- `feature/home/src/main/java/io/stashapp/android/feature/home/HomeScreen.kt` — Spine inline top bar, SpineResumeCard slot, Spine rail headers, LazyRow spacedBy(10.dp)
- `feature/library/src/main/java/io/stashapp/android/feature/library/LibraryScreen.kt` — Always-visible search field, filter chip row with AccentPrimary active state, results count row, GridCells.Adaptive(180.dp)
- `feature/library/src/main/java/io/stashapp/android/feature/library/FilterSheet.kt` — containerColor=SpineColors.Bg, fillMaxHeight(0.84f), AccentPrimary chip colors, Reset+Apply footer (no Save view)
- `feature/detail/src/main/java/io/stashapp/android/feature/detail/DetailScreen.kt` — Card-style hero, title block, CTA button, MetadataGrid, PerformerRow, ChapterRow, TagFlow
- `feature/browse/src/main/java/io/stashapp/android/feature/browse/BrowseScreen.kt` — SpineSegmentedControl, StudiosGrid, PerformersList, TagsGrid (AccentCool throughout)

## Decisions Made

- Used `if (false)` gate for SpineResumeCard instead of checking UiState field — HomeUiState has no resume field yet; plan explicitly required this approach to avoid adding data flow
- Used non-lazy chunked Row/Column for MetadataGrid (not LazyVerticalGrid) to prevent nested scroll crash (T-05-10 threat mitigation)
- Kept `onSaveAsDefault` parameter in FilterSheet signature but removed button from rendered UI (D-01 Q4 locked decision)
- `organizedCount = 0` placeholder in LibraryScreen — LibraryUiState has no organized count field yet
- Markers tab shows "Markers coming soon" placeholder — no data source exists for this tab

## Deviations from Plan

None — plan executed exactly as written. All intentional stubs and placeholder conditions were specified in plan instructions.

## Known Stubs

| Stub | File | Line | Reason |
|------|------|------|--------|
| `if (false)` SpineResumeCard gate | HomeScreen.kt | 91 | HomeUiState has no resumeScene field — plan explicitly required this approach |
| `serverHost = ""` | HomeScreen.kt | 138 | HomeUiState has no server field — plan says "use empty string for now" |
| `onSearchClick = { /* TODO */ }` | HomeScreen.kt | 84 | No existing search action in HomeScreen to wire to |
| `organizedCount = 0` | LibraryScreen.kt | 223 | LibraryUiState has no organized count field — plan says "use 0 as placeholder" |
| Markers tab placeholder | BrowseScreen.kt | 122–133 | No data source for markers; plan explicitly allows placeholder |

These stubs do not prevent plan goals — all four screens are fully styled per Spine design. Data wiring for resume/search/organizedCount is deferred to future plans.

## Compile Gate Results

```
./gradlew :feature:home:assembleDebug :feature:library:assembleDebug :feature:detail:assembleDebug :feature:browse:assembleDebug --no-daemon
BUILD SUCCESSFUL in 8s
166 actionable tasks: 166 up-to-date
```

All four feature modules assemble successfully.

## ImmutableList Preservation (T-05-07)

- HomeScreen.kt: `ImmutableList<SceneSummary>` preserved in `RailScenes` composable
- DetailScreen.kt: No ImmutableList was in scope (uses `List<PerformerRef>`, `List<TagRef>`, `List<Marker>` from SceneDetail model — unchanged)
- BrowseScreen.kt: Uses Paging3 `LazyPagingItems` (not ImmutableList) — unchanged

## FilterSheet Save View Confirmation

No rendered "Save view" button in FilterSheet. The `onSaveAsDefault` parameter remains in the function signature for backward compatibility but is not called from any UI element in the rendered layout. Footer contains only Reset (TextButton, ghost) and Apply (AccentPrimary filled Button).

Grep result: `Save view\|SaveView\|saveView` — 0 hits in rendered UI (comment and call-site pass-through excluded).

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary changes introduced. All changes are UI-only (Compose layout and color token updates).

## Self-Check: PASSED

- `feature/home/.../HomeScreen.kt` — FOUND
- `feature/library/.../LibraryScreen.kt` — FOUND
- `feature/library/.../FilterSheet.kt` — FOUND
- `feature/detail/.../DetailScreen.kt` — FOUND
- `feature/browse/.../BrowseScreen.kt` — FOUND
- Commit `b2cf1b0` (SPINE-05) — FOUND
- Commit `0ce60f7` (SPINE-06) — FOUND
- Commit `a35e64a` (SPINE-07) — FOUND
- Commit `c222f6c` (SPINE-08) — FOUND
- Build: SUCCESSFUL

## Next Phase Readiness

- All Group-1 screens (HomeScreen, LibraryScreen, DetailScreen, BrowseScreen) carry the Spine design language
- Plan 5.3 (SettingsScreen, ConnectionScreen, PlayerScreen, NavCustomizeSheet) runs in parallel — no file overlap
- Future plan should wire HomeUiState.resumeScene to the SpineResumeCard `if (false)` gate
- Future plan should add `organizedCount` to LibraryUiState and wire to the results count row

---
*Phase: 05-spine-compose-ui-redesign*
*Completed: 2026-05-19*
