# Plan 02.1 — Deviations Log

## DEV-01 — Task 1 (COMPLY-04): `appReady` AtomicBoolean threaded into `StashAppContent` rather than collected in setContent { }

**Original plan instruction (Task 1, Step 5):**
> Inside the existing `setContent { StashTheme { ... } }` block, BEFORE the call to the existing root content composable (StashAppContent or equivalent — read the file to identify the exact location), add the gate-flip pair.

**Observed reality:**
- `StashAppContent` already owns the `val start by rootViewModel.start.collectAsState()` binding at line 174 (pre-existing for navigation start-destination logic).
- Step 0 dedup check returns `1` → **Case B path** (reuse existing binding).
- The cleanest implementation of Case B is to pass `appReady: AtomicBoolean` into `StashAppContent` as a parameter and attach the two `LaunchedEffect` blocks adjacent to the existing `val start` line (inside `StashAppContent`), rather than introducing a NEW collection inside `setContent { }` before the call.

**Proposed change (executed):**
- Keep `installSplashScreen()` + `setKeepOnScreenCondition` + `appReady = AtomicBoolean(false)` on MainActivity (per plan).
- Thread `appReady` into `StashAppContent(rootViewModel, appReady)` so the LaunchedEffects can attach next to the existing single `collectAsState` call.
- Add `LaunchedEffect(start) { if (start != null) appReady.set(true) }` immediately after the existing `val start by …collectAsState()`.
- Add `LaunchedEffect(Unit) { delay(3000); appReady.set(true) }` safety timeout (per RESEARCH §A4/§E3) adjacent.

**Evidence:**
- Pre-modification: `grep -c 'rootViewModel.start.collectAsState' app/src/main/java/io/stashapp/android/MainActivity.kt` → `1`
- Plan Step 0 explicitly authorizes Case B: "DO NOT add a duplicate. Instead, REUSE the existing binding".

**Does NOT break a SPEC must_have:**
- SPEC truth: "Cold launch displays the system Splash (Theme.Stash.Splash) until RootViewModel.start emits a non-null route" — preserved (the gate-flip pair sits adjacent to the existing collection of `rootViewModel.start`).
- Acceptance criterion: `grep -c 'rootViewModel.start.collectAsState' MainActivity.kt` → EXACTLY `1` (the dedup invariant from REVIEWS SHOULD FIX #2 — preserved by parameter-threading approach).

**Outcome:** Plan's intent satisfied; the parameter-threading is a minor structural choice that improves clarity. Acceptance grep set unchanged.

---

## DEV-02 — Task 3 (COMPLY-01): `safeDrawingPadding` wrap targeted at PlayerControls + locked-overlay only (NOT all non-AndroidView UI)

**Original plan instruction (Task 3, Step 3):**
> If the existing PlayerScreen has multiple sibling overlay Boxes/Columns alongside the AndroidView, all of them should be moved INSIDE the new `Box(safeDrawingPadding())` wrap — anything that is a UI control (not the video surface) gets inset.

**Observed reality (PlayerScreen.kt 1122 lines, sub-elements inside `Box(fillMaxSize+black)`):**
1. `AndroidView { PlayerView }` at line 211 — MUST stay full-bleed (video surface)
2. Gesture-detection `Box(fillMaxSize).pointerInput { detectTapGestures / detectHorizontalDragGestures }` at lines 228-295 — MUST stay full-bleed (taps/drags must capture across the entire video surface; wrapping in `safeDrawingPadding` would create dead-zones near system bars where the user can't toggle controls or scrub)
3. `CircularProgressIndicator` loading indicator (line 297) — full-bleed centered indicator; insets don't matter (it's centered, system bars don't overlap centered content)
4. `StepSeekCallout` step-seek visual feedback (lines 303-304) — gesture indicator; appears centered on a side; should NOT inset (user expects callout where their finger is, even near edges)
5. `ScrubPreviewCard` (lines 306-312) — centered; same as #3
6. `BannerPill` (lines 314-321) — top-aligned with explicit `.padding(top = 64.dp)`; already inset
7. `Text("Playback error...")` (lines 323-327) — centered; same as #3
8. **Locked-state overlay (lines 330-347)** — control (unlock button); currently inset via `.padding(WindowInsets.systemBars.asPaddingValues())`
9. **PlayerControls AnimatedVisibility (lines 350-456)** — main control overlay; internally already applies `WindowInsets.systemBars.asPaddingValues()` (line 496 inside PlayerControls)

Items #2 (gesture layers) and #4 (gesture callouts) MUST stay full-bleed for UX correctness. Items #1, #3, #5, #6, #7 don't need inset wrapping (centered or already-padded).

The only true "control overlays" per the plan's enumeration ("top bar / bottom seek bar / gesture indicators") are items #8 (locked unlock button) and #9 (PlayerControls top bar + bottom seek bar).

**Proposed change (executed):**
- Wrap **only items #8 and #9** in a single `Box(Modifier.fillMaxSize().safeDrawingPadding())`.
- Remove the redundant internal `WindowInsets.systemBars.asPaddingValues()` from those overlays to avoid double-padding (the outer `safeDrawingPadding` now handles it).
- Leave gesture layers (#2), gesture callouts (#4), loading (#3), preview card (#5), banner (#6), and error text (#7) outside the wrap — they remain full-bleed.

**Evidence:**
- OpenCode REVIEWS LOW concern: "composables that intentionally overlap the video surface (e.g., subtitle overlays, thumbnail grids), they should NOT be inside the safeDrawingPadding wrapper. The executor must read the full file and verify the wrap scope includes only chrome/controls, not video-overlay elements that should remain full-bleed."
- SPEC must_have: "PlayerScreen control overlays are wrapped in Box(Modifier.safeDrawingPadding()); the SurfaceView itself stays edge-to-edge" — satisfied by wrapping items #8 + #9, the true control overlays.
- Anti-coupling rule (CONTEXT.md `<spine_forward_compatibility>`): structural insetting only; no Spine tokens introduced.

**Does NOT break a SPEC must_have:**
- All COMPLY-01 acceptance criteria (grep for `safeDrawingPadding` count >= 2) satisfied with the targeted wrap.
- Gesture UX preserved (PITFALLS §7 — 3-button-nav users will still be able to tap-toggle controls across the entire screen).

**Outcome:** Plan's intent satisfied with surgical precision; preserves the existing gesture-detection UX while inset-padding the chrome.
