# Phase 2 Plan 02.2 — Partial Summary (Tasks 1-3 complete; Task 4 checkpoint)

**Status:** Awaiting human verification at Task 4 (S23+ UAT screenshot capture).
**Branch:** `phase-2/comply-platform-compliance`
**Cumulative Phase 2 commits so far:** 6 (3 from Plan 02.1 + 3 from Plan 02.2 Tasks 1-3).
**Plan 02.2 budget for remaining commits:** 2 more (Task 4 produces no commit — it's a checkpoint that hands off to the human; Task 5 produces the final UAT+screenshots commit).

---

## Tasks completed

### Task 1 — COMPLY-03 + COMPLY-05 (orphan permission removal)

Two atomic commits per CONTEXT.md Decision 7 default (each removal independently bisect-meaningful).

| Commit | SHA | Subject |
|--------|-----|---------|
| COMPLY-05 | `481c303` | `chore(comply): COMPLY-05 — remove orphan FOREGROUND_SERVICE_MEDIA_PLAYBACK permission` |
| COMPLY-03 | `1aff210` | `chore(comply): COMPLY-03 — remove orphan POST_NOTIFICATIONS permission` |

**Files touched:** `app/src/main/AndroidManifest.xml` (2 deletions across the 2 commits).

**RESEARCH A7 safety re-check:** `grep -rn 'NotificationManager\|NotificationCompat\|\.notify('` in `feature/ app/ core/` returned zero matches before COMPLY-03 deletion — confirmed safe.

**Plan 02.1 manifest edits preserved:**
- `android:enableOnBackInvokedCallback="true"` — present.
- `@style/Theme.Stash.Splash` on `MainActivity` — present.

**Build:** `./gradlew --configuration-cache assembleDebug` green after each commit.

### Task 2 — COMPLY-06 (per-app language picker)

Single atomic commit covering the build flag, the LanguageRow composable, and the strings.

| Commit | SHA | Subject |
|--------|-----|---------|
| COMPLY-06 | `e215699` | `feat(comply): COMPLY-06 — per-app language picker (generateLocaleConfig + LanguageRow + ACTION_APP_LOCALE_SETTINGS)` |

**Files touched (5):**
- `app/build.gradle.kts` — `androidResources { generateLocaleConfig = true }` sibling of `defaultConfig`.
- `app/src/main/res/values/strings.xml` — `settings_language` + `settings_language_description` keys added (per CONTEXT.md lock).
- `app/src/main/res/resources.properties` — NEW; `unqualifiedResLocale=en` (required by AGP 8.7.3 when `generateLocaleConfig=true`).
- `feature/settings/src/main/res/values/strings.xml` — NEW; co-located strings (see Deviation #1 below).
- `feature/settings/src/main/java/io/stashapp/android/feature/settings/SettingsScreen.kt` — added `LanguageRow()` composable with `Build.VERSION.SDK_INT < TIRAMISU` early return + `ACTION_APP_LOCALE_SETTINGS` intent fire; placed under a new top-of-scroll `SectionHeader("App")`.

**R-class audit result (per REVIEWS SHOULD FIX #5 Step 0):** The grep returned **0** hits of `import io.stashapp.android.R` in `feature/`. Per the plan's third decision branch (and the build failure that confirmed it), the strings had to be co-located in the feature module's own `res/values/strings.xml` because the Gradle dependency direction is `app → feature/settings` (never the reverse) — `io.stashapp.android.R` is not visible from `feature/settings`. Both copies of the strings exist in the repo (app + feature); AGP merges resources at the app level so duplicates are harmless. Full rationale in `02.2-DEVIATIONS.md` #1 and in the commit body.

**AGP-generated locale_config:** `app/build/generated/res/localeConfig/debug/xml/_generated_res_locale_config.xml` exists (AGP uses an underscore-prefixed filename, not bare `locale_config.xml` — see Deviation #3). Merged debug manifest references `android:localeConfig="@xml/_generated_res_locale_config"` correctly. Source `AndroidManifest.xml` untouched (Pitfall E6 honored).

**Build:** `./gradlew clean assembleDebug` green after the commit (proving the feature-R reference resolves).

### Task 3 — Backlog seeding (REQUIREMENTS.md Deferred section)

Single atomic commit, docs-only.

| Commit | SHA | Subject |
|--------|-----|---------|
| backlog | `9c65e91` | `docs(comply): seed COMPLY-07-3BTN + COMPLY-02-NAV-EVENT in REQUIREMENTS Deferred section` |

**File touched (1):** `.planning/REQUIREMENTS.md` (8 lines inserted; new subsection `### Phase 2 COMPLY — partial-coverage carry-forward (auto-logged 2026-05-17)`).

Both entries follow the DEPS-16 / REVIEWS-C4 hygiene format with explicit revisit triggers (hardware availability OR AGP-9 phase / Activity Compose 1.13+).

`git diff HEAD~1 HEAD --name-only` lists only `.planning/REQUIREMENTS.md` — no leakage.

---

## Verification snapshot (post-Task-3)

All Task 1-3 grep invariants pass:

```
POST_NOTIFICATIONS:        0  (removed)
FGS_MEDIA_PLAYBACK:        0  (removed)
base FOREGROUND_SERVICE:   1  (preserved per CONTEXT.md §7)
enableOnBackInvokedCallback: 1 (Plan 02.1 preserved)
Theme.Stash.Splash:        1  (Plan 02.1 preserved)
generateLocaleConfig:      1
settings_language (app):   2  (key + description)
ACTION_APP_LOCALE_SETTINGS: 2  (1 code, 1 doc comment)
SDK_INT TIRAMISU gate:     1
private fun LanguageRow:   1
LanguageRow() call site:   2  (1 def, 1 call)
Uri.fromParts("package":  1
AGP locale_config files:   2  (intermediates + packaged)
source manifest localeConfig: 0  (Pitfall E6 — clean)
COMPLY-07-3BTN in REQS:    1
COMPLY-02-NAV-EVENT in REQS: 1
PITFALLS §7 in REQS:       1
REVIEWS-C4 ACCEPT in REQS: 1
```

Cumulative Phase 2 commits since master:
```
9c65e91 docs(comply): seed COMPLY-07-3BTN + COMPLY-02-NAV-EVENT in REQUIREMENTS Deferred section
e215699 feat(comply): COMPLY-06 — per-app language picker (generateLocaleConfig + LanguageRow + ACTION_APP_LOCALE_SETTINGS)
1aff210 chore(comply): COMPLY-03 — remove orphan POST_NOTIFICATIONS permission
481c303 chore(comply): COMPLY-05 — remove orphan FOREGROUND_SERVICE_MEDIA_PLAYBACK permission
73ef77e docs(02.1): summary — plan execution complete                     [Plan 02.1]
2572aad feat(comply): COMPLY-01 — strip themes.xml bar colors ...         [Plan 02.1]
0b1f4f5 feat(comply): COMPLY-02 — predictive back manifest flag ...       [Plan 02.1]
ba7ff55 feat(comply): COMPLY-04 — Splash Screen API via core-splashscreen [Plan 02.1]
```

Build state: `./gradlew --configuration-cache assembleDebug` green. APKs ready at:
- `app/build/outputs/apk/debug/app-arm64-v8a-debug.apk` (32 MB — S23+ install target)
- `app/build/outputs/apk/debug/app-armeabi-v7a-debug.apk` (32 MB — 32-bit fallback)

(No universal APK because `splits { abi { isUniversalApk = false } }`.)

---

## Deviations (Tasks 1-3)

Full detail in `.planning/phases/02-comply-platform-compliance/02.2-DEVIATIONS.md`:

1. **R-class location** (Rule 3 — blocking issue auto-fix) — strings co-located in `feature/settings/res/values/strings.xml` because `feature/settings` cannot import `io.stashapp.android.R`. Both copies retained per CONTEXT.md lock; AGP merges resources at app level. R-class pattern decision documented in the COMPLY-06 commit body per REVIEWS SHOULD FIX #5.
2. **`resources.properties` created** (anticipated by plan) — required by AGP 8.7.3 when `generateLocaleConfig=true`; plan called this out as a possible warning, surfaced as a hard error.
3. **`locale_config.xml` filename** (documentation note, not a code deviation) — AGP names the generated file `_generated_res_locale_config.xml` (underscore prefix). Verification grep adjusted from `-name 'locale_config.xml'` to `-name '*locale_config*'`. Merged manifest references the correctly-named-by-AGP resource (`@xml/_generated_res_locale_config`).
4. **Local untracked files** preserved (`local.properties` with local SDK path; `AGENTS.md`; `UI_HANDOFF.md`; `design_handoff_slopper_spine/`) — out of plan scope.

---

## Task 4 — HUMAN CHECKPOINT (now)

`type="checkpoint:human-verify"` — manual S23+ UAT screenshot capture (12 PNGs).

**Cannot be automated.** Per Plan 02.2 + the orchestrator instructions:

- Build is green; APKs ready (see paths above).
- Full UAT instructions written to `.planning/phases/02-comply-platform-compliance/02-UAT-INSTRUCTIONS.md` — includes:
  - The locked 12-PNG naming schema.
  - The Light/Dark capture procedure.
  - Reviewer checklist (5 visual checks).
  - SCREENSHOTS.md template + 02-UAT.md row scaffold for the Task 5 executor.

**Resume signal expected from the user:**
> `approved — S23+ gesture-nav capture complete, 12 PNGs in place, verdict PASS`
> (or `PASS-WITH-NOTES` + the note, or `FAIL` + the regression description).

**What Task 5 will do after resume:**
- Write `SCREENSHOTS.md` (per Plan 02.2 Step 1 template; 30+ lines; device + build SHA + reviewer metadata).
- Write `02-UAT.md` (per Plan 02.2 Step 2 template; one row per `DEVICE_TESTING.md` checklist item; gesture-nav rows PASS; 3-button-nav rows DEFERRED → COMPLY-07-3BTN; REVIEWS-C4 ACCEPT block + Reviewer Sign-Off block).
- Single atomic commit: `docs(comply): COMPLY-07 — S23+ gesture-nav UAT + 12-PNG screenshot pack (3-button-nav DEFERRED → COMPLY-07-3BTN)`.
- Bring Phase 2 commit count to 8 (matching CONTEXT.md Decision 2 budget).
- Update SPEC-LAYER.md / produce final SUMMARY (handled by orchestrator after Task 5).

---

## Open items for the human

1. Flash `app/build/outputs/apk/debug/app-arm64-v8a-debug.apk` on your Galaxy S23+ (gesture nav, Android 16). Install instructions in `02-UAT-INSTRUCTIONS.md` §1.
2. Capture the 12 PNGs per `02-UAT-INSTRUCTIONS.md` §2-3.
3. Run the 5 reviewer checks per `02-UAT-INSTRUCTIONS.md` §4.
4. Hand back with verdict + (if PASS-WITH-NOTES) the note.

If you observe a regression that the executor must fix before re-capturing:
describe the screen + the regression in plain English. The follow-up executor
will open a fix commit on the same branch before Task 5 re-tries the writeup.
