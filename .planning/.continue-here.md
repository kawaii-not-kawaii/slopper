---
context: default
phase: post-v1.1 maintenance — quality gates restored; APK delivered; device UAT pending
task: install and smoke-test the delivered Slopper APK; use CI-gated PR delivery for repository changes
total_tasks: n/a
status: paused
last_updated: 2026-08-01
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **`git add` aborts the whole command on one bad pathspec → silent partial staging** — a multi-path `git add` that hits one invalid path stages NOTHING and exits non-zero, but a follow-on `git commit` still commits whatever was already staged. This broke the icon commit TWICE (committed the XML repoint + drawable deletion without the new PNGs → a checkout-breaking commit). Mitigation: after staging, ALWAYS run `git diff --cached --stat` and verify the FULL expected fileset before committing. Never assume `git add` succeeded.

**Do not proceed until checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Partial-staging commit | `git add a b badpath` aborts → commit lands an incomplete set (broke icons twice) | blocking | `git diff --cached --stat` must show every expected file before `git commit`; verify counts |
| Piped exit code masking | `tailscale file cp … \| head` reported "SENT" on a FAILED copy (pipe exit = head's) | advisory | Run success-critical commands WITHOUT a pipe, or check `${PIPESTATUS[0]}` / explicit `if cmd; then` |
| Kotlin 2.2.x under AGP 9 | 2.2.x is a hard dead-end on AGP 9 — built-in Kotlin needs KSP2/Kotlin-2.3; legacy `kotlin.android` crashes (AGP-9 removed `BaseExtension`). Cost 4 blockers (B-08-01..04) | advisory | Toolchain is now Kotlin 2.3.20 + KSP 2.3.9 — do NOT attempt to "revert to stable 2.2.x" |
| Scorecard SARIF noise | Scorecard's governance checks uploaded as code-scanning alerts clutter the Security tab (they are not code bugs) | advisory | scorecard.yml keeps `publish_results` (badge) but no `upload-sarif`; CodeQL is the only code-scanning tool |

<current_state>
**Milestone v1.1 (AGP-9 Toolchain Modernization) is complete.** Phases 7–10 are COMPLETE. Quality, documentation, and launcher-name fixes are committed in `444d9a7` (`fix: restore project quality gates`). At handoff capture, local `master` was clean and one commit ahead of the local `origin/master` tracking ref.

The final local gate `./gradlew :app:assembleDebug detekt ktlintCheck test lint --no-daemon` passed on 2026-08-01 (896 tasks: 137 executed, 759 up-to-date). The JVM suite has 61 passing tests across 16 reports; there are no `androidTest` tests. The app label is now **Slopper** (`io.stashapp.android`, version `0.2.0-alpha`, versionCode 5).

`./gradlew :app:assembleRelease --no-daemon` passed after the commit. The arm64 release APK verifies with APK Signature Scheme v2 using the Android debug certificate because no release keystore is configured. Taildrop successfully sent `/home/yun/Slopper-0.2.0-alpha-arm64.apk` to the online Galaxy S23+ peer `yuniors-s23.twin-wezen.ts.net`; installation and current runtime UAT are not yet confirmed.
</current_state>

<completed_work>
**Roadmap (v1.1):**
- Phase 7 GRADLE-9 — complete (Gradle-9 readiness ADR + deprecation sweep; AGP-8.7.3 fold-forward)
- Phase 8 AGP-9 — complete. Historical Phase-8 landing toolchain: **AGP 9.2.1, Gradle 9.4.1, Kotlin 2.3.20, KSP 2.3.9 (KSP2 + built-in Kotlin), Hilt 2.59.2, Apollo 5.0.0, compileSdk 36 / targetSdk 35, detekt 2.0.0-alpha.3 (`dev.detekt`), baseline-profile 1.5.0-alpha06.** Its phase gate was green (497 tasks).
- Phase 9 LIBS — complete. Media3 1.10.0 + nextlib 1.10.0-0.12.1 (locked pair, hard cap), activity-compose 1.13.0, core-ktx 1.18.0. Gate green (501 tasks).
- Phase 10 CI-SIGNING — complete.

**Current maintenance delivery (`444d9a7`):**
- Restored detekt, ktlint, and Android Lint gates; preserved Apollo cancellation/error mapping and expected paging/scene-data failures.
- Added narrowly scoped Media3 opt-ins and lifecycle-aware Compose state observation.
- Refreshed public/planning documentation and renamed the launcher label from Stash to Slopper.
- Built the arm64 release APK, verified its v2 debug-key signature, and delivered it to the Galaxy S23+ via Taildrop.

**Earlier off-roadmap work (historically committed and pushed):**
- Repo PUBLIC + GPL-3.0 + alpha-labeled (description/topics/README banner).
- OpenSSF Scorecard (6.3/10) — License/Security-Policy/SAST/Pinned-Dependencies all 10/10. SARIF→code-scanning upload disabled (badge kept).
- Hardening: SHA-pinned ci.yml actions + Dockerfile digest, CodeQL SAST workflow, SECURITY.md, Gradle wrapper-validation.
- Secret scanning + push protection + Dependabot security alerts enabled.
- dependabot.yml pruned (activity/core un-ignored; toolchain lockstep + media3 pair + benchmark-alpha kept ignored, with corrected rationale).
- New v3 "Sage" adaptive app icon (15 density PNGs + monochrome themed layer; old vector removed).
- Player fixes: scrims gated on `controlsVisible` + immersive system-bar hide.

**Historical on-device validation (Galaxy S23 `yuniors-s23`, before HEAD `09d4272`, via Taildrop):** the app ran and playback worked, validating the then-current Media3/nextlib FFmpeg software-codec native libraries. This is preserved as historical evidence, not evidence that the current APK has passed UAT.
</completed_work>

<remaining_work>
- **Install delivered APK:** open the Taildrop notification on the Galaxy S23+, install `Slopper-0.2.0-alpha-arm64.apk`, and uninstall any differently signed existing build first if Android reports a signature mismatch.
- **Current device UAT:** smoke launch, connect, library grid search/filter, scene detail, rating/organize/O-counter, markers/ChapterStrip, playback, PiP, and queue/shuffle/repeat.
- **Repository delivery:** if `444d9a7` is not yet on `origin/master`, deliver it only through a GitHub pull request and merge only after required CI passes.
</remaining_work>

<decisions_made>
- **D-04b LIFTED:** Kotlin 2.3.20 + KSP 2.3.9 + AGP-9 built-in Kotlin — the only path that compiles under AGP 9 (2.2.x proven dead).
- **AR-08-01:** baseline-profile remains a build-time-only stable-only exception; current plugin is 1.5.0-alpha07 (the original decision landed on alpha06).
- **AR-08-02:** detekt remains a build-time-only exception; current plugin is 2.0.0-alpha.5 (the original decision landed on alpha.3).
- Media3 HARD CAP at 1.10.0 (no nextlib 1.10.1 pairing).
- Repo public → fixed the CI billing block (private-repo free Actions minutes were exhausted).
- Full blocker chain B-08-01..04 + all accepted risks documented in `.planning/STATE.md`.
</decisions_made>

<blockers>
- No current compile, static-analysis, unit-test, lint, release-assembly, or Taildrop blocker is known.
- Current runtime behavior is unverified until the delivered APK is installed and smoke-tested on the Galaxy S23+.
- The release APK is debug-key signed because no release keystore is configured; it is suitable for sideload testing, not production distribution, and may require uninstalling builds signed with another key.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — the authoritative toolchain matrix, locked decisions, accepted risks (AR-08-01/02), and the full B-08-01..04 blocker chain narrative.
2. `.planning/ROADMAP.md` §Phase 10 — the completed CI-SIGNING scope + research flag.
3. `docs/adr/0001-gradle-9-readiness-and-agp8-fold-forward.md` — Phase-7 ADR (Gradle-9 readiness, plugin verdicts).
4. `.planning/phases/08-.../08-RESEARCH-2.3.x.md` — the verified Kotlin-2.3.x toolchain version web (why these exact versions).

## Infrastructure State
- Quality/delivery commit: `444d9a7` (`fix: restore project quality gates`). At handoff capture, local `master` was clean and one commit ahead of the local `origin/master` tracking ref; verify PR/CI/merge state live rather than relying on this snapshot.
- Repository: public on GitHub (`kawaii-not-kawaii/slopper`), GPL-3.0, alpha-labeled; app version `0.2.0-alpha`, launcher label `Slopper`.
- CI workflows exist for both GitHub and Forgejo. This handoff does not assert a live remote workflow result.
- Current toolchain: Gradle 9.4.1, AGP 9.2.1, Kotlin 2.3.20, KSP 2.3.9, Hilt 2.59.2, Apollo 5.0.1, Media3 1.10.0 + nextlib 1.10.0-0.12.1, detekt 2.0.0-alpha.5, baseline-profile 1.5.0-alpha07; compileSdk 36, targetSdk 35, minSdk 26.
- Local gate: `./gradlew :app:assembleDebug detekt ktlintCheck test lint --no-daemon` passed 2026-08-01 (896 tasks: 137 executed, 759 up-to-date; 61 passing JVM tests in 16 reports; no `androidTest` tests).
- Release delivery: `app/build/outputs/apk/release/app-arm64-v8a-release.apk` copied to `/home/yun/Slopper-0.2.0-alpha-arm64.apk`; v2 signature verified with Android debug certificate; SHA-256 `56ec4577069398bb6d65c872678ba5ccade3527390b9b87271bb3d0fc40eb3ad`; Taildrop exit 0 to `yuniors-s23.twin-wezen.ts.net`.

<context>
The AGP-9 milestone is complete, the current quality gates are green, and the launcher label is Slopper. Commit `444d9a7` contains the verified code/documentation changes. The arm64 release APK was built, signature-verified, and Taildropped, but installation and runtime UAT remain unverified. Repository delivery must use a GitHub PR and must not merge before CI passes. The Kotlin 2.2.x→2.3.x blocker chain remains resolved historical context and should not be re-litigated.
</context>

<next_action>
1. Install `Slopper-0.2.0-alpha-arm64.apk` from Taildrop on the Galaxy S23+; uninstall a differently signed existing build if Android rejects the debug certificate.
2. Run and record current device UAT (launch → connect → library → scene detail → markers/playback/PiP/queue).
3. If the quality/delivery commit is not yet merged, push it through a GitHub pull request, watch required CI to completion, and merge only after every required check passes.
</next_action>
